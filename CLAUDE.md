# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Commands

```bash
npm run dev              # start dev server (Turbopack)
npm run build             # production build (also runs the TypeScript check)
npm start                 # serve the build from `npm run build` (production mode) — for *-prod.mjs scripts below
npm run lint               # ESLint (flat config, eslint.config.mjs)
npm run locations:import:kocaeli   # fetch+filter OSM/Overpass candidates for one province
npm run locations:verify           # polygon-verify, dedupe, score candidates -> data/locations/*.json
npm run locations:report           # print a verification summary from data/locations/*.json
npm run locations:test-filter      # unit-test the Overpass candidate filter logic
npm run test:photo-feature   # pure-logic tests for app/_lib/photo-validation.ts, no server needed
npm run test:photo-api       # E2E test of the /api/job-photos/process route against a running dev server
npm run test:photo-browser   # Playwright E2E for the photo-upload UI, against a running dev server
npm run test:regression      # Playwright smoke test that unrelated flows still work, against a running dev server
npm run test:location-system # Playwright E2E for the İl/İlçe/Bölge-Tesis picker (job-location.ts), against a running dev server
```

There is no Jest/Vitest — "tests" are plain Node scripts under `scripts/`, run directly (`node scripts/....mjs`), not through a runner. Two kinds:
- **Pure-logic** (`test-photo-feature.mjs`, `locations/test-filter.mjs`): import the `.ts` module directly using Node's built-in TypeScript support, no server needed.
- **Browser/E2E** (`test-photo-api.mjs`, `browser-test-*.mjs`): require `npm run dev` already running on `localhost:3000` first (`test-photo-api.mjs` hits the route with `fetch`; the `browser-test-*.mjs` ones drive real Chromium via Playwright). `test:photo-browser` needs fixture images from `node scripts/generate-photo-test-fixtures.mjs` first.

Treat `npm run lint` + `npm run build` as the baseline correctness gate for app code; use the scripts above (or a throwaway `node -e "..."`) to independently cross-check pure logic (money parsing, phone normalization, geo functions, photo validation) before trusting it.

`scripts/tmp-*.mjs` (e.g. `tmp-completion-flow-test.mjs`, `tmp-withdraw-offer-test.mjs`) are one-off Playwright verification scripts written alongside a specific feature commit and left committed afterward as a record — they are not npm scripts and aren't meant to be maintained as a regression suite. Follow the same pattern for a new feature (`tmp-<feature>-test.mjs`, requires `npm run dev` running) rather than editing an old one to test something unrelated. A `-prod` suffix (e.g. `tmp-verify-provider-profile-prod.mjs`, `tmp-production-mode-route-audit-test.mjs`) marks the same kind of script pointed at a **production build** (`npm run build && npm start`, not `npm run dev`) instead of dev — used to catch things dev mode can't (the `NODE_ENV === "development"` dev-seed-account gate being off, real minified bundle/chunk 404s, etc.); these register fresh accounts through the real Kayıt Ol flow rather than relying on `DEV_ACCOUNTS`; there are already over a dozen of these, so add a new `-prod` script rather than trying to parameterize an existing dev-mode one.

`scripts/tmp-supabase-*.mjs` (e.g. `tmp-supabase-auth-integration-test.mjs`, `tmp-supabase-auth-session-persistence-test.mjs`, `tmp-supabase-provider-profile-integration-test.mjs`) are a third kind, specific to the Supabase Auth migration (see "Supabase Auth migration" below): plain `@supabase/supabase-js` scripts (no Playwright), needing a running dev server, `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` in the environment, and a real Supabase project to sign real users up against — not runnable against a mocked/offline backend. Some also take a `SB_SECRET_KEY_FOR_TEST` env var at invocation time (never committed) to drive admin-only assertions. Follow this pattern (`tmp-supabase-<flow>-test.mjs`) for new Supabase-auth-flow verification rather than extending an existing script.

`scripts/test-customs-documents-and-badges.mjs` is a fourth, fundamentally different kind: a **permanent** security regression test (its own header comment explains why it deliberately does *not* carry the `tmp-` prefix — every other script here is a point-in-time record, this one is meant to be maintained). It targets only a fully local, isolated Docker Supabase stack (`npx supabase start`; URL/keys are hardcoded from `npx supabase status` output, never read from `.env.local`, specifically so it can never accidentally hit the hosted dev project) and asserts three things: that the reverted `job_customs_documents` infrastructure (see "Provider verification badges" below) never reappears, that `provider_documents` visibility stays company-owner-or-admin-only, and that the `badge_types`/`provider_badges` grant/revoke/history flow works. Follow this pattern — a real Docker-only regression test, no `tmp-` prefix — only for something meant to guard a fixed rule going forward; use `tmp-supabase-*` for one-off flow verification instead.

**Local Supabase stack** (Docker Desktop required, not an npm script): `npx supabase start` / `npx supabase stop` / `npx supabase status` (prints the local URL + anon/service-role keys — where the hardcoded values in `test-customs-documents-and-badges.mjs` come from) / `npx supabase db reset` (drops and re-applies every migration under `supabase/migrations/` from scratch against the local Docker Postgres — the way to sanity-check a migration file edit before it's pushed anywhere). `project_id = "malsevk-2"` in `supabase/config.toml`. This is the same local stack the "Future Supabase/PostgreSQL design" validation history (below) describes running its Docker dry-run against — unrelated to the hosted dev project (`NEXT_PUBLIC_SUPABASE_URL` in `.env.local`) that `npm run dev` and the `tmp-supabase-*.mjs` scripts talk to.

## Architecture

**Stack:** Next.js 16 (App Router, Turbopack), TypeScript, React 19, Tailwind CSS v4. No persistent server-side state for most of the app's actual data — nearly everything is still client-side `localStorage`, written as typed data-layer modules under `app/_lib/`. **The one major exception, and a live in-progress migration, is user auth/session**, which now runs on real Supabase Auth rather than localStorage — see "Supabase Auth migration" below before touching `session.ts`, `users.ts`, `login-form.tsx`, or anything under `app/_lib/supabase*`/`app/auth/`/`app/kayit-tamamla/`/`app/sifre-*`. Every other feature (`job-store`, `offers`, `notifications`, `ratings`, provider profiles, ...) still reads/writes `localStorage` exclusively, as described throughout the rest of this document, with a growing list of exceptions: job creation dual-writes (best-effort, opt-in behind `NEXT_PUBLIC_ENABLE_SUPABASE_JOB_SYNC`; localStorage stays authoritative either way — see "Supabase Geçişi Faz 2–4" below); contact messages (`app/bize-ulasin`) are now **fully** Supabase-backed with no localStorage fallback at all (see "Bize Ulaşın" below); and provider service-category **authorization** (distinct from mere *selection*, which is still `provider-services.ts`/localStorage) is Supabase-only by design, since a grant/revoke must be visible across devices (see "Service Authorization" below). There are four Route Handlers: `app/api/job-photos/process/route.ts` (stateless image processing that can't run in the browser; see "Photo upload pipeline" below), `app/api/provider-documents/validate/route.ts` (stateless document validation, same shape — see "Provider document verification" below), `app/api/health/route.ts` (a DB connectivity check — see "Database scaffold" below), and `app/auth/confirm/route.ts` (the Supabase email-link landing point — see "Supabase Auth migration" below). These four, plus `proxy.ts`'s own middleware logic, are the only server-side code in the app. `tsconfig.json` defines a `@/*` path alias but nothing actually imports through it — every real import in the codebase is relative (`../_lib/...`, `./...`); match that convention rather than introducing `@/`-style imports. `next.config.ts#experimental.proxyClientMaxBodySize` is set to `"20mb"` — Next.js 16's proxy buffers request bodies to 10MB by default and silently truncates anything larger (no error), which broke `sharp` decoding on real uploads; raised to cover the 15MB document-upload limit plus multipart overhead. Bump this if either route's own size limit (`photo-validation.ts`/`document-validation.ts`) ever increases.

### Directory roles

- `app/_components/` — UI components (route-excluded via the `_` prefix). Presentational components have no hooks; anything reading session/localStorage is a `"use client"` component.
- `app/_lib/` — the entire data/business layer: localStorage-backed "tables" (`users.ts`, `job-store.ts`, `offers.ts`), an IndexedDB blob store (`photo-blob-store.ts`), pure validation modules (`*-form-validation.ts`, `money.ts`, `phone.ts`, `password-rules.ts`, `photo-validation.ts`), a shared cross-source lookup (`jobs-lookup.ts`), and reactive hooks (`use-*.ts`). Five of the `use-*.ts` hooks are reusable UI-interaction primitives rather than data hooks — reuse them instead of reimplementing the same behavior locally: `use-dismiss-animation.ts` (measure element height → animate slide+fade+collapse → invoke a callback only after the animation completes, skipped entirely under `prefers-reduced-motion`; used by the notification list), `use-scroll-lock.ts` (iOS/Android-safe body scroll lock via `position: fixed` + scroll-offset restore, not plain `overflow: hidden`; used by the mobile nav menu), `use-dropdown.ts` (open/close state + outside-click/Escape-to-close, with an `extraRefs` escape hatch for portal-rendered content that `Node.contains()` would otherwise see as "outside"; used by the mobile menu and profile/notification dropdowns), `use-auto-dismiss-banner.ts` (time-triggered, not user-triggered: shows a standalone banner for a fixed duration then fades it out — deliberately separate from `use-dismiss-animation.ts`'s click-triggered slide+collapse; used by the job-requests panel's delete confirmation banner), and `use-media-query.ts` (a `matchMedia`-backed `useSyncExternalStore` hook for responsive JS branching — see "Provider job listing" below for why this is safe from hydration mismatches here but wouldn't necessarily be elsewhere).
- `app/api/job-photos/process/` — `runtime = "nodejs"` (needs `sharp`'s native bindings, won't run on Edge). Stateless request-in/image-out; writes nothing server-side.
- `app/_data/turkey/` — static reference JSON for provinces and districts only (`provinces.json`, `districts.json`, complete for all 81 provinces), bundled into the app at build time. Facility data (OSB/port/free-zone/etc.) does **not** live here — see "Turkey location data" below.
- `data/` (repo root, **not** under `app/`) — output of the location-verification pipeline: `data/locations/locations.json` (the single facility dataset), `data/locations/location_suggestions.json`, `data/locations/location_manual_overrides.json`, `data/locations/raw/*.json` (per-province Overpass fetch cache), and `data/boundaries/*.geojson` (district polygons, currently only Kocaeli). Read by `app/_lib/turkey-locations.ts` via a relative import; regenerated by the scripts below, not hand-edited.
- `scripts/locations/` — standalone Node ESM (`.mjs`, no TypeScript, no ts-node) CLI pipeline: Overpass API fetch → district-polygon verification → dedup → confidence scoring → writes to `data/locations/`. Not part of the Next.js build; run manually via the `locations:*` npm scripts. `migrate-legacy.mjs` is a one-off (already-run) script, not an npm script — see "Turkey location data" below.

### No real backend: what that means in practice

- "Tables" are localStorage-backed modules, not a database. Reads/writes go through a cache-plus-`useSyncExternalStore` pattern (see `job-store.ts`, `offers.ts`, `users.ts#usersStore`): a module-level cache compares the raw localStorage string to avoid returning new object references on every render, `getServerSnapshot` always returns `null`/`[]` for SSR safety, and writes call an internal `notify()` so every subscribed component re-renders. **`session.ts` is the one module that no longer follows this pattern** — see "Supabase Auth migration" below.
- **Do not use `useEffect` + `setState` to read localStorage on mount** — the project's ESLint config enforces `react-hooks/set-state-in-effect` and will fail lint on that pattern. Use the existing `useSyncExternalStore`-based hooks (`use-session.ts`, `use-jobs.ts`, `use-offers.ts`, `use-users.ts`) or extend that pattern for new localStorage-derived state.
- **Writes must not silently claim success.** `local-storage.ts#writeJson` wraps `localStorage.setItem` in try/catch, logs the real error via `console.error`, and returns a `boolean` (not `void`) — `false` on failure, with the module-level cache/`notify()` left untouched so subscribers keep seeing the last-known-good data instead of a phantom update. Every table's internal write wrapper (`job-store.ts#writeUserCreatedJobs`, `offers.ts#writeAllOffers`, `users.ts#writeUsers`, `notification-reads.ts`/`notification-dismissals.ts#writeIds`) propagates that boolean, and every public mutation (`createJob`, `updateJob`, `createOffer`, `updateOfferStatus`, `registerUser`, ...) checks it and returns `{ ok: false, error: STORAGE_WRITE_ERROR_MESSAGE }` (the one shared, non-technical error string, exported from `local-storage.ts`) instead of the normal success value when the underlying write actually failed — never `console.error`-and-continue. Side effects that depend on a write succeeding (deleting an old photo blob, deleting a stale logo blob) are ordered *after* the guarded write, not before, so a failed write can never leave a dangling reference to an already-deleted blob (see `job-store.ts#deleteJobPhoto`'s write-then-delete order). `deleteJobWithOffers` is the one deliberate exception: its secondary `offers` write (flipping sibling `pending` offers to `rejected`) is best-effort after the primary job deletion already succeeded — a failure there is logged, not surfaced as an overall failure, since unwinding an already-successful deletion would need real rollback machinery this codebase doesn't have.
- `app/_lib/users.ts#hashPassword`/`registerUser`/`verifyLogin` (unsalted SHA-256 via `crypto.subtle`) are **legacy, pre-Supabase-Auth-migration code**, explicitly documented in that file as a dev-only stand-in — real user accounts no longer go through them at all (see "Supabase Auth migration" below); they now only run for the dev-only seed accounts (`seedDevAccountsIfNeeded`, below) and inside the now-orphaned `provider-registration.ts` (kept in the tree but no longer called by any UI).
- `session.ts` no longer re-validates a stored session object against `StoredUser` (the pre-migration "K1" audit fix, hand-editing `localStorage["malsevk.session.v1"]` to claim `role: "admin"`, is now moot — that key is actively deleted on load, see below) — the real authorization boundary is now a live Supabase Auth session plus `profiles.role`, checked server-side for admin routes via `require-admin.ts`'s `is_admin()` RPC call. The historical, still-relevant caveat carries forward unchanged in spirit: RLS/RPC design is the real boundary, not anything client-readable — see "Supabase Auth migration" below and the RLS/RPC design under "Future Supabase/PostgreSQL design".
- **The old six-account `DEV_ACCOUNTS` localStorage seed described in earlier versions of this doc is gone, not just unwired.** `users.ts#seedDevAccountsIfNeeded`/`upsertDevAccount`/`seedNakliyeciProviderProfileIfNeeded`/`seedGumrukMusaviriProviderProfileIfNeeded`, and the `DEV_ACCOUNTS` array itself (which held plaintext dev passwords), were all deleted outright — they were still statically bundled into production client JS (via `DEV_ACCOUNT_EMAILS` importing from the same module) despite nothing having called any of them since the Supabase Auth migration, so a later audit ("Kritik İlan Senkronizasyonu" bölüm 8, see below) removed the dead code rather than leaving it unreachable. `DEV_ACCOUNT_EMAILS` itself survives, now sourced from a separate password-free list, so `reset-demo-data.ts` is unaffected. `admin` is otherwise unreachable: it is never a selectable "Hesap Türü" in the registration form (`types.ts#UserRole`'s third value), so real Supabase accounts (see next) are its only creation path today. See "Kritik İlan Senkronizasyonu & Development Temizliği" below for the new dev-login mechanism that replaced this.
- Authorization checks (role checks, ownership checks, offer-status transitions) are implemented once in the `_lib` data-layer functions (e.g. `offers.ts#createOffer`/`updateOfferStatus`) and called from UI — never re-implemented ad hoc in a component. `contact-access.ts#getRevealedContactForOffer` is the single gate for revealing a counterparty's phone/email (only when an offer is `accepted` and the caller is a party to it); nothing else should read raw phone/email off a `StoredUser`. Within that gate, each field is further filtered through `applyContactVisibility`, which reads the *owning* user's own opt-out preference — `StoredUser.showEmailAfterAgreement`/`showPhoneAfterAgreement` (`?? true`, so an unset preference behaves exactly like the pre-feature default of always-visible), settable by either role from Hesap Ayarları's "İletişim Bilgisi Görünürlüğü" card (`contact-visibility-settings.tsx` → `users.ts#updateContactVisibility`, deliberately not role-gated) — a user can hide their own phone and/or email even from a party who has otherwise earned contact access; `name` is never subject to this preference. `job-requests.ts#canViewJobAddress` is the parallel gate for `Job.addressText` — same `ENGAGED_OFFER_STATUSES` timing, but deliberately a separate function (not folded into `contact-access.ts`, which stays scoped to phone/email) with one extra exception: the job's own owner always sees it, regardless of offer status.
- The `/api/job-photos/process` route reads a client-supplied `role` field to gate uploads to `hizmet-alan` — this is defense in depth, not the authorization boundary. Since there's no real session on the server, a forged request could send any role; the actual authority check happens again server-side-equivalent in `job-store.ts#createJob`/`deleteJobPhoto` (the localStorage write path), which is what a spoofed request can't bypass.
- `reset-demo-data.ts` + `app/gelistirme/demo-veri-sifirla` (`notFound()` unless `NODE_ENV === "development"`) is a dev-only tool to wipe transactional data (jobs/offers/ratings/notification read+dismiss state/recently-viewed) belonging to the seed accounts (`DEV_ACCOUNT_EMAILS`) without touching the seed accounts themselves — a job counts as the seed account's if `requesterId` matches, an offer/rating if either it belongs to such a job *or* its `providerId`/`(rater/provider)Id` is a seed account (so a seed provider's offer on a *real* user's job is removed without touching that job or its other offers). There's no separate "operation" record to clean up (`Job.operationId` is just a field — deleting a seed account's jobs, including every sibling in a multi-service operation, already covers it) and no separate notification table either (`notifications.ts` derives them live, so they disappear once the underlying jobs/offers do — `DemoDataCounts#demoNotificationCount` only exists to *report* that, via the real `getNotificationsForSession`, not to delete anything). It has a manual dry-run-then-apply mode (`planDemoDataReset()` / `executeDemoDataReset()`, used by that page) and a one-time auto-migration (`runDemoDataResetMigrationIfNeeded()`) gated by a `localStorage` version flag — bump `DEMO_DATA_RESET_VERSION` to make it re-run once in browsers that already completed an earlier version. Note: `seedDevAccountsIfNeeded()` (below) only ever syncs the 3 accounts themselves — there is no seed for jobs/offers/notifications, so nothing here needs to be "disabled" for a reset to stick.

### Supabase Auth migration (in progress, uncommitted)

The current branch (`feat/supabase-migration`) is mid-migration from the fully-localStorage auth described above to real Supabase Auth. As of this writing only `supabase/migrations/0001`–`0022` are committed; **migrations `0023` (`provider_profile_and_document_write_paths.sql`) and `0024` (`provider_document_lifecycle.sql`), and every app-code file described in this section, are uncommitted working-tree state** — check `git status`/`git diff` rather than assuming this section describes committed history. Old and new systems deliberately **coexist**, not accidentally: this is a real in-progress state, not dead/orphaned code left by mistake, and every seam is commented in-file (Turkish, tagged "SUPABASE AUTH GEÇİŞİ" / "PROFİL/PROVIDER GEÇİŞİ") rather than tracked in a separate status doc.

- **Client setup** (`app/_lib/supabase/`): `env.ts` fail-fasts if `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` are unset — no `service_role`/secret key is ever read from app code. `browser-client.ts`/`server-client.ts` are Supabase's standard `@supabase/ssr` App Router pattern (the server one reads/writes cookies via `next/headers#cookies()`, async because Next 16's `cookies()` is async); `middleware-client.ts#updateSupabaseSession` is the one used from `proxy.ts` (see "Site-wide access gate" above) and deliberately calls `supabase.auth.getUser()`, not `getSession()`, forcing a real Auth-server round-trip so a forged/stale cookie can't fool route protection.
- **`session.ts` is a full rewrite, not an extension** of the pre-migration module: it no longer reads `localStorage["malsevk.session.v1"]` at all (that key is now actively deleted on first load as one-time cleanup) — the single source of truth is a real Supabase Auth session plus the user's `profiles` row (`role`/`full_name`/`account_status`/`onboarding_completed`), kept in an in-memory cache synced via `supabase.auth.onAuthStateChange`. The exported contract (`Session` type, `useSession()`, `sessionStore`) is **unchanged on purpose** so the 20+ existing callers didn't need edits. `setSession()`/`clearSession()` (the old imperative write API) are gone — real login/logout now go through `supabase.auth.signInWithPassword`/`signOut` directly (`login-form.tsx`/`profile-menu.tsx`), and the cache picks it up automatically. `useSession()` deliberately returns `null` both when `profiles.role` is still `NULL` (signup started but registration not completed) and when `account_status !== "active"` — the latter is explicitly documented in-file as a **client-side-only, best-effort gate**: no RLS policy/RPC in Faz 1 actually checks `account_status`, so a suspended user's JWT is technically still valid against a direct RPC call (see `docs/database/schema-reference.md` "Açık Kararlar" #6). A parallel, separate cache/store — `sessionProfileDetailsStore` + `use-session-profile.ts#useSessionProfileDetails()` — exposes the *full* profile row + auth email for "view my own profile" screens, kept apart from `Session` rather than widening that type; unlike `Session`, it stays populated even when `account_status !== "active"`, so account-settings screens can show a real suspended-account message instead of looking merely logged-out. `session.ts` also exports `refreshSession()`, an explicit re-fetch callers invoke right after a mutating RPC (`complete_registration`, a service-category update, ...) — `onAuthStateChange` only fires on real auth events, never on data mutations, so nothing else would pick up the change.
- **Two-step registration**: `login-form.tsx` calls `supabase.auth.signUp()`, stashes the form's non-sensitive fields via `pending-registration-draft.ts` (`sessionStorage`, never the password/email) since email confirmation may be clicked in a different tab, then either finishes immediately (if this dev Supabase project has email confirmation off and `signUp` already returned a session) or waits for the user to click the confirmation email. **`app/_lib/registration-metadata.ts`** is a second, redundant prefill path added after live testing found the `sessionStorage` draft alone wasn't enough: `signUp()` also writes the same non-sensitive fields (never password/email/documents) into Supabase's own `user_metadata`, so `/kayit-tamamla` can still prefill the form when the confirmation link is opened in a different tab *or device* where `sessionStorage` is empty — the form is always prefilled for review, never auto-submitted from metadata, and `complete_registration`'s `p_role` always comes from the form's own user-visible state, never from metadata (metadata's `role` is display-only). `app/auth/confirm/route.ts` is the single landing point for every Supabase email link and routes onward to `/sifre-guncelle`, `/kayit-tamamla`, or `/panel` — it handles **two** distinct link shapes, not one: `token_hash`+`type` via `verifyOtp` (the original path) and a PKCE `code` via `exchangeCodeForSession` (added after live testing against the real hosted dev project's default email templates, which return `code` once `@supabase/ssr`'s default `flowType` is in play — both signup confirmation and `type=recovery` password reset can arrive as either shape, so neither path was removed). `app/kayit-tamamla` (`complete-registration-form.tsx`) then calls `app/_lib/complete-registration.ts#finishSupabaseRegistration()` — idempotent (skips the RPC if `profiles.role` is already set, tolerates the RPC's own "already done" error code) — which calls the `complete_registration` RPC and then writes provider service categories/documents/consents the same way registration always has; the RPC call itself normalizes the phone number via `phone.ts` first (a real bug found and fixed during live testing — a locally-formatted number like `"0532 111 22 33"` was previously sent unnormalized and violated `profiles`' phone `CHECK` constraint). This is a **new, parallel function that sits alongside, not inside,** the pre-migration `users.ts#registerUser`/`provider-registration.ts#registerProviderAccount` — those two still exist, unmodified, but are no longer called by `login-form.tsx` for either role (`provider-registration.ts` is now orphaned/unused, kept only because deleting it was out of this migration's scope).
- **`users.ts`'s localStorage `StoredUser` table is demoted to a best-effort mirror, not replaced.** `upsertSupabaseUserMirror()` writes/updates a `StoredUser` row keyed by the real Supabase `auth.users.id` (with `passwordHash` set to the inert `SUPABASE_MANAGED_PASSWORD_MARKER` sentinel, never a real hash) so the many screens that still call `findUserById`/`getAllUsers` directly (`contact-access.ts`, `ratings.ts`, `my-offers-panel.tsx`, ...) keep working unmodified during the migration. `hashPassword`/`registerUser`/`verifyLogin` (the pre-migration SHA-256 auth) are untouched but now only exercised by the dev-seed path (see below) and the orphaned `provider-registration.ts` — `verifyLogin` is never called anywhere in the real login flow anymore.
- **Admin routes gained a real server-side check on top of the existing client-side one.** `require-admin.ts#requireAdminOrRedirect(path)` is `await`ed at the top of `app/admin/page.tsx` and `app/admin/iletisim-mesajlari/page.tsx` (both converted to `async` Server Components for this) — no Supabase user → `redirect("/giris-yap?...")`; a real user who fails the `is_admin()` RPC → `notFound()` (hides the route's existence, same pattern as `app/gelistirme/demo-veri-sifirla`). The panels' own pre-existing client-side `useSession()`/`AuthGateNotice` checks are left in place underneath as a second, now-redundant layer.
- **Several profile/provider screens now dual-write**: `provider-profile-editor.tsx` and `service-info-editor.tsx` still write to `StoredUser.providerProfile`/`provider-services.ts` as their authoritative, blocking path, but now also fire a best-effort remote write of bio/foundedYear/regions via `supabase-provider-profile.ts#upsertMyProviderProfileRemote` (both editors) and of the service-category selection via `supabase-provider-services.ts#setMyProviderServiceCategoriesRemote` (`service-info-editor.tsx` only) — surfacing a non-blocking `remoteSyncWarning` banner on failure, never rolling back or blocking the local write. Logo sync is narrower: only `provider-profile-editor.tsx` calls `supabase-provider-logo.ts#uploadMyProviderLogoRemote`/`deleteMyProviderLogoRemote` (Storage upload/delete, which internally calls `supabase-provider-profile.ts#setMyProviderProfileLogoPathRemote` to update the `provider_profiles.logo_path` column) — `service-info-editor.tsx`'s own logo field stays local-only for now. `basic-profile-editor.tsx` (new, in Hesap Ayarları) is the one editor that writes *only* remotely, via `supabase-profile-update.ts#updateMyProfileRemote()`, limited to the 6 `profiles` columns that actually have a column-level `UPDATE` grant (`full_name`/`phone`/`company_name`/`company_type`/`province`/`district`). `supabase-provider-documents.ts#uploadAndRegisterProviderDocument` (Storage upload + the `create_provider_document` RPC from migration `0023`) is currently only called from the registration flow (`complete-registration.ts`) — there is no post-registration "upload another document" UI wired to it yet.
- **Provider document lifecycle (migration `0024`, layered on `0023`)**: adds `'superseded'` as a 5th `provider_documents.current_review_status` value (alongside `pending`/`approved`/`rejected`/`revision_requested`) — settable only by `review_provider_document`'s own approval branch (approving a document auto-supersedes, never deletes, any *other* currently-`approved` row of the same provider+`document_type`, with no `provider_document_reviews` log row for the superseded side — treated as a side effect of the new approval, not an independent admin decision), never client-writable. Also adds a partial unique index, `provider_documents_one_pending_per_provider_type` on `(provider_id, document_type) WHERE current_review_status = 'pending' AND deleted_at IS NULL`, closing a duplicate-pending-row race found during live testing; `create_provider_document` now converts that index's `23505` into `MLK81`, already handled transparently by `uploadAndRegisterProviderDocument`'s existing orphan-Storage-cleanup-on-error path (no client change needed). `app/_lib/supabase-provider-document-review.ts` is the new remote read/write module: `listAllProviderDocumentsForAdminRemote()`/`listMyProviderDocumentsRemote()` (plain `select`s relying on RLS's `provider_id = auth.uid() or is_admin()` from `0013` to scope results — a non-admin caller of the "admin" list silently gets an empty list, not an error) and `reviewProviderDocumentRemote()` (wraps the `review_provider_document` RPC, mapping `MLK50`/`MLK71`/`MLK75`/`MLK76` to Turkish messages). Two new, actually-wired UI consumers: `admin-provider-document-review-panel-remote.tsx` — rendered on `app/admin/page.tsx` directly below the existing localStorage-based `AdminProviderDocumentReviewPanel`, separated by an `<hr>` and deliberately **coexisting** rather than replacing it (its own comment calls it the "gerçek Supabase karşılığı"/real Supabase counterpart) — only lets an admin act on `currentReviewStatus === "pending"` rows, since a superseded/already-decided row has no RPC path back; and `provider-document-status-list.tsx` ("Belgelerim"), rendered in `account-settings-content.tsx` for `hizmet-veren` sessions only, giving the pre-existing `notifications.ts#getProviderDocumentReviewNotifications` rejected/revision-requested deep link (which already pointed at `/panel/hesap-ayarlari`) a real destination, plus its own replacement-upload flow for exactly those two statuses via `uploadAndRegisterProviderDocument`.
- **New-device/incognito session hydration**: `app/_lib/hydrate-provider-mirror.ts#hydrateLocalUserMirrorIfMissing` is called fire-and-forget from `session.ts#refreshSessionCache`, right after a real Supabase session + `profiles` row is established — if this browser has no local `StoredUser` mirror yet for this user (`findUserById` returns `null`, i.e. never registered/logged in on this device before), it one-way-populates one from remote `profiles`/`provider_profiles`/`provider_services`/Storage-logo data via two new `users.ts` functions, `upsertSupabaseUserMirror()` (base fields, `passwordHash` set to the `SUPABASE_MANAGED_PASSWORD_MARKER` sentinel) and `hydrateProviderProfileFromRemote()` (`providerProfile` fields, `companyName` falling back via `||` — not `??` — to the top-level `profiles.company_name` since `provider_profiles` itself has no `company_name` column) — necessary because `ProviderProfileEditor`/`ServiceInfoEditor`/`ContactVisibilitySettings` still read the local mirror directly. Deliberately never overwrites an existing local record, so a prior remote-write failure on this device is never silently clobbered.
- **New pages/components**: `app/sifre-sifirla` (`forgot-password-form.tsx`, calls `resetPasswordForEmail`, always shows a neutral success message to avoid email-enumeration; uses the same synchronous ref-based double-submit lock as the registration form, added after live testing showed a fast double-click could fire two `resetPasswordForEmail` requests against Supabase's low hourly email quota — a plain `submitting` state check wasn't fast enough) and `app/sifre-guncelle` (`update-password-form.tsx`, calls `auth.updateUser({password})` on an active recovery session, then signs out and redirects to login) round out the password-reset flow that `app/auth/confirm/route.ts` feeds into. `supabase-auth-errors.ts#mapSupabaseAuthError()` is the one shared Supabase-`AuthError`-to-Turkish-message mapper used by login/kayıt/sifre-sifirla/sifre-guncelle.
- **Env vars**: `.env.example` documents `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` as the only Supabase vars — both intentionally public/client-bundled, no `service_role`/secret key is ever defined for app code. This is unrelated to `DATABASE_URL` (the separate, unwired Neon/Drizzle scaffold — see "Database scaffold" below).
- **Known, documented gaps, not oversights**: `ProviderProfile.expertise`/`companyName`-on-provider-profile and a few other legacy fields have no remote column at all; and on the hosted dev Supabase project used for validation, `service_role` itself has no REST access to any `public`-schema table/function (a platform/project-level gap the SQL-migration task deliberately left unpatched — see "Future Supabase/PostgreSQL design" below). **`account_status` suspension now does have server-side enforcement** (migration `0042`, uncommitted) — the paragraph that used to live here describing it as client-side-only is stale; see "Suspension enforcement, FK delete lifecycle, and document-group simplification" below.

### Photo upload pipeline

Job photos flow through three layers that only make sense together:

1. **Client-side pre-check** (`photo-validation.ts`): sniffs the real file format from magic bytes (never trusts the extension/declared MIME), enforces size/count limits, and SHA-256-hashes content to reject exact duplicates.
2. **Server-side processing** (`app/api/job-photos/process/route.ts`, Node runtime): re-validates by actually decoding the image with `sharp`. HEIC/HEIF (common from iPhones) can't be decoded by the bundled libvips due to codec licensing, so those go through `heic-convert` (WASM, unrestricted) first. The pipeline then auto-rotates from EXIF orientation and **strips all EXIF/GPS metadata** (no `.withMetadata()` call) before returning JPEG/PNG/WebP bytes.
3. **Client-side storage** (`photo-blob-store.ts`): the processed blob is written to IndexedDB (localStorage's ~5-10MB quota can't hold up to 10 photos × 10MB each). `job-store.ts` only stores a `storageKey` pointing into that blob store on the `Job.photos` record; `use-job-photo-url.ts` resolves a `storageKey` back to a revocable `URL.createObjectURL` for `<img>` rendering.

### Domain model

- Three roles on `types.ts#UserRole`: `hizmet-alan` (service requester, posts `Job`s), `hizmet-veren` (service provider, posts `Offer`s on jobs), and `admin` (internal-only, gates the multi-module `/admin` panel — see "Admin panel modularization" below; never a selectable "Hesap Türü" at registration, created only by the dev seed). Role gating is checked both in UI (which fields/buttons render) and in the data layer (which operations are allowed). Registration (`login-form.tsx` kayıt mode, validated by `register-form-validation.ts`) collects company info for both public roles — `companyName`, `companyType` (`company-type.ts`), `province`/`district`, plus a combined legal-consent checkbox (see "Legal documents" below); `hizmet-veren` registration used to additionally collect a service-category selection and 1+ "Faaliyet Belgesi" uploads as part of the same form — **that KYC step has since moved out of registration entirely** (see "Provider document verification" below): a fresh `hizmet-veren` account now starts with zero selected services/documents and picks/uploads them afterward from `/panel/belge-yukleme`. **As of the Supabase Auth migration (see that section below), both roles now go through the same two-step `supabase.auth.signUp()` → `finishSupabaseRegistration()` path** rather than writing a `StoredUser` directly — the older, single-step `users.ts#registerUser`/`provider-registration.ts#registerProviderAccount` functions this paragraph used to describe as the live registration path still exist but are no longer called by `login-form.tsx` for either role.
- Jobs come from two separate sources merged only through `jobs-lookup.ts#findJobById`: `jobs.ts` is a static in-memory array of seed example listings (`requesterId: null`, not localStorage-backed), while `job-store.ts` is the real localStorage "table" for user-created jobs. Never query `job-store.ts` alone when you need "any job by id" — other modules (`offers.ts`, `contact-access.ts`) go through `jobs-lookup.ts` so seed listings resolve too.
- `Job.requesterId` links a job to the user who created it (`null` for the seeded example listings) — this is what `contact-access.ts` and the notifications system use to determine "is this my job."
- **Job location** is `province`/`district` (always present, real names from `turkey-locations.ts`), `workLocationType` (the Bölge/Tesis display name — either copied from a picked catalog `Facility.name`, or the user's own facility name in `locationMode: "custom"`), `facilityId?` (the picked `Facility.id`; never set in custom mode), `locationMode?` (`"catalog"` | `"custom"`, absent on pre-2026-07-25 jobs and always read as `"catalog"` when absent), `companyOrFactoryName?`, `addressText?`, `neighborhood?`, `locationUrl?`, `directionsNote?` — see "Centralized job location" below for the shared formatter/resolver, the custom-facility flow, and the address privacy gate.
- `Job.operationId?`/`Job.workEndDate?` — see "Çoklu Hizmet Operasyonu" below. Both absent on every job created before this feature (and on any job still created via the single-service path); `workEndDate` pairs with the pre-existing `workDate` to form a date *range* rather than a single day. **`workDate` can never be submitted in the past** — `job-form-validation.ts#validateWorkDateRange` (the one shared check behind `validateJobForm`/`validateServiceItem`, so single-job create, edit, and every card of a multi-service operation all enforce it identically) rejects a past `workDate` via `jobs.ts#isJobDateInPast`, and every `<input type="date">` for `workDate`/`workEndDate` across `job-request-form.tsx`/`job-edit-form.tsx`/`job-requests-panel.tsx`'s republish dialog also sets `min` from `jobs.ts#getTodayLocalDateString()` (workEndDate's `min` is `workDate || today`) as a UI-level nudge on top of that submit-time check — neither function is UTC-based (`isJobDateInPast` parses the "YYYY-MM-DD" string's Y/M/D components directly into a local-time `Date` rather than `new Date(string)`, which ECMAScript parses as UTC midnight; `getTodayLocalDateString` reads `getFullYear`/`getMonth`/`getDate` rather than `toISOString().slice(0, 10)`) specifically so a negative-UTC-offset user late in their local day never gets a `min`/validation boundary that's silently off by a day. This only gates *new* submissions — an already-past `workDate` on an existing job (expired/completed/archived) is never touched by this check outside of an active edit-and-save.
- `Job.createdAt?`/`Job.publishEndAt?`/`Job.republishedFromJobId?`/`Job.republishedToJobId?` — see "İlan Yayın Süresi Yönetimi" below for the 14-day listing-expiry/republish system these back.
- `Job.status` (`"yayinda" | "tamamlandi" | "iptal"`) never changes after creation for a real user's job — `createJob` always writes `"yayinda"`, and no other function (including `updateJob`) touches it. Time-varying state (in-progress, tamamlandı, etc.) is derived at read-time from the job's `Offer`s via helpers in `job-requests.ts` (`getJobRequestFilter`, `getJobAvailabilityForProvider`, `isJobEditable`) — check that file, not `Job.status`, when you need a job's effective state. `getJobOfferAvailability` returns `"acik"` for a `"yayinda"` job as long as its settled offer (if any) is at most `"accepted"` — see "New offers vs. accepted vs. work started" below for exactly where it flips to `"kapali"`, since `Job.status` itself never does. `jobHasAcceptedOffer` has **no callers left anywhere in the app** (per its own doc comment, kept only for backward API/documentation purposes) — a pre-database-migration audit fix (`offers.ts` "K2") found that its `ENGAGED_OFFER_STATUSES`-only definition deliberately excludes `completed`, which let a completed (and possibly already-rated) job's listing be deleted despite `deleteJobWithOffers`'s own error message promising otherwise; that guard now checks `job-requests.ts#getSettledOfferForJob(jobId, offers) !== null` directly (ENGAGED ∪ `completed`) instead.
- **Job editing** is gated by `job-requests.ts#isJobEditable`/`JOB_NOT_EDITABLE_MESSAGE` (the offer process having started — any offer in `ENGAGED_OFFER_STATUSES` ∪ `completed` ∪ `cancelled` — permanently locks a job from further edits) — the single source of truth called from three independent layers that must stay in sync: the "İlanı Düzenle" link's visibility (`job-requests-panel.tsx`), the edit route's own guard (`job-edit-form.tsx`), and the data-layer write (`job-store.ts#updateJob`, which takes the current `Offer[]` as a parameter precisely so it can re-check this without importing `offers.ts` — that would be circular, since `offers.ts` already imports `job-store.ts`).
- `Offer.status`: `pending -> accepted | rejected` via `offers.ts#updateOfferStatus` (job-owner/requester only). An `accepted` offer can further move to `in_progress` via `offers.ts#startWorkForOffer`, or to `agreement_failed` via `offers.ts#recordAgreementFailure` (both requester-only, both leave `Job.status` untouched). `agreement_failed` is deliberately excluded from `ENGAGED_OFFER_STATUSES` (and thus from `getSettledOfferForJob`), which is what lets the job be deleted afterward (`deleteJobWithOffers` only blocks deletion on an active/completed offer) and frees the provider's capacity slot — it does *not* gate re-offering, since new offers were never blocked in the first place (see "Single active acceptance" below). A `pending` offer can also move sideways to `withdrawn` via `offers.ts#withdrawOffer` (provider-only) — deliberately excluded from `ENGAGED_OFFER_STATUSES` too, since a never-accepted offer never held capacity or contact access.
- **Single active acceptance per job**: a job can receive `pending` offers from multiple providers concurrently, but at most one may ever be settled (accepted or further along) at a time — enforced by `job-requests.ts#isOfferPendingActionBlocked` (built on `getSettledOfferForJob`, which unions `getEngagedOfferForJob` and `getCompletedOfferForJob` — the single shared source of truth), called from *both* `offers.ts#updateOfferStatus` (rejects a second accept/reject attempt at the data layer, immune to a stale tab or a bypassed button) and `incoming-offer-card.tsx` (swaps the Kabul Et/Reddet buttons for a fixed notice, `job-requests.ts#OFFER_PENDING_BLOCKED_MESSAGE`). This fires as soon as **any** offer is `accepted` — before work starts — and deliberately does **not** mutate sibling offers to `rejected`: they stay `pending` untouched, so if the settled offer later fails out to `agreement_failed` they become actionable again automatically (purely derived, no extra write); once the settled offer instead progresses *past* `accepted` (work actually starts), those siblings surface as closed in the provider's own tabs instead — see "Provider offer tabs" below. This accept/reject-action lock is a **separate, earlier** threshold than whether the job still accepts brand-new competing offers — see next.
- **New offers vs. accepted vs. work started.** `job-requests.ts#isJobClosedToNewOffers(jobId, offers)` is the single source of truth for "can anyone else still submit a competing offer on this job" — built on the same `getSettledOfferForJob`, but only `true` once the settled offer is *past* `"accepted"` (`in_progress`/`completion_requested`/`completion_disputed`/`completed`). An `accepted`-but-not-started offer deliberately does **not** close the job — it's a pre-work-start negotiation stage, so other providers can still freely submit competing `pending` offers. Three call sites share this one check and must never drift apart: `offers.ts#createOffer` (data-layer authority), `offers.ts#canProviderSubmitNewOffer` (what `offer-panel.tsx` renders — the form is replaced by "Bu ilan için bir hizmet verenle iş başlamış." once it flips — and what the "Operasyondaki Hizmetler" card's own "Teklif Ver" link checks, see "Operation discovery & status UI" below), and `getJobOfferAvailability` (feeds its `"kapali"` value — previously a dead branch, since `Job.status` never changes, now live). Because all three read the same function, a "Teklif Ver" button that's visible always corresponds to a real, submittable offer form on that job's own page.
- **Provider offer tabs ("Verdiğim Teklifler")**: `job-requests.ts#getProviderOfferFilter(offer, offers)` — note it takes the *full* offers array, not just the caller's own, because it needs sibling context — is the single source of truth for which of the four `my-offers-panel.tsx` tabs (Aktif/Devam Eden/Tamamlanan/Kapanan Teklifler) an offer appears in, and `getProviderOfferNotificationHref`/`getProviderOffersTabHref` build every Hizmet Veren notification's `href` from that same function, so a notification always lands on the tab the offer actually renders in (never a hand-rolled route string). "Kapanan Teklifler" bundles three unrelated reasons an offer is permanently non-actionable: (1) a still-`pending` sibling offer on a job where a *different* offer has progressed past `accepted` (`isOfferClosedByJobProgress`, built on `getSettledOfferForJob` rather than a new status) — shown with an overridden "Başka Bir Hizmet Verenle Anlaşıldı" badge and a dedicated notification (see below); (2) `agreement_failed`; (3) `cancelled` (a disputed job resolved as cancelled, shown with an extra "İtiraz sonrası iş iptal edildi." line). (2) and (3) keep their real `getOfferStatusLabel` badge instead of the override. None of the three render any action button in that tab. `getProviderOfferFilter` only recognizes `"devam-eden"`/`"tamamlandi"`/`"kapanan-teklifler"` as explicit `?durum=` values (anything else, including an old/removed tab key, silently falls back to `"aktif"` rather than erroring) — always build a link to this page via `getProviderOffersTabHref(filter)`/`getProviderOfferNotificationHref(offer, offers)` rather than hand-writing a `?durum=...` query string, or it will silently land on the wrong tab.
- **Incoming offers screen ("Gelen Teklifler")** shows one job at a time, picked from a row of per-job chips, rather than a flat list or a category hierarchy — `incoming-offer-grouping.ts#groupIncomingOffersByJob(offers, jobById)` builds a flat `IncomingOfferJobGroup[]` (one entry per distinct `jobId`, no category level), rendered by `incoming-offers-panel.tsx` as a chip strip plus a single `incoming-offer-job-section.tsx` for whichever job is selected (wrapping the unchanged `incoming-offer-card.tsx` per offer). This replaced an earlier two-level "Hizmet Türü → İlan → Teklifler" design (`groupIncomingOffersByCategoryAndJob`/`IncomingOfferCategoryGroup[]`/`incoming-offer-category-section.tsx`, all now deleted) because two different job postings in the same service category used to get visually merged under one category header. `groupIncomingOffersByJob` is a single, non-sorting pass — a group's position is fixed by wherever its first (already-sorted) offer lands — so callers must pre-sort with `job-requests.ts#sortIncomingOffersForDisplay`, which applies a seven-tier priority (`incomingOfferSortWeight`): `accepted` first, then `pending`, then `in_progress`/completion-in-progress stages, then `completed`, then `agreement_failed`/`cancelled`, then `rejected` — `accepted` was deliberately moved *above* `pending` (previously the reverse) so that accepting an offer never visually demotes it below its still-pending siblings on the same job. It's implemented as a deterministic slot-reordering algorithm rather than a comparator returning `0` across different jobs, specifically to avoid a non-transitive/engine-dependent order when two jobs' offers interleave in the base (recency-sorted) array — see the function's own doc comment for the full reasoning. `incoming-offers-panel.tsx` derives the selected chip purely at render time (`effectiveSelectedJobId`: the last-clicked job id if it's still in `jobGroups`, else `jobGroups[0]`) rather than syncing it via `useEffect` + `setState` — the project's lint rule forbids that pattern anyway (see "No real backend" below) — so a newly-arrived offer or a job dropping out of the list is reflected automatically on the next render. Visibility is a separate, upstream concern: `job-requests.ts#isOfferShownInIncomingOffersScreen` (its own single source of truth, deliberately kept apart from the broadly-shared `isOfferVisibleInNormalLists`) removes `rejected`/`agreement_failed` offers from this screen entirely — not deleted, not merely reordered, just excluded before grouping — so a job with zero remaining visible offers simply never produces a chip (no explicit "collapse" step needed), and reappears automatically the moment a new offer arrives for it. An offer whose job can't be resolved (deleted — see "Notifications are derived, not stored" below) gets its own chip labeled `"İlan Bilgisine Ulaşılamayan Teklifler"` (one per missing `jobId`, never merged together). The panel also drops a job's offers entirely once `getJobRequestFilter(job, offers) === "tamamlandi"` (the winning offer has moved to Hizmet Taleplerim's "Tamamlandı" view, and any still-`pending` siblings are no longer meaningful) — deliberately *not* for `"kabul-edildi"`/`"devam-eden"`, since the requester's Kabul Et/Reddet/Tamamlandığını Onayla/İtiraz Et actions for those stages live on this same screen, inside `IncomingOfferCard`. The completion `JobRatingModal` lives in `incoming-offers-panel.tsx`, not in `incoming-offer-card.tsx`'s own local state, and is rendered in a shared branch outside both the empty-state early return and the job-selector `if`/else — same pattern `job-requests-panel.tsx` uses: the card that triggers the modal can itself unmount (its job's chip disappearing from the grouping above, e.g. when it was the only remaining visible offer) in the same render the offer completes, which would silently close a modal owned by local state — or skip its render entirely behind an early return — before it's ever seen.
  - **Each chip shows only the service category, total offer count, and (if any) an unread-new-offer badge — never the job's own long title.** The chip's `title` HTML attribute still carries the full job title as a native hover tooltip, and a small muted `formatShortJobDate(job.workDate)` ("GG.AA") line is rendered under the offer count purely so two independent listings in the same category stay distinguishable without reintroducing a long title. The red badge (top-right of the category line, `bg-danger` rounded pill, same visual language as `notification-bell.tsx`'s own unread count) is deliberately **not** the chip's total-offer count — it's the count of that job's still-unread `"yeni_teklif"` notifications, computed directly from `group.offers` (already the exact filtered/visible set for this screen) against `notification-reads.ts` via `notifications.ts#newOfferNotificationId(offerId)` (the one shared id-scheme function `newOfferNotifications` itself also uses, so the two can never drift). It is deliberately independent of `notification-dismissals.ts` — deleting a bell notification is a different action from having viewed a job's offers, so a dismissed-but-still-unopened offer keeps counting toward the badge.
  - **Chip typography is a strict three-tier hierarchy, and every chip is forced to the same height regardless of content.** The category label is the visually dominant line (`text-sm font-bold tracking-heading leading-tight text-foreground` — `--fg`/`#10233f`, the app's standard near-black navy heading color, not a one-off hex) and always `truncate`s to exactly one line no matter how long the label is, so a long category can never grow one chip taller than its siblings. The offer-count line (`text-xs font-semibold`) is deliberately smaller than the title; the date line (`text-[11px] text-muted-foreground/70`) is the smallest and most faded, and — critically — is **always rendered**, even for the `"İlan Bilgisine Ulaşılamayan Teklifler"` group that has no real job/date (it renders a literal space instead of being conditionally omitted). Omitting that row conditionally was a real bug: `flex flex-wrap`'s per-line `stretch` only equalizes chip heights within the *same* wrapped row, not across the whole strip, so a chip lacking the date row would end up visibly shorter than chips on a different line. `min-w-40` gives every chip the same floor width so a short category ("Nakliye") doesn't look like a stray tiny pill next to a long one ("Kapalı Depolama") — `max-w-full` plus the title's own `truncate` still keep it mobile-safe.
  - **Marking read requires a real, job-scoped user action — never a passive default selection.** `incoming-offers-panel.tsx`'s own `useEffect` (keyed on `selectedGroup`) only calls `notification-reads.ts#markNotificationsRead` (the batch sibling of `markNotificationRead`, one localStorage write for every new-offer id on that job at once) when the currently-selected job got there via `manuallySelectedJobId === selectedGroup.jobId` (an actual chip click) or via a notification deep link (`?ilanId=`/`?offerId=` matching this job/one of its offers) — landing on `jobGroups[0]` purely because no click/deep-link happened yet does **not** mark it read. This was a deliberate correction during development: an earlier version marked-read on any displayed selection including the passive default, which meant a job that became `jobGroups[0]` *because* a brand-new offer had just arrived would have its own badge clear before the user ever saw it — exactly backwards. Clicking a chip that's already the (passively) selected one still works, since `manuallySelectedJobId` genuinely changes from `null` to that job's id on the first click.
- **Completion is a two-party handshake, not a single write.** From `in_progress`, the provider calls `offers.ts#requestCompletion` (-> `completion_requested`, records `completionRequestedByUserId`/`completionRequestedAt`); the requester then either `confirmCompletion` (-> `completed`) or `disputeCompletion` with a 10–1000 char note (-> `completion_disputed`), and a disputed offer is resolved by the requester via `resolveCompletionDispute` to either `completed` or `cancelled`. The party that requested completion is blocked from confirming/disputing their own request. Because there's no server-side scheduler, a `completion_requested` offer that the requester never acts on is auto-approved after `COMPLETION_AUTO_APPROVE_DAYS` (7 days) by `applyExpiredCompletionAutoApprovals()` — this runs lazily (checked from `use-offers.ts` on read, not on a timer) and sets `autoCompleted: true`, which caps the `ratings.ts` review window to `AUTO_COMPLETED_RATING_WINDOW_DAYS` (30 days; manually-confirmed completions have no review deadline). Within `job-requests-panel.tsx`'s "Devam Eden" tab, jobs whose engaged offer is stuck in `completion_requested` are sorted to the top — `job-requests.ts#isOfferAwaitingCompletionConfirmation(engagedOffer)` is the single source of truth (a boolean-only wrapper around that one status, not a new derived state); the tab's sort is a stable `Array.prototype.sort` (stable since ES2019) over just that weight, so entries sharing a weight keep whatever order `myEntries`/`useAllJobs()` already gave them rather than inventing a secondary date comparison. Aktif/Tamamlanan tabs are untouched.
- **Provider capacity**: `provider-capacity.ts#MAX_ACTIVE_JOBS` (5) caps how many jobs a `hizmet-veren` can hold concurrently, counted via `getActiveJobCount`/`hasReachedActiveJobLimit` over `job-requests.ts#ENGAGED_OFFER_STATUSES` (`accepted`, `in_progress`, `completion_requested`, `completion_disputed`) — a completion request or an unresolved dispute still counts against capacity; only a terminal status (`completed`, `cancelled`, `agreement_failed`, `rejected`, `withdrawn`) frees a slot. Enforced at **two** independent points, not just one: `offers.ts#createOffer`/`canProviderSubmitNewOffer` (offer-creation time) and, separately, `updateOfferStatus` when `nextStatus === "accepted"` (accept-time, against a freshly-read offers list) — the second check exists because a provider can hold multiple still-`pending` offers on independent jobs while under capacity, and two different requesters accepting those offers unaware of each other could otherwise push the provider past `MAX_ACTIVE_JOBS`; a rejected accept attempt leaves the offer `pending` and every other record untouched.
- **Ratings** (`ratings.ts`): a requester submits one 1–5 star `Rating` per completed `Offer` for the provider (`submitRating`), aggregated per-provider via `getProviderRatingSummary`. Gated by the auto-completion review window above, not by role/ownership checks alone. `job-rating-modal.tsx` shows the provider's identity next to the star input — logo via `use-job-photo-url.ts` on `providerProfile.logoStorageKey`, falling back to the same initials-avatar pattern used by `profile-info-card.tsx`/`panel-welcome-card.tsx` (`profile.ts#getInitials`), and company name if set else the provider's own name — read straight off `offer.providerId`/`users.ts#findUserById`; the modal only ever receives the single accepted-then-completed offer being rated (from `getCompletedOfferForJob`), so no extra "is this the right offer" check is needed.
- **Provider profiles** (`provider-profile.ts`): an optional `ProviderProfile` (company name, logo, 50–500 char bio, founded year, service regions, expertise, `serviceCategories`, `serviceFeatures`, `experienceRange`) on `StoredUser`, meaningful only for `hizmet-veren`. **`ProviderProfile.expertise`/`serviceCategories` are both deprecated** (per their own doc comments in `types.ts`) now that `provider-services.ts` is the single source of truth for a provider's current service-category selection (see "Provider document verification" below) — `expertise` is still read/written only by the legacy Hesap Ayarları > Firma Profili "Uzmanlık Alanları" chips (`provider-profile-editor.tsx`), and `serviceCategories` is kept only so older stored profiles still parse in `normalizeStoredUser`; new code must never write to either, and `service-catalog.ts#migrateLegacyExpertiseToServiceCategoryIds` is the one-way bridge from the old field to the new table — fully optional and normalized on read like `Job.photos`, so pre-migration users just render blank/`"-"` fields rather than fabricated data. **Two separate, deliberately-independent edit surfaces write to the same `StoredUser.providerProfile` object** — each preserves the other's fields on save rather than clobbering them: Hesap Ayarları > Firma Profili (`provider-profile-editor.tsx`, requires companyName+bio) through `users.ts#updateProviderProfile` (carries `serviceCategories`/`serviceFeatures`/`experienceRange` through unchanged), and Panel > Profilim > Hizmet Bilgilerim (`service-info-editor.tsx`, a completion flow that never requires companyName/bio) through `users.ts#updateProviderServiceInfo` (carries companyName/bio/foundedYear/logoStorageKey/expertise through unchanged). `regions` is the one field both screens genuinely share and either can overwrite. The logo blob lives in the same IndexedDB store as job photos (`photo-blob-store.ts`) under a separate key namespace, uploaded client-side only (not via `/api/job-photos/process`, which stays `hizmet-alan`-gated). Viewed read-only by the counterparty inline within `IncomingOfferCard` (no separate drawer/modal component) — it never reads phone/email/address/tax/TC fields; contact reveal remains solely `contact-access.ts#getRevealedContactForOffer`'s job. `getProviderProfileSummary` computes average rating, in-progress count, and acceptance rate purely from existing `ratings.ts`/`job-requests.ts` data — no separate event log, and no "average response time" metric (dropped: providers initiate offers here, so there's no "provider responded" event to time).
- **Provider identity is anonymized on Gelen Teklifler until an offer is accepted**: `job-requests.ts#isOfferProviderIdentityRevealed(offer)` mirrors `contact-access.ts`'s `ENGAGED_OFFER_STATUSES` timing exactly (identity and phone/email always reveal/hide together, via two deliberately separate "parallel gate" functions rather than one shared one — same pattern as `canViewJobAddress`), and returns `false` for `pending`/`rejected`/`agreement_failed`/`cancelled` — once an offer leaves those states it never re-anonymizes. While hidden, `incoming-offer-card.tsx#getAnonymousProviderLabel(providerId)` shows a deterministic `"Hizmet Veren #NNNN"` label (a non-cryptographic hash of `providerId`, stable across renders/sessions so a requester can still recognize "the same provider" across multiple offers) instead of company name/logo/rating/review count/completed jobs/expertise/bio/region, keeping pre-acceptance evaluation price/duration/description-driven.
- **Service catalog** (`service-catalog.ts`): the platform's single centralized, hierarchical service taxonomy (`SERVICE_CATEGORY_GROUPS`) — feeds *both* a provider's multi-select "Hizmet Bilgilerim" service selection *and* the job-posting/editing "Hizmet Kategorisi" single select (`job-request-form.tsx`, `job-edit-form.tsx`), sharing the same `id` space so a provider's selected service and a job's category can in principle line up (no automatic matching yet — just a shared data foundation). Supersedes but does not remove `jobs.ts#SERVICE_CATEGORIES` (an old flat 8-item list still written by some legacy records); the two are bridged by a one-way, best-effort migration layer — `resolveLegacyJobCategoryToId` (single value, for `Job.category`), `migrateLegacyExpertiseToServiceCategoryIds` (multi-value, for `ProviderProfile.expertise`), and `getCategoryDisplayLabel` (safe display for either old or new values). `"Depolama"` is deliberately left unmapped (no 1:1 equivalent in the new catalog) — migration functions silently skip values they can't map rather than inventing one; original legacy data is never mutated or lost. Also houses `SERVICE_FEATURE_OPTIONS`/`EXPERIENCE_RANGE_OPTIONS` (Aşama 2 additions to `ProviderProfile`) and `getProviderServiceInfoCompletion` (the "Profil Tamamlanma %" checklist shown in `service-info-editor.tsx`). **"Liman Hizmetleri" was later simplified from six categories to three** — `lashing`+`unlashing` → `lashing-unlashing`, `konteyner-dolum`+`konteyner-bosaltim` → `konteyner-dolum-bosaltim`, `yukleme-gozetimi`+`bosaltma-gozetimi` → `gozetim-hizmetleri` — plus ten unrelated categories (Liman Personeli, Depo Personeli, Depo Düzenleme, Paletleme, Etiketleme, Sayım Hizmeti, Paletli/Ağır Yük Depolama, Vardiyalı Çalışma, Proje Yük) were dropped outright into `REMOVED_CATEGORY_IDS`. `resolveServiceCategoryId(rawId)` is the one function every reader goes through: it maps a pre-merge id via `MERGED_CATEGORY_ID_ALIASES` transparently to its new id, returns a still-valid id unchanged, or `null` for anything in `REMOVED_CATEGORY_IDS`/unrecognized — `getCategoryDisplayLabel` shows `DEPRECATED_CATEGORY_LABEL` ("Artık Kullanılmayan Hizmet") for the removed set. No stored `Job`/`ProviderProfile` record is ever migrated or deleted for this — old ids just resolve differently on read.
- **Product info fields** ("Ürün Bilgileri" — Ürün Adedi/Tonaj/Ürün Cinsi) are category-scoped `Job` fields (`productQuantity?`, `productTonnage?`, `productType?`), single source of truth `product-catalog.ts`: `requiresProductInfo(categoryId)` is `true` only for the union of the three Liman Hizmetleri ids (`PORT_SERVICE_CATEGORY_IDS`) and Nakliye (`isTransportationCategory`, itself just `categoryId === NAKLIYE_SERVICE_CATEGORY_ID`) — every other category, including Depolama/Forklift, is out of scope. `productQuantity`/`productType` are required whenever in scope; `productTonnage` is required only for Nakliye and optional across all of Liman Hizmetleri (`isTonnageRequired`). Every render site must go through `hasProductInfo`/`formatJobProductInfoLine` rather than checking a raw field's presence — both re-check `requiresProductInfo` first, so a leftover value on a job whose category was later moved out of scope (data never migrated/deleted) never surfaces in the UI. In a multi-service operation each sibling service carries its own product info independently (`job-store.ts#OperationServiceInput`), same as every other per-service field.
  - **`product-type-combobox.tsx#ProductTypeCombobox` shows exactly one box at a time — picking "Listede Yok, Kendim Gireceğim" morphs that same box into a manual text input, it never opens a second one below it.** This was a real, fixed bug: an earlier version left the read-only combobox trigger (showing the sentinel label) in place *and* rendered a second, separate "Ürün Cinsini Yazınız"-labelled input underneath it, so the same logical field visibly had two boxes at once. The fix is a single conditional render, not a layout tweak: `isCustom` (`value === PRODUCT_TYPE_CUSTOM_VALUE`) picks between two mutually exclusive branches sharing one `<label>` (always "Ürün Cinsi", `htmlFor` pointed at whichever input is actually mounted) — `!isCustom` renders the combobox trigger + suggestion listbox; `isCustom` renders a plain `<input>` (no listbox, no `role="combobox"`) bound to `customText`/`onCustomTextChange`, with a "Hazır listeden seç" text button below it that calls `onChange("")` to switch back. The prop contract (`value`/`onChange`/`customText`/`onCustomTextChange`/`customFieldId`) is **unchanged** — this was a presentational fix only, so `job-form-validation.ts`'s sentinel+custom-text validation and every caller's submit-payload resolution logic didn't need to change. The three callers (`job-request-form.tsx`, `job-edit-form.tsx`, `customs-brokerage-fields.tsx`) each still apply `col-span-full` to their own wrapping `<div>` when `isCustom` (the grid-cell-width fix from a separate, earlier task) — that part was orthogonal to the two-boxes bug and stayed as-is, now simply applying to the one morphed box instead of a stacked pair. Auto-focus was already correct and untouched — `use-focus-on-become-true.ts#useFocusOnBecomeTrue` only fires on the false→true transition, never on first render, so switching into custom mode focuses the new input immediately without an extra click and without stealing focus on initial page load.
- **Nakliye Güzergâh Yönetimi** (transport pickup/delivery routing) gives Nakliye-category jobs a second location, not just one. Pickup reuses the existing shared `province`/`district`/`workLocationType`/`facilityId`/`addressText`/`locationMode` fields verbatim (no separate pickup fields) — deliberately, so every screen that already renders those fields keeps working unchanged. Delivery ("Teslim Edilecek Yer") is six new, Nakliye-only `Job` fields — `deliveryProvince`/`deliveryDistrict`/`deliveryLocationType` (`"facility" | "open_address"`)/`deliveryFacilityId`/`deliveryFacilityName`/`deliveryAddressText` — populated only when `product-catalog.ts#isTransportationCategory(category)`, and cleared unconditionally otherwise by `job-store.ts#resolveDeliveryLocationFields` (same "single write path, cleared out of scope" pattern as Product info fields/Gümrük Müşavirliği fields below, so re-categorizing a job away from Nakliye silently drops stale delivery data instead of dragging it along).
  - **Both sides always carry a name AND an address, regardless of method.** `nakliye-location-fields.tsx#NakliyeLocationFields` (the one shared component for both "Yük Alınacak Yer" and "Teslim Edilecek Yer") renders an always-visible, always-required "Açık Adres" textarea beneath the İl/İlçe/Liman-Sanayi-OSB row — never conditionally hidden by which method is chosen — and, only when "Listede yok, kendim gireceğim" is picked, an additional required "Liman / Sanayi / OSB Adı" free-text input (`NakliyeLocationFieldValues.customFacilityName`, max 150 chars) for the user's own facility name. This was a real fix, not a new feature layered on: the address used to be shown/collected *only* in manual mode and silently discarded in catalog mode, and manual mode never captured a name at all (pickup wrote a fixed placeholder string into `workLocationType`, delivery left `deliveryFacilityName` empty) — both gaps are closed. `customFacilityName` maps into the exact same denormalized "name" field either side already had — pickup's `workLocationType`, delivery's `deliveryFacilityName` — via `job-request-form.tsx#resolveNakliyePickupPayload`/`resolveDeliveryLocationPayload` and `job-edit-form.tsx`'s equivalent inline logic, so no new `Job` field was needed; `job-store.ts#resolveLocationFields` (pickup, shared with every other category) already carried `addressText`/`workLocationType` through unconditionally, but `resolveDeliveryLocationFields` had its own old "facility XOR address" mutual-exclusion baked into the data layer itself and had to be fixed there too — a UI-only fix would not have been enough.
  - **Switching between catalog and manual preserves the address, never clears it** — `handleFacilityChange` inside `NakliyeLocationFields` only ever clears `customFacilityName` (when moving *into* catalog mode; moving into manual mode leaves whatever was typed before untouched) and never touches `addressText`. Only an actual province or district change clears `facilityId`/`customFacilityName`/`addressText` together (a stale facility or address can never survive a location change).
  - `deliveryAddressText` used to share `job.addressText`'s exact privacy gate (`job-requests.ts#canViewJobAddress`) with no separate reveal rule — **that's since changed on the job detail page specifically**: see the `NakliyeRouteSummary` note just below, which now shows both sides' address unconditionally there. `canViewJobAddress` itself is untouched and still the real, unmodified gate everywhere else (every non-Nakliye category, and any other surface that isn't this one summary panel). The facility/manual name itself was never gated (same principle as `formatJobLocationLine` elsewhere never hiding a facility name). A `deliveryFacilityId` (like the pickup one) is re-validated at the data layer, not just in the form: `nakliye-route.ts#isFacilityInProvinceDistrict`/`job-store.ts#verifyNakliyeFacility` silently drops (rather than errors on) a facility id that doesn't actually belong to the given province+district — the manual name itself needs no such check since it's free text.
  - `nakliye-route.ts#getNakliyeRouteInfo` is the single resolve function shared by `job-detail-content.tsx` (full route) and `job-listing-row.ts` (short label, via `getNakliyeShortRoute`). **The full-route rendering used to live in a separate `nakliye-route-card.tsx` component — that file is now deleted**, its behavior folded directly into two functions private to `job-detail-content.tsx` itself, `NakliyeRouteSummary`/`NakliyeRoutePoint`, moved into the page's top summary panel (a desktop-density-redesign task) so the route is shown exactly once — replacing both the old card *and* the old single-line pickup-only `formatJobLocationLine` display, which are never rendered together for a Nakliye job (`nakliyeRoute` is computed once via `isTransportationCategory(job.category) ? getNakliyeRouteInfo(job) : null` and gates both: non-`null` renders `NakliyeRouteSummary` and suppresses the old address block below via `showAddress && !nakliyeRoute`; `null` — either a non-Nakliye job, or a rare old Nakliye record with no delivery info at all — falls back to the pre-existing single-line display, never fabricating route data). One deliberate behavior change came with the move (see the bullet above): both pickup and delivery `addressText`, plus pickup's `directionsNote`/`locationUrl`, now render unconditionally inside this panel, bypassing `canViewJobAddress` entirely for Nakliye jobs on this page. The short-route shape (`job-listing-row.ts`) is unaffected — still a `{ locationLabel, nameLabel }` pair per side (`"{İl} / {İlçe}"` plus the facility/manual name), never the full address — rendered as two stacked lines per side in both `job-listing-table.tsx` and `job-listing-cards.tsx`; `nameLabel` is only ever empty for a pre-existing job that predates this fix.
- **Gümrük Müşavirliği "Operasyon Bilgileri"** is a second, independent category-scoped `Job` field group, single catalog source `customs-brokerage-catalog.ts`, gated everywhere by the one function `isCustomsBrokerageCategory(categoryId)` (`job-request-form.tsx`, `job-edit-form.tsx`, `job-store.ts`, `job-detail-content.tsx` all call it identically — `"gumruk-musavirligi"` is never separately hardcoded). The form (`customs-brokerage-fields.tsx`) currently collects four fields: İşlem Türü (`customsTransactionType`, `CUSTOMS_TRANSACTION_TYPES`, required single-select) and Ürün Cinsi (`customsProductType`, required) sit side by side in the top two-column grid; Talep Edilen Hizmetler (`customsRequestedServices`, optional multi-select from `CUSTOMS_REQUESTED_SERVICE_OPTIONS`) and destekleyici evrak uploads (`customsDocuments`) follow below. `customsOfficeId` ("Gümrük Müdürlüğü", `KOCAELI_CUSTOMS_OFFICES` — Kocaeli-only), `customsGtipCode`, `customsDeclarationItemCount`, and `customsContainerCount` are all **retired legacy fields**: none of them are collected by any form or shown in `job-detail-content.tsx` anymore (Ürün Cinsi took over `customsOfficeId`'s former top-grid slot), but a pre-existing job's stored value survives untouched across later edits — `job-store.ts#resolveCustomsBrokerageFields` deliberately never includes any of the four in its returned object (neither the in-scope nor the out-of-scope branch), so `updateJob`'s `{...existing, ...resolved}` merge just never overwrites them (same "omit the key entirely, let the merge preserve it" pattern as `companyOrFactoryName`/`neighborhood`/`locationUrl`/`directionsNote` in `resolveLocationFields`); `customs-brokerage-catalog.ts#KOCAELI_CUSTOMS_OFFICES`/`isCustomsOfficeId`/`getCustomsOfficeLabel` and `parseCustomsCount` themselves are left in place (unused by any current call site) rather than deleted, purely so the retired data's meaning stays documented. `customsProductType` shares the exact same catalog+combobox as every other category's "Ürün Bilgileri" — `product-catalog.ts#PRODUCT_TYPE_SUGGESTIONS` and `product-type-combobox.tsx#ProductTypeCombobox` (the same `PRODUCT_TYPE_CUSTOM_VALUE` sentinel/"Listede Yok, Kendim Gireceğim" pattern, resolved through `customsProductTypeCustomText`) — it remains a fully separate `Job` field from `product-catalog.ts#Job.productType`, since the two catalogs' scopes (Liman Hizmetleri/Nakliye vs. Gümrük Müşavirliği) never overlap; no separate free-text input or separate suggestion list was invented for it. `customsDocuments` reuses `JobPhoto`'s exact IndexedDB-blob-plus-`storageKey` shape (`photo-blob-store.ts`) but is restricted to a narrower extension set, `CUSTOMS_DOCUMENT_ALLOWED_EXTENSIONS` (pdf/jpg/jpeg/png/docx/xlsx — a subset of `document-validation.ts#ALLOWED_EXTENSIONS`), via `job-customs-document-upload.tsx`/`job-customs-document-editor.tsx`. In a multi-service operation, only the Gümrük Müşavirliği service card carries these fields — never copied to sibling services (`job-detail-content.tsx` renders the read side inline, gated by the same `isCustomsBrokerageCategory` check).
- **Geri Dönüşüm & Atık Tahliye ("Recycling & Waste Removal")** is a third, independent category-scoped `Job` field group (uncommitted, migrations `0045`–`0047`), same architecture as Gümrük Müşavirliği/Nakliye above — single catalog source `recycling-catalog.ts`, gated everywhere by `isRecyclingCategory(categoryId)` (`"geri-donusum-atik-tahliye"`, its own single-category `service-catalog.ts` group, `"Geri Dönüşüm & Hizmetleri"`). The form (`recycling-fields.tsx`, one shared JSX block rendered identically by `job-request-form.tsx`, `job-edit-form.tsx`, and `admin-job-edit-form.tsx` — same role as `customs-brokerage-fields.tsx`) collects: Malzeme Kategorisi → Alt Tür (a two-level catalog, 7 material categories each with its own "Diğer" subtype — deliberately *not* the shared `product-catalog.ts#PRODUCT_TYPE_CUSTOM_VALUE` free-text-sentinel pattern, since only Malzeme Durumu's "Diğer" needs a free-text note here), Tahmini Miktar + Birim (kg/ton/adet, same 2-decimal comma-or-dot parsing rule as `product-catalog.ts#parseProductTonnage`), Malzeme Durumu (ayrıştırılmış/karışık/diğer, with a conditional 300-char note only for "diğer"), and "Hizmet kapsamında neler yapılmasını istiyorsunuz?" (`recyclingScopeOfWork`, a multi-select of sahadan-toplama/yükleme/tesisten-tahliye/taşıma). `job-store.ts#resolveRecyclingFields` clears all seven fields whenever the category is out of scope, the same pattern as `resolveCustomsBrokerageFields`/`resolveDeliveryLocationFields`. **Deliberately no separate "reverse offer"/scrap-purchase transaction model** — an earlier draft of this feature planned a buy-side/reverse-pricing flow; it was fully dropped (see `0045`'s own migration header), so `offers.ts`/`create_offer` needed zero changes: a Hizmet Veren still submits one ordinary single-total-price MALSEVK offer covering everything checked in Hizmet Kapsamı. No scope option (especially Yükleme/Taşıma) auto-creates a separate Forklift/Nakliye job — a requester can still add one manually via Çoklu Hizmet Operasyonu, with no automatic link between the two. `formatRecyclingSummaryLine` is the one-line card/table summary, following `product-catalog.ts#formatJobProductInfoLine`'s "•"-joined convention, returning `null` outside scope or when nothing was entered.
- **`services-section.tsx`** (the landing page's "Lojistik operasyon hizmetleri" section) renders the same catalog-driven cards to every role but makes them interactive only for a logged-in `hizmet-veren` (`useSession().role === "hizmet-veren"`): `hizmet-alan`/guest get a plain, non-clickable `<div>` card (no "İncele" affordance), while a provider gets a `<Link href="/ilanlar?kategori={category.id}">` card — `provider-job-listing.tsx` reads that `kategori` search param on mount to seed the "Hizmet Türü" filter, so clicking a service card from the landing page lands a provider directly on their "Aktif İlanlar" list pre-filtered to that category. Icons are assigned per `service-catalog.ts` group id (`GROUP_ICONS`, falling back to a generic icon for an unmapped group) rather than per individual category, so adding a new category to an existing group needs no icon change.

### Provider document verification ("Hizmet Veren Belge Kontrolü") & the `admin` role

`hizmet-veren` registration **used to be** a KYC-lite gate requiring 1+ service categories and 1+ "Faaliyet Belgesi/Raporu" uploads before an account could even exist — **that's no longer true** ("Hizmet Veren Onboarding Sadeleştirmesi," uncommitted `login-form.tsx`/`complete-registration.ts`/`register-form-validation.ts` diffs). Registration now collects neither: `finishSupabaseRegistration()`'s document/declaration validation only runs `if (validServiceIds.length > 0)`, and the real form always sends an empty array, so a brand-new account starts with zero selected services and zero documents. Both now happen post-registration, one category at a time, from **`/panel/belge-yukleme`** (`document-upload-content.tsx`): pick a not-yet-selected category → upload its document → the page calls the same full-replacement service-selection RPC (`set_provider_service_categories`, `0014`) plus `supabase-provider-documents.ts#uploadAndRegisterProviderDocument` — the same document-upload function the old registration flow used, just called later and one document at a time, now tagged with a `serviceCategoryId` (see `provider_documents.service_category_id` and the corrected `(provider_id, document_type, service_category_id)` one-pending-per-service unique index, both from migrations `0038`/`0040` below). `?hizmet=<categoryId>` deep-links straight to that category's step, which is how an admin's `service_document_required` notification (`request_provider_document` RPC, `0039`, admin-only — it only prompts, it grants nothing) routes a provider to the right place. **An admin approving a document now also grants category access, automatically** — this flipped back and forth: `0038` deliberately kept document approval and category authorization as two independent admin actions, but `0041` (uncommitted, see "Service Authorization" below) found that split confusing in practice and made `review_provider_document`'s `approved` branch call `authorize_provider_service` itself. Rejecting/requesting revision still only ever touches `provider_documents`, never authorization. The five modules below still describe the underlying document/service-selection machinery (largely unchanged), plus a sixth (Gümrük Müşavirliği) that layers a service-specific extension on top:

- **`provider-services.ts`** is the single source of truth for a provider's current service-category selection — a `{userId, serviceCategoryId}` relational table, following the exact `job-store.ts` module-cache-plus-`useSyncExternalStore` pattern (`providerServicesStore`, subscribed to by `use-provider-services.ts#useProviderServiceCategoryIds` for reactive callers) so a change in one tab (or Hesap Ayarları/Hizmet Bilgilerim in the same tab) is picked up live elsewhere. `setProviderServiceCategoryIds` is a full replace (delete-then-insert, one write), silently drops ids `service-catalog.ts#isServiceCategoryId` doesn't recognize, and is the one write path shared by both the registration form and Panel > Profilim > Hizmet Bilgilerim (`service-info-editor.tsx`). This supersedes the deprecated `ProviderProfile.serviceCategories`/`expertise` fields (see "Provider profiles" above).
- **`provider-documents.ts` + `provider-document-reviews.ts`** split "current state" from "history," the same relationship `Offer.status` has with `notifications.ts`: `StoredProviderDocument` (metadata only — filename/mime/size/`indexedDbStorageKey` into the same `photo-blob-store.ts` IndexedDB used by job/logo photos — plus a **denormalized** `reviewStatus`/`reviewNote`/`reviewedAt`/`reviewedByAdminId`, and a `documentType: "genel" | "gumruk-musaviri-izin-belgesi"` field, default `"genel"` on read for records written before the field existed) is the queryable "what's it at now," while `StoredProviderDocumentReview` is an append-only action log ("which admin decided what, when"). The only write path that touches either is `provider-document-reviews.ts#recordProviderDocumentReview(session, input)` — it re-checks `session.role === "admin"` itself (data-layer authority, same pattern as `offers.ts#updateOfferStatus`, not just a UI gate), requires a non-empty note for `rejected`/`revision_requested` (optional for `approved`), and only appends the log row after the denormalized fields actually wrote successfully. If the log-row write itself then fails, the function best-effort rolls the just-written denormalized fields back to their pre-change values and returns a real `{ok: false}` (a pre-database-migration audit fix, "Y4") — previously that write's return value went unchecked, so a failed log write still reported `{ok: true}` to the admin UI while the review history silently never recorded it.
- **`document-validation.ts` + `app/api/provider-documents/validate/route.ts`** mirror the photo-upload pipeline's two-layer pattern exactly (client pre-check / server real validation), extended for document formats: PDF/DOC(X)/XLS(X)/ODT/JPG/PNG/WEBP/HEIC/HEIF/TIF, magic-number-vs-extension cross-checking (`EXTENSION_CONTAINER`), and `hasDangerousDoubleExtension` to catch a script hidden as a middle extension segment (`"rapor.exe.pdf"`). Because `doc`/`xls` share the same OLE-CFB signature and `docx`/`xlsx`/`odt` share the same ZIP signature, the client layer can only classify by container family — the server route (Node-only, `runtime = "nodejs"`) does the deeper check: raw byte-search for known ZIP entry names (`word/document.xml`, `xl/workbook.xml`, `mimetype`/`opendocument.text`) to disambiguate the Office/ODF subtype, and rejects any ZIP containing a `vbaProject.bin` entry (a macro-enabled file renamed to a "safe" extension) regardless of declared extension. Images are further verified by actually decoding them with `sharp` (HEIC/HEIF falling back to `heic-convert`, same as the photo pipeline). This route has no `role`/session check — at the point registration calls it, no real user/session exists yet, so it's input validation only, not an authorization boundary (documented in the route's own comment). `ALLOWED_EXTENSIONS` is exported precisely so a narrower upload surface (the Gümrük Müşaviri İzin Belgesi field, below) can pass a restricted subset instead of hand-duplicating the list.
- **`provider-document-consents.ts`** records acceptance of one or more versioned, single-sentence declarations, keyed by `ProviderDocumentStatementId` (`getProviderDocumentConsent`/`recordProviderDocumentConsent` both take an optional `statementId`, defaulting to `GENERAL_DOCUMENT_STATEMENT_ID` so every pre-existing call site keeps working unchanged) — same versioning principle as `legal-documents.ts`'s registry (each statement's version is bumped independently, never caller-supplied), but deliberately not a full `LegalDocumentMeta`: each is one sentence shown inline in the registration form, not a multi-section document with its own route/page. `"belge-dogruluk-beyani"` (the original, general "documents are genuine" declaration) is required for every `hizmet-veren` registration that uploads a general document; `CUSTOMS_LICENSE_STATEMENT_ID` (`"gumruk-musaviri-belge-beyani"`, "Yüklediğim belge bana aittir ve günceldir.") is a second, independent declaration recorded only for Gümrük Müşavirliği registrations (see below) — both can exist as separate rows for the same user.
- **`provider-registration.ts#registerProviderAccount` describes the pre-Supabase-Auth, now-legacy registration transaction** — per "Supabase Auth migration" above, this function is orphaned/unused (`login-form.tsx` calls `supabase.auth.signUp()` instead for every role today, and the real form no longer even collects the services/documents this function expects). Kept here for grounding on the underlying four-table transaction shape, which the *current* per-category `/panel/belge-yukleme` flow above deliberately does **not** replicate — a single document/category upload there is one Storage-upload-then-RPC call, not a four-table all-or-nothing transaction, since there's much less to roll back. Historically: it drove `users.ts#registerUser` + `provider-services.ts` + `provider-documents.ts` + `provider-document-consents.ts` as one manually-simulated "all or nothing" transaction, the same rollback-after-partial-write shape `job-store.ts#createJobsForOperation` uses for photos, just spread across four tables instead of one: documents are uploaded to IndexedDB *before* this function is ever called (so the user can preview/delete them while still filling out the form — `provider-document-upload.tsx`, which deletes any orphaned blob on unmount if the form is abandoned before submit), so every failure path here — including `registerUser` itself failing — deletes those same blobs (`deletePhotoBlobs`), and every failure *after* `registerUser` succeeded also deletes the just-created user plus any of the other three tables' rows already written, via `deleteUserById`/`deleteProviderServicesForUser`/`deleteProviderDocumentsForUser`/`deleteProviderDocumentConsentForUser`.
- **Gümrük Müşavirliği ("Customs Brokerage") is a second, service-specific KYC extension layered on top of the base flow above, centralized in `customs-license.ts`** — the single source of truth for: (1) `isCustomsOnlyRegistration`/`isGeneralDocumentRequired`, which the registration form (`login-form.tsx`), client validation (`register-form-validation.ts`), and the data layer all call identically — picking *only* Gümrük Müşavirliği (`service-catalog.ts#GUMRUK_MUSAVIRLIGI_SERVICE_CATEGORY_ID`) skips the general Faaliyet Belgesi/declaration requirement entirely (a dedicated "Gümrük Müşaviri İzin Belgesi" upload plus its own `CUSTOMS_LICENSE_STATEMENT_ID` declaration takes its place, tagged `CUSTOMS_LICENSE_DOCUMENT_TYPE` on `provider-documents.ts`), while picking it *alongside* another category requires both the general document and the customs license, since the two requirements are additive, not exclusive. (2) Previously, `canSubmitOffersAsCustomsBroker(userId)` was an account-wide offer-submission gate blocking *every* offer (not just Gümrük-category ones) until the license document was approved, checked in `offers.ts#canProviderSubmitNewOffer`/`createOffer` and mirrored in `offer-panel.tsx`. **That function and both call sites are now deleted** (uncommitted `offers.ts`/`offer-panel.tsx` diffs): the generalized "Hizmet Bazlı Provider Yetkilendirmesi" (see "Service Authorization" below) produces the same effect and better — it blocks only the unauthorized category, not the whole account — so the customs-specific gate was retired as redundant rather than kept alongside it. `customs-license.ts` survives only for point (1) above, documented in the module's own rewritten header.
- **Nakliye/Gümrük Müşavirliği discovery isolation, as this bullet used to describe it, no longer exists as its own rule** — `job-visibility.ts#ISOLATED_SERVICE_CATEGORY_IDS`/`getSelectedIsolatedCategoryIds` are deleted outright (uncommitted diff). It's been generalized to *every* category, not just these two, and re-gated on admin **authorization** rather than mere service **selection** — see "Service Authorization" below, which is now the single source of truth for `job-visibility.ts#resolveVisibility` and everything that used to depend on this bullet (`isJobVisibleToSession`/`filterVisibleJobs`/`useIsJobVisibleToSession`/`useFilterVisibleJobs`, the `my-offers-panel.tsx` "İlan artık mevcut değil" fallback for a hidden job's own old offer, `panel-summary.ts`'s visibility-filtered Hizmet Veren counters — all still exist, just now reading authorization instead of selection).
- **`app/admin`'s original review screen — `admin-provider-document-review-panel.tsx` and its Supabase-backed sibling `admin-provider-document-review-panel-remote.tsx` — was dead code (kept but unused) for a while, and is now actually deleted** (uncommitted diff — check `git status` for the `D`) — see "Admin panel modularization" below for what replaced it. The feature it implemented is fully absorbed by `/admin/firma-belgeleri` (`supabase-provider-document-review.ts`, itself a straight port of this same read/write shape onto Supabase tables): gated on `is_admin()`, listing every provider's documents with Onayla/Reddet/Yeniden Belge İste-equivalent actions (now `reviewProviderDocumentRemote`), split by `document_type`/`service_category_id` rather than a dedicated Gümrük Müşavirliği section. A second, unrelated `/admin` module for contact-form triage lives at `app/admin/iletisim-mesajlari` — see "Bize Ulaşın" below; it was untouched by the modularization work aside from also gaining the server-side `requireAdminOrRedirect` guard, and its `AdminNav` tab strip now renders *only* there (its former other host is the just-deleted panel).

### Service Authorization (Hizmet Bazlı Provider Yetkilendirmesi)

Migration `0038_service_authorization.sql` (uncommitted) replaced the old, inconsistent access model — two "isolated" categories (Nakliye, Gümrük Müşavirliği) gated purely by *selecting* them in `provider-services.ts`, every other category gated by nothing at all — with one rule applied uniformly to **every** service category: a provider only sees or can offer on a category's jobs once an **admin** has explicitly authorized that provider for that specific category. Selecting a service in `provider-services.ts` (still how a provider declares interest, still what drives the "Belge Yükleme" flow above) no longer grants any access by itself.

- **Schema/RPCs.** New table `public.provider_service_authorizations`: `provider_id`, `service_category_id`, `authorized_at`/`authorized_by`, `source_document_id` (nullable FK into `provider_documents`), `authorize_reason`, `revoked_at`/`revoked_by`/`revoke_reason` (all-or-none). Rows are never deleted, only revoked — a partial unique index enforces at most one *active* (non-revoked) row per provider+category, same pattern as `provider_badges`. RLS: readable by the owning provider or an admin only. Three admin-only (`is_admin()`) `SECURITY DEFINER` RPCs: `authorize_provider_service(p_provider_id, p_service_category_id, p_source_document_id?, p_reason?)` (idempotent upsert of the active row, fires a `service_authorized` notification); `revoke_provider_service_authorization(p_provider_id, p_service_category_id, p_reason)` (reason required); and `request_provider_document(p_provider_id, p_service_category_id, p_message?)` (`0039` — writes **no** authorization row, purely a `service_document_required` notification nudging the provider toward `/panel/belge-yukleme?hizmet=...`, see above). The single decision point both RLS and every RPC/view funnel through is `public.provider_can_view_category(p_provider_id, p_category_id)` — every existing caller (`jobs_select_visible` RLS, `create_offer`/`accept_offer`) was already written against this function, so redefining its body in place propagated the generalization with zero call-site changes. `provider_documents` also gained a nullable `service_category_id` column and `create_provider_document` a matching 7th parameter (validated against the caller's own selected services, else `ML124`).
- **Not hardcoded to specific categories** — unlike the old Nakliye/Gümrük-only isolation list, this applies uniformly to every row in `service_categories`; the old `service_categories.visibility_scope` column is now unread/dead.
- **Client-side gating.** `job-visibility.ts` was rewritten around this: `resolveVisibility` now checks `authorizedServiceCategoryIds.includes(jobCategoryId)` unconditionally for every category (no more isolated-only branch), fed by `getAuthorizedServiceCategoryIdsSync` (`supabase-provider-service-authorizations.ts`) rather than the old selected-services read. The reactive hook counterpart, `useAuthorizedServiceCategoryIds(providerId)`, now lives in its own file, `app/_lib/use-authorized-services.ts` — a thin `useSyncExternalStore` wrapper around that same module's `authorizedServicesStore`/`refreshAuthorizedServiceCategoryIds`, fetching once per mount/`providerId` change (a real Supabase round-trip, not a local read — deliberately not a live subscription, since a page-refresh/remount cadence is sufficient here). A second, separate reactive hook, `app/_lib/use-my-service-authorizations.ts#useMyServiceAuthorizations(providerId)`, wraps `supabase-my-service-authorizations.ts#getMyServiceAuthorizations` (the *display*-oriented 7-status read described below, not the visibility check) for `provider-service-status-card.tsx` and `document-upload-content.tsx` — its `loading` flag deliberately only ever moves `true → false`, never back to `true` on a `providerId` change mid-session, to respect the project's `react-hooks/set-state-in-effect` rule. Offer submission checks the visibility gate via `isJobVisibleToSession` inside `offers.ts#canProviderSubmitNewOffer`/`createOffer`. This is a **replacement, not an addition** — see the deleted `canSubmitOffersAsCustomsBroker` note above.
- **Provider-facing UI.** `provider-service-status-card.tsx` ("Hizmet Yetkileri," on Panel > Profilim) shows every catalog category with a per-row status from `supabase-my-service-authorizations.ts#getMyServiceAuthorizations` (`MyServiceStatus`, 7 values, joining `provider_services`+`provider_documents`+`provider_service_authorizations`): `not_selected` ("Seçilmedi"), `document_required` ("Belge Gerekli"), `document_pending` ("Admin Onayı Bekleniyor"), `document_rejected` ("Reddedildi"), `approved_awaiting_authorization` ("Belge Onaylandı, Yetki Açılıyor" — added alongside `0041` below; a document that's `approved` but whose authorization row hasn't landed yet, now a rare/transient state rather than a dead end since approval auto-authorizes), `authorized` ("Aktif"), `revoked` ("Yetki Kaldırıldı"). `provider-listing-access-banner.tsx`, wired into `provider-job-listing.tsx`, replaces the generic empty-state on Aktif İlanlar with a category-specific "you're not authorized for X yet" notice when relevant.
- **Admin-facing UI.** `admin-service-authorizations-card.tsx` ("Hizmet Yetkileri"), rendered on the Firmalar company-detail page (`admin-company-detail.tsx`, `/admin/firmalar/[id]`) — one row per category with Belge Talep Et / Yetkilendir / Yetkiyi Kaldır (reason-required) actions, wrapping `admin-companies.ts#authorizeProviderServiceAsAdmin`/`revokeProviderServiceAuthorizationAsAdmin`/`requestProviderDocumentAsAdmin`. This card explicitly replaced the old "Rozet Durumu" (badge) row on the same page — see "Provider verification badges" below for why that's a bigger change than it sounds (badges' own admin UI is now gone entirely, not just this one row).
- **Notifications are real rows here, not derived.** Unlike ~20 other `NotificationType`s (see "Notifications are derived, not stored" below), `service_document_required`/`service_authorized`/`service_authorization_revoked` are genuine persisted `public.notifications` rows, written inside the RPCs themselves and read via `supabase-service-authorization-notifications.ts`/`use-service-authorization-notifications.ts`, merged into the rest of `use-notifications.ts`.
- **Supabase-only, deliberately not dual-write** — same reasoning as `provider_badges`: an admin grant on one device must be visible to the provider on a completely different device on next refresh, which a localStorage mirror can't do (documented at length in `supabase-provider-service-authorizations.ts`'s own header).
- **`0041_auto_authorize_on_document_approval.sql` (uncommitted) links document approval to authorization, which `0038` had deliberately left as two separate admin actions.** Root cause (found in real testing, not design review): an admin approving a document via `/admin/firma-belgeleri` never itself changed `provider_can_view_category` — the admin had to *also* go authorize the same category from the Firmalar detail page, or the provider stayed stuck "belge onaylı, hizmet yetkisiz" despite `/ilanlar` truthfully-but-confusingly saying "not authorized yet." Fix: `review_provider_document`'s `p_status = 'approved'` branch now calls `authorize_provider_service` itself, in the same already-`is_admin()`-verified context, best-effort (wrapped in its own exception handler — a failure here never rolls back the document approval itself). Target category resolution: the document's own `service_category_id` if set; else `gumruk-musavirligi` if `document_type = 'gumruk-musaviri-izin-belgesi'`; else (only pre-`/panel/belge-yukleme`-era general documents with neither) every one of the provider's currently-selected non-Gümrük categories. A one-time backfill DML (direct `INSERT`, not through the RPC — `auth.uid()` is `NULL` in migration context so `is_admin()` would always fail `MLK50`) retroactively authorizes every already-`approved` document with no matching active authorization row, using the document's real `reviewed_by`/`reviewed_at` rather than "now." A new view, `public.provider_document_authorization_gaps`, is a live health check for the same inconsistency going forward (should stay empty in normal operation) — `admin-dashboard-data.ts#documentApprovalAuthorizationGaps` counts it and `admin-dashboard-content.tsx` renders a warning card when non-zero. A second, independent bug found alongside this (in app code, not this migration): `getMyServiceAuthorizations`'s status ladder had no branch for "document approved but no active authorization," silently falling through to `document_required` (as if no document existed at all) — fixed by adding the `approved_awaiting_authorization` status above; `0041`'s fix makes this branch rarely trigger in practice, but it was a real, independently-rooted classification bug either way.

### Provider verification badges (Doğrulama Rozetleri)

A committed, Supabase-only (no app-code UI yet outside the admin panel below) trust-signal system, added in migration `0025_customs_documents_and_verification_badges.sql` alongside an unrelated, since-fully-reverted feature (see the note at the end of this section) — `badge_types` (an extensible catalog, DB-first rather than code-first like `service-catalog.ts`, so a new badge type is just an `INSERT`, no migration/CHECK change needed) is seeded with exactly two rows: `mavi-tik` ("Mavi Tik"/Blue Check — a company whose mandatory verification documents, `provider_documents`, have been admin-approved) and `altin-tik` ("Altın Tik"/Gold Check — reserved for future performance-based criteria, `is_purchasable: false` on both rows, and explicitly **not purchasable**, no automatic-award RPC exists for either today). `provider_badges` is the grant/revoke history (one active row per provider+badge-type via a partial unique index on `revoked_at is null`; revoking never deletes a row, it stamps `revoked_at`/requires a reason) — written only via the admin-only `grant_provider_badge()`/`revoke_provider_badge()` RPCs. **No RLS policy exposes `provider_badges` to anyone but the badge's own owner or an admin** — there is deliberately no public/counterparty-visible view yet, so badges do **not** currently render on job listings, offer cards, incoming-offer cards, or any provider-profile screen. **As of a later, uncommitted pass, no admin UI surfaces them either.** The admin "Firmalar" module briefly surfaced badge state as `activeBadgeLabels`, and a dedicated "Rozet Yönetimi" module (`admin-badges.ts`) once granted/revoked them via `grant_provider_badge`/`revoke_provider_badge` — both were removed once the generalized "Hizmet Bazlı Provider Yetkilendirmesi" (see "Service Authorization" above) shipped: `admin-companies.ts`'s own header comment states the badge read was deliberately dropped in favor of the real, functional per-category authorization, and `app/admin/rozet-yonetimi`/`admin-badges-list.tsx`/`admin-badge-detail.tsx`/`admin-badges.ts` no longer exist in the tree at all — actually deleted, not orphaned-but-present like the old document-review panel above used to be. `badge_types`/`provider_badges` themselves are untouched in the database and still fully functional; `grant_provider_badge`/`revoke_provider_badge` today are only ever invoked by `scripts/test-customs-documents-and-badges.mjs`'s permanent regression test (see "Commands" above) — there is currently no admin UI path to grant or revoke a badge at all. Don't confuse this with the *new* "Hizmet Yetkileri" card on the Firmalar company-detail page (`admin-service-authorizations-card.tsx`): that's a different table (`provider_service_authorizations`) gating actual job-category access, not a trust badge — one dev comment loosely calls it the badge row's replacement, but functionally it's a new, unrelated access-control mechanism, not a badge-management UI.

`0025` originally also added a second, unrelated piece — `job_customs_documents` (a new, job-scoped customs-paperwork upload table, distinct from the already-documented provider-registration `customsDocuments`/Gümrük Müşavirliği fields above) plus its Storage bucket (`0026_job_customs_documents_storage.sql`) — which was a scope error: MALSEVK never stores shipment paperwork (bill of lading, invoice, packing list, certificate of origin, etc.), only a company's own verification documents. `0027_remove_job_customs_documents.sql` fully reverts it (the table, both its RPCs, and the three Storage policies, dependency-ordered, no `CASCADE`) without touching `badge_types`/`provider_badges`/`0025`'s own Bölüm 2-3 at all — the badge half was never in scope for the revert. One harmless remnant: the `job-customs-documents` row in `storage.buckets` itself can't be dropped via plain SQL (Supabase blocks direct DDL on Storage tables, `42501`) and needs a separate service-role Storage Admin API call to fully clean up — noted as future ops work in `0027`'s own header, not yet done.

### Admin panel modularization (in progress, uncommitted)

A restructuring of `/admin` into a multi-module dashboard, layered on top of the Supabase Auth migration above — every file here is new/uncommitted working-tree state (check `git status`), extensively commented (Turkish, tagged "ADMIN PANELİ MODÜLERLEŞMESİ"). It started additive/transitional, not a clean cut — old files kept as dead code by design, two different admin-nav paradigms coexisting pending a later unification — but that's since partially resolved: the one screen that actually *was* dead code (the original document-review panel) has now been deleted outright rather than left in place, while the two-admin-nav-paradigm split (`AdminShell` vs. `AdminNav`) is still there, just narrower (`AdminNav` now has only one host left, see below).

- **`admin-shell.tsx#AdminShell`** is a sidebar+header layout (desktop: sticky left sidebar; mobile: horizontal scrollable pill nav, same visual language as `AdminNav`) listing 7 modules — **Dashboard** (`/admin`), **Hizmet Alanlar** (`/admin/hizmet-alanlar`), **Firmalar** (`/admin/firmalar`), **Firma Belgeleri** (`/admin/firma-belgeleri`), **İlan Yönetimi** (`/admin/ilanlar`), **Operasyon Yönetimi** (`/admin/operasyonlar`), **Sistem Beslemesi** (`/admin/sistem-beslemesi`) — each gated server-side by `require-admin.ts#requireAdminOrRedirect(currentPath)` (no Supabase user → redirect to `/giris-yap?redirect=...`; a real but non-admin user → `notFound()`, same route-hiding pattern as `app/gelistirme/demo-veri-sifirla`). **This list has already changed shape once, uncommitted**: the original "Faz 2" set had a standalone **Rozet Yönetimi** (`/admin/rozet-yonetimi`) module in this slot instead — it and its backing files (`admin-badges-list.tsx`/`admin-badge-detail.tsx`/`admin-badges.ts`) are now deleted outright (not orphaned-but-present, actually gone from the tree), replaced in the nav by **Hizmet Alanlar** (a new, unrelated module, 2nd position — see below) once the generalized "Service Authorization" feature (above) made the old badge-management screen pointless — see "Provider verification badges" above. Net effect: still 7 modules, still none are placeholders, but the set and the 2nd-position slot both changed. `AdminShell` **does not touch or replace** the pre-existing `AdminNav` two-tab strip (`/admin` "Hizmet Veren Belge Kontrolü" / `/admin/iletisim-mesajlari` "Bize Ulaşın Mesajları") — but `AdminNav` now renders **only** inside `iletisim-mesajlari`'s panel (also gated by `requireAdminOrRedirect` server-side, on top of its own pre-existing client-side check): its other former host, `admin-provider-document-review-panel.tsx`, has since been deleted too (see "Provider document verification" above), so `AdminNav`'s "Hizmet Veren Belge Kontrolü" tab is now stale in a stronger sense than before — it links to `/admin`, which has never shown that screen since `AdminShell` took over, and there is no longer a second surface anywhere that still renders `AdminNav` alongside it.
- **`app/admin/page.tsx`** (converted to an `async` Server Component) now renders `AdminShell` + `admin-dashboard-content.tsx` instead of the old `AdminProviderDocumentReviewPanel` — that panel's function (provider-document review) now lives entirely in the `/admin/firma-belgeleri` module below, with a richer UI (status-tab filtering, a dedicated per-document review screen, full review history) than the old single-screen version ever had. The old panel component files (`admin-provider-document-review-panel.tsx` and its Supabase-backed sibling `admin-provider-document-review-panel-remote.tsx`) were left in the tree unused for a while, out of an earlier task's scope — they've since been deleted outright (uncommitted `D` in `git status`), so "dead code, kept but unused" no longer applies; see "Provider document verification" above. `admin-dashboard-content.tsx`'s stat-card grid also gained a `totalRequesters` tile linking to the new `/admin/hizmet-alanlar` (below), alongside the pre-existing `totalProviderCompanies`/`pendingDocumentReviews` tiles. It also renders a conditional warning card (only when non-zero) for `documentApprovalAuthorizationGaps` — a count from the `0041` health-check view `provider_document_authorization_gaps` (see "Service Authorization" below), surfacing the "belge onaylı ama yetkisiz" data inconsistency `0041` itself is meant to prevent from recurring.
- **`/admin/firmalar`** (`admin-companies-list.tsx` + `admin-company-detail.tsx`) lists every `role = 'hizmet-veren'` profile with search/filters (service category, document status, active/inactive, date range) and a per-company detail page (profile info, service categories, document-status summary, plus the "Hizmet Yetkileri" card — see "Service Authorization" above — where the badge-status row used to be). Its "Firma Belgelerine Git" button links to `/admin/firma-belgeleri?firma={id}`, which that module reads via `useSearchParams` to scope its list to just that company. The detail page also gained two admin-only actions (uncommitted, migration `0036_profile_edit_as_admin.sql`): **Askıya Al/Geri Aç** via the pre-existing `suspend_user`/`reinstate_user` RPCs (`0016`) — this is the first real UI caller of either — and **Profili Düzenle**, opening the shared `AdminProfileEditForm` (also used by Hizmet Alanlar, below) which calls `update_profile_as_admin` (`admin-companies.ts#updateCompanyProfileAsAdmin`). That RPC edits the exact same 6 `profiles` columns a self-service user can already edit via `basic-profile-editor.tsx` (`full_name`/`phone`/`company_name`/`company_type`/`province`/`district`) — it doesn't widen *which fields* are editable, it bypasses the row-ownership restriction in `profiles_update_own`'s RLS policy (`id = auth.uid()` only, no `is_admin()` branch) via `SECURITY DEFINER`, since the pre-existing column-level grants alone don't let an admin update someone else's row.
- **`/admin/firma-belgeleri`** (`admin-company-documents-panel.tsx` + `admin-document-review-detail.tsx`) lists all provider documents with status tabs (Bekleyen/Onaylanan/Reddedilen/Revizyon/Tümü) and a per-document review screen — reusing the existing `supabase-provider-document-review.ts#reviewProviderDocumentRemote` RPC wrapper and review-history read, not a new write path.
- **`/admin/hizmet-alanlar`** (`admin-requesters-list.tsx` + `admin-requester-detail.tsx`, backed by `admin-requesters.ts`) is Hizmet Alanlar's structural counterpart to Firmalar above, but scoped to `role = 'hizmet-alan'` profiles and reading only the `profiles` table (no `provider_profiles`/`provider_services`/`provider_documents`/`provider_badges` joins, since requester accounts never have those rows) — `id, full_name, company_name, company_type, phone, province, district, account_status, created_at`, via the same `is_admin()` RLS branch. The list supports free-text search (name/company/phone) plus an active/inactive account-status filter. The detail page shows the same profile fields and the same two actions as Firmalar's detail page above — suspend/reinstate, and "Profili Düzenle" via the same shared `AdminProfileEditForm` → `update_profile_as_admin` (here, `admin-requesters.ts#updateRequesterProfileAsAdmin`) — but no "view jobs" action exists for a requester. **This module took over Rozet Yönetimi's old nav slot** (see the `admin-shell.tsx` bullet above) purely because both changes shipped in the same pass, not because the two are related — Rozet Yönetimi's badge-management UI is fully gone (see "Provider verification badges" above), and this module is unrelated new work that happened to land alongside it.
- **`/admin/ilanlar`** (`admin-jobs-list.tsx` + `admin-job-detail.tsx`, backed by `admin-jobs.ts`) lists every non-deleted job from the `admin_job_list` view (`0017`) with a **derived, not stored**, 6-value status (`yayinda`/`teklif_bekliyor`/`devam_ediyor`/`tamamlandi`/`suresi_doldu`/`kapatildi` — `admin-jobs.ts#deriveStatus`, computed client-side from `closed_at`/`publish_end_at`/the job's offer statuses, deliberately independent of but conceptually parallel to the localStorage-side derivation in `job-requests.ts`) and an admin "yayından kaldır" action via the `close_job_as_admin` RPC (`0016`) — that RPC's `reason` param is **not** free text, it's constrained to the same 4-value `JobClosureReason` CHECK `job-closure.ts` already defines client-side (a real bug — free text was originally sent and silently rejected with `23514` — caught and fixed during this module's own live Playwright testing). It now also renders product-info fields (Ürün Adedi/Tonaj/Ürün Cinsi) — the earlier claim that it didn't, because `public.jobs` had no matching columns before migration `0028`, is stale; it's since been revisited. **It also gained a full job-moderation + content-edit capability** (uncommitted, migrations `0035_job_moderation.sql`/`0037_job_admin_edit_field_parity.sql` — see "İlan Moderasyonu" below for the schema/RLS/RPC detail): an "İlan Moderasyonu" card on `admin-job-detail.tsx` with Onayla/Reddet/Düzenle actions, and an inline `admin-job-edit-form.tsx` covering title/description/location/dates/product-info/customs-product-type/delivery fields (category itself stays uneditable), writing through `update_job_as_admin`.
- **`/admin/operasyonlar`** (`admin-operations-list.tsx` + `admin-operation-detail.tsx`, backed by `admin-operations.ts`) is **read-only by design** (task requirement: an admin must never manually change an operation's status) — no RPC/write path exists in this module at all. Despite the module's name, each row is actually an individual `offers` record (from the `admin_offer_list` view, `0017`), not a row from the `operations` table — `admin-operations.ts`'s own comment explains why: `operations` only ever gets a row from a genuine multi-service creation (`create_operation_with_jobs`; the single-job `create_job` path never writes one), so it isn't rich enough to represent the module's 5-bucket status model (`bekleyen`/`devam_eden`/`tamamlanan`/`iptal`/`anlasmazlik`, derived from `Offer.status` the same way the app's own `job-requests.ts` groups statuses) or the requested single-Offer detail fields (hizmet veren/hizmet alan/ilan/teklif). The detail screen's full `offer_status_history` (`0005`) timeline is real, not synthesized.
- **`/admin/sistem-beslemesi`** (`admin-facility-candidates-list.tsx` + `admin-facility-candidate-detail.tsx`, backed by `admin-facility-candidates.ts`) is "Admin Paneli Faz 3" — a review queue ("Tesis Onay Merkezi") for the free-text facility names users type when picking "Listede yok, kendim gireceğim" (see "Centralized job location"/"Nakliye Güzergâh Yönetimi" below), backed entirely by migration `0029`'s new `facility_candidates`/`facility_candidate_raw_entries` tables and its four RPCs (`submit_facility_candidate_entry`, `approve_facility_candidate`, `reject_facility_candidate`, `update_facility_candidate_suggestion`) — no existing table/migration was touched. List view filters by status (Bekleyen/Onaylanan/Reddedilen)/confidence/search; the detail screen lets an admin correct the suggested facility type (`LIMAN`/`OSB`/`SANAYI`/`ANTREPO`/`GUMRUK_SAHASI` — a 5-value vocabulary the migration deliberately introduced fresh rather than reusing `turkey-locations.ts#FacilityType`, since the two don't overlap 1:1) before approving or rejecting with a reason. **Approving a candidate only flips its own row's `status` to `'approved'`** — it does *not* feed back into `data/locations/locations.json` (the actual catalog `turkey-locations.ts` reads from): that file is a static, build-time-bundled JSON produced offline by `scripts/locations/`, and Vercel's read-only filesystem makes a live admin-triggered rewrite of it impossible (`0029`'s own header explains this design constraint). Treat this module as a discovery/triage queue, not a live facility-catalog editor — see "Supabase Geçişi Faz 2–4" below for how candidates actually get submitted into it.
- **Fully Supabase-backed, no localStorage.** `admin-companies.ts`/`admin-requesters.ts`/`admin-dashboard-data.ts`/`admin-jobs.ts`/`admin-operations.ts`/`admin-facility-candidates.ts` query `profiles`, `provider_profiles`, `provider_services`/`service_categories`, the `provider_document_status_summary`/`admin_job_list`/`admin_offer_list` views (`0017`), `provider_documents`, `provider_service_authorizations` (`0038`), `jobs`, `offers`, `offer_status_history`, `operations`, `facility_candidates`/`facility_candidate_raw_entries` (`0029`), and the `active_job_listings` view — all via `createSupabaseBrowserClient()`, relying entirely on each table's existing `is_admin()` RLS branch (no `service_role` bypass). RPCs now span `0016`'s `close_job_as_admin`/`suspend_user`/`reinstate_user`, `0029`'s facility-candidate RPCs, `0036`'s `update_profile_as_admin`, `0038`/`0039`'s `authorize_provider_service`/`revoke_provider_service_authorization`/`request_provider_document`, and `0035`/`0037`'s `approve_job_as_admin`/`reject_job_as_admin`/`update_job_as_admin` — `badge_types`/`provider_badges` (`0025`) are no longer queried by any admin module, see "Provider verification badges" above. Dashboard counts render as "—" (`null`) rather than a fake `0` on query failure, so a broken query never masquerades as "genuinely zero."

### Çoklu Hizmet Operasyonu (multi-service job creation)

`job-request-form.tsx` (the Hizmet Alan job-creation form, `/hizmet-talebi-olustur`) lets one submission produce **more than one independent `Job`** — e.g. a requester needing both Lashing and a separate Depolama service for the same operation, each with its own dates, gets two fully independent `Job` records rather than one job that awkwardly represents two services. This was built incrementally and the pieces only make sense together:

- **Data model**: `Job.operationId?: string` (a `crypto.randomUUID()`, produced by `job-store.ts#createOperationId`) is the *only* thing that links sibling jobs — there is no separate "operation" table/record. It is purely an internal join key, never a human-facing code (no `MSOP-...`-style display string exists anywhere). `job-store.ts#getJobsByOperationId(operationId)` / `getSiblingJobs(jobId)` (the latter: resolve the job, return `[]` if it has no `operationId`, else return `getJobsByOperationId` minus itself) are the only two query helpers — both operate purely over `readUserCreatedJobsSnapshot()` (user-created jobs only; the static `jobs.ts` seed listings can never have an `operationId`).
- **Two independent creation paths, deliberately not unified**: `job-store.ts#createJob` (single job, unchanged from before this feature — no `operationId` is ever written by it) and `job-store.ts#createJobsForOperation` (`services: OperationServiceInput[]`, **rejects** anything with fewer than 2 entries with an explicit error rather than silently delegating to `createJob`). Deciding which to call is the *form's* job (`job-request-form.tsx`'s `services.length === 1` branch), not the data layer's — `createJobsForOperation` refuses to be a drop-in replacement for the single-job path.
- **Every field is per-service, not operation-wide** — category, title, description, dates (`workDate`/`workEndDate`), and the full location (district/facility/address/custom-mode fields) all live on each `OperationServiceInput` entry. Only `province` (Türkiye geneli serbestçe seçilebilir; Kocaeli is just the shared starting default — see "Provider job listing" below), `operationDetails`, and the photo set are genuinely shared across the whole operation. In the form, each service is a `ServiceEntry` (local React state, `job-request-form.tsx`) keyed by a client-only `localId: crypto.randomUUID()` — never sent to the data layer, never used as an array index for identity (so deleting a middle card can't corrupt a later card's state, and React never confuses two cards after a reorder).
- **"Ana hizmetle aynı lokasyon"**: every service after the first (`index > 0`) has a `useMainLocation` toggle, checked by default. When checked, `job-request-form.tsx#getEffectiveLocation` resolves that service's location fields from the *first* service (`services[0]`) instead of its own (empty) ones — this is a form-only convenience; `createJobsForOperation` itself has no concept of "same as main," it just receives each service's already-resolved location. Unchecking reveals that card's own full İl→İlçe→Bölge/Tesis→Açık Adres picker (same `SearchableSelect` flow as "Centralized job location" below).
- **Atomicity without a real transaction**: `createJobsForOperation` validates everything up front (category count/uniqueness/catalog-validity via `service-catalog.ts#isServiceCategoryId`, each service's own date-range sanity — deliberately re-checked here even though `createJob` never validates `workDate` itself, since multiple independent ranges arriving in one call warrant the extra defense — and photo count), then persists a **separate** copy of the shared photos for *each* service (`persistPhotosOrRollback` called once per service, fresh `storageKey`s every time) before writing anything to the jobs table. If any one service's photo persistence fails, every already-persisted sibling's blobs are deleted and the function returns an error *before* `writeUserCreatedJobs` is ever called — so a failure never leaves a partial operation (some jobs created, some not). The one deliberate cost of per-service photo copies (rather than a shared `storageKey`) is that the same image is duplicated in IndexedDB once per sibling service — necessary because `deleteJob`/`deleteJobPhoto` delete blobs by `storageKey`, and siblings must be deletable independently without corrupting each other's photos (see "sibling jobs are fully independent" below).
- **Sibling jobs are fully independent after creation** — same `operationId`, otherwise no relationship is enforced anywhere. There is no "the whole operation closes if one job closes" logic, no bulk edit/cancel/offer/notification of any kind. Offers, capacity, contact reveal, completion, disputes, ratings — every mechanism described elsewhere in this document keys off a single `Job`/`Offer`, and none of them were touched to special-case multi-service operations; a sibling job is, to every other system in the app, just another ordinary job that happens to share a `Job.operationId` value. The UI *does* surface the relationship for discovery/status purposes (see "Operation discovery & status UI" below) but never for bulk action — each sibling still takes its own independent offer/accept/complete/rate path through the existing single-`Job` machinery.
- **Operasyon Önizleme (preview) is a client-side mode switch, not a route.** `job-request-form.tsx` holds a `mode: "form" | "preview"` state on the *same* component — submitting the form (`handleSubmit`) only validates (shared `operationDetails`/photo count via `job-form-validation.ts#validateSharedOperationFields`, each service via `validateServiceItem`, cross-service duplicate categories via `findDuplicateServiceCategoryIds`) and flips `mode`, it never calls `createJob`/`createJobsForOperation` itself. The preview renders every service's *resolved* data (real district/facility name, never a literal "same as main" placeholder) plus a single shared-photo thumbnail (`URL.createObjectURL` on `photos[0].blob`, revoked on change/unmount — photos are never re-uploaded for the preview). Its own "İlanı Yayınla" / "N Hizmet İlanını Yayınla" button (`handlePublish`) is the **only** place either creation function is actually invoked; "← Düzenlemeye Dön" just flips `mode` back — since nothing unmounts, no form state is ever lost. The same synchronous `submitLockRef` double-submit guard used elsewhere in this form now guards `handlePublish` instead of the form's `onSubmit`.
- **Post-creation redirect**: single service still goes to `/ilanlar/${job.id}` (unchanged). Multiple services redirect to `/panel/hizmet-taleplerim?operasyonIlanSayisi=${count}` — no new operation-detail route/page exists (deliberately out of scope) — and `job-requests-panel.tsx` reads that query param to show a "`{count}` hizmet ilanı başarıyla oluşturuldu." banner, following the exact same static (non-auto-dismissing) query-param-banner pattern already used there for `guncellendi=1` (set by `job-edit-form.tsx` after a single-job edit).

#### Operation discovery & status UI

A single card on `job-detail-content.tsx` (`/ilanlar/[id]`, not gated by role) and one grouping layer on the provider job listing surface an operation's siblings **purely for discovery/status** — it does not create a bulk-action surface; every "Teklif Ver"/accept/complete path still lands on a single `Job`/`Offer` exactly as described above.

- **`operation-status-card.tsx` ("Operasyon Durumu") is the only place on the page that lists an operation's sibling services** — it went through two prior, wrong iterations before landing here, worth knowing both so the history doesn't repeat: (1) it once coexisted with two sibling cards, `operation-sibling-jobs-card.tsx` ("Bu Operasyondaki Diğer Hizmetler") and `operation-service-offers-card.tsx` ("Operasyondaki Hizmetler", the one with the "Teklif Ver" link), all three rendering the *same* sibling list independently; (2) an attempt to "simplify" that only consolidated each row's *text* (folding a global badge + a viewer-specific note into one label) while still leaving all three cards standing — still three separate sections showing the same data. Both are gone: the two sibling files were deleted outright and every case they covered (data, status derivation, the "Teklif Ver" action, the sibling-row `Link` navigation) was folded into this one card's own row list, below its existing progress bar/summary tiles (which this consolidation did not change). Reads the full sibling list via `use-jobs.ts#useJobsForOperation(operationId)` (wraps `job-store.ts#getJobsByOperationId`, so ordering is always true creation order, never the reversed/newest-first order `useAllJobs()` uses elsewhere). Each row is a CSS **grid** (`grid-cols-1 sm:grid-cols-[minmax(0,1fr)_auto]`), not `flex flex-wrap` — the latter was a real bug: flexbox's wrap decision is based on children's *hypothetical* (unshrunk) width, not their post-shrink/`truncate`d width, so a long category/title combo made the row wrap the "Teklif Ver"/status badge onto its own line or push it left instead of letting the title column shrink-and-ellipsis. The grid's `minmax(0, 1fr)` left column has a real shrink floor of 0 (enabling `truncate`) and the `auto` right column never competes for space, so the action always stays right-aligned on one line at desktop widths; on mobile (`grid-cols-1`) the two stack as separate full-width rows with `justify-self-end` keeping the action flush right either way.
- **Status bucketing is centralized** in `job-requests.ts`: `getOperationStatusBucket(job, offers)` derives one of `OperationStatusBucket` (`getJobRequestFilter`'s 4 values + an `"iptal"` fallback) per job, `getOperationStatusBucketLabel`/`Tone` render it (now only for the card's own aggregate summary tiles — see below), and `accumulateOperationStatusCounts(jobs, getBucket)` is the shared reduce-to-summary helper behind `getOperationStatusSummary` (real, job-level truth — what the listing-page grouping below uses) and `getPublicOperationStatusSummary` (`getBucket` is injected precisely so summaries can share the counting math without duplicating it) — `operation-status-card.tsx`'s progress bar/summary tiles (Toplam Hizmet/Teklife Açık/Devam Ediyor/Tamamlandı/İptal Edildi) use the latter, GLOBAL/job-geneli and identical for every viewer; this aggregate layer is unrelated to the per-row status below and was not touched by the row-consolidation work.
- **Each sibling row shows exactly one status/action, from one function** — `offers.ts#getOperationServiceCardStatus(job, offers, session, {allowOfferAction})` — no parallel "global badge + personal note" pair (that was exactly the earlier, wrong design). It either returns `{kind: "teklif-ver"}` (this viewer, a `hizmet-veren`, can genuinely submit a new offer here — same gate as `canProviderSubmitNewOffer`/`offer-panel.tsx`) or `{kind: "label", label, tone}`, a single already-resolved string ("Teklif Bekliyor", "Teklif Kabul Edildi", "Devam Ediyor", "Tamamlama Onayı Bekleniyor", "İtiraz Sürecinde", "Tamamlandı", "İptal Edildi", "Başka Hizmet Verenle Anlaşıldı", "İşe Başlama Onayı Bekleniyor", or the generic "Teklife Açık" fallback) — resolution order and viewer isolation (own offer via `getOfferForJob` checked before any job-level fallback; a sibling's offer never leaks into another viewer's row) are documented on the function itself, not duplicated here. `allowOfferAction: !isCurrent` — the row for the job currently being viewed never shows "Teklif Ver" (its own real offer form is already on the same page, in the `OfferPanel` above) and is marked "(Bu ilan)" instead; that row is also the only one that isn't a link. Every other (sibling) row is a `<Link href="/ilanlar/[id]">` wrapping the whole row (preserving the old sibling-card's click-to-navigate/discovery behavior) — "Teklif Ver" renders as a plain `<span>` styled like a button *inside* that link rather than a nested `<a>`, since clicking anywhere on the row already goes to that job's own page where the real `OfferPanel` lives.
- **Aktif İlanlar listing grouping** (`job-listing-row.ts#groupJobListingRowsByOperation`): collapses 2+ sibling rows already surviving the filter bar into one `OperationListingItem` (`job-listing-table.tsx`/`job-listing-cards.tsx` render it in the *same* columns/card shape as a normal row — a dynamic "Hizmet Türü" badge replaces the category chip, a progress line reuses the closedReason note's slot — no new columns, ordinary single-job rows are pixel-identical to before). The representative/"primary" row is the true first-created sibling (via `getJobsByOperationId`, independent of the list's own display order) among currently-visible survivors. **The filter only ever decides whether an operation appears at all** (any one sibling passing the filter is enough) **— it never shrinks what the card reports**: total/completed/progress/summed-offer-count always come from the operation's full sibling set (`getOperationStatusSummary`, the real/job-level version — this listing is provider-only, so there's no owner-vs-viewer ambiguity to begin with), not just the filtered-visible subset. Getting this backwards was an actual shipped bug (Aşama 5.1): a filter that matched only 1 of an operation's 3 services used to make the whole operation look like a plain single-service job with a truncated total.
- **The badge and its detail line track per-service completion, not just total/progress.** `job-listing-row.ts#getJobListingCategoryBadgeLabel` (shared by table/cards, `job.title`/"İlan Başlığı" is never touched by it) renders `"{kategori} Hizmeti Arıyor"` for a single job, or `"Operasyon • {kalan} Hizmet Arıyor"` for an operation where `kalan = totalCount - completedCount` (both from `getOperationStatusSummary`, never guessed from progress percent) — a completed service drops out of `kalan` but the badge is never shown reading "0 Hizmet Arıyor" because the whole row disappears first (next bullet). Directly below the badge, `operation-service-tags.tsx#OperationServiceTags` (same shared-component pattern) lists **every** service in an operation (service-catalog order, never truncated/collapsed behind a "+N") as inline `·`-separated text, styling a completed one (`OperationServiceSummary.isCompleted`, from `getOperationStatusBucket`) with muted/strikethrough text rather than a removed/replaced tag — so a still-open sibling and a finished one are visible side by side.
- **Completed jobs/operations are removed from "Aktif İlanlar" entirely, a fourth gate alongside expiry/closure.** `job-completion.ts#isJobFullyCompletedForListing(job, offers)` is the single source of truth `provider-job-listing.tsx`'s `activeJobs` filter checks (`!isJobListingExpired && !isJobManuallyClosed && !isJobFullyCompletedForListing`): a single job counts once its own `getOperationStatusBucket` is `"tamamlandi"`; an operation counts only once **every** sibling (via `getJobsByOperationId`, the full set, not just filter-visible ones) is individually "resolved" — an internal `isOperationSiblingResolvedForListing` helper, `tamamlandi` bucket **OR** manually closed **OR** expired, not completion alone (fixed after a real bug: an operation with e.g. one truly-completed service and two manually-closed-but-never-completed services used to never satisfy an all-`"tamamlandi"` check, so its one completed sibling leaked through as a phantom single-job row instead of the whole operation disappearing) — one sibling that is still genuinely open (not completed, not closed, not expired) keeps the whole operation visible, and the remaining-count badge (`job-listing-row.ts#getJobListingCategoryBadgeLabel`'s `totalCount - completedCount`) is correct as a side effect: closed/expired siblings never enter `activeJobs`/`rows` in the first place (their own independent `!isJobManuallyClosed`/`!isJobListingExpired` checks exclude them), so any operation still shown only ever has (completed ∪ still-open) members in its totals. Same non-deleting architecture as the publish-window/closure gates: `Job.status` is never touched, nothing is deleted, the job/operation just stops appearing in this one discovery surface — `my-offers-panel.tsx`/`job-requests-panel.tsx`'s "Tamamlandı" tabs, `panel-summary.ts` counters, and `operation-status-card.tsx` (which must keep showing a completed operation's full history) all derive their own status independently and never call this function.

### İlan Yayın Süresi Yönetimi (job listing publish window)

`app/_lib/job-publish-window.ts` is the single source of truth for how long a `Job` stays active: every listing publishes for exactly `JOB_PUBLISH_WINDOW_DAYS` (14) days from `Job.createdAt`. Expiry never deletes the job — it only drops out of active lists/search/offer capacity and becomes manageable from a dedicated "Süresi Dolan İlanlar" tab (below). Every function in the file is pure and takes an optional `now: Date = new Date()` parameter, with no timer/scheduler anywhere — the state is recomputed on every read, by design, so the same logic could move server-side (a cron/queue worker) later with zero changes (see the file's own closing comment).

- **Window math**: `computePublishEndAt(startIso)` adds `JOB_PUBLISH_WINDOW_DAYS` in raw milliseconds (`Date.getTime()`), never calendar-day arithmetic — this deliberately avoids DST/timezone shifting a listing's close time by an hour depending on when/where it's viewed. `getJobPublishEndAt(job)` prefers the stored `publishEndAt`, falling back to deriving it from `createdAt` if missing. `createPublishWindow(now)` produces the `{createdAt, publishEndAt}` pair and is called **only** from `job-store.ts#createJob`/`createJobsForOperation`/`republishJob` — no other call site should hand-compute either field.
- **Legacy-safe default**: a job with no reliable `createdAt` (pre-dates this feature) is never treated as expired — `isJobPublishWindowExpired` returns `false` whenever `getJobPublishEndAt` can't resolve a date, rather than guessing.
- **Critical exception, shared with the offer-workflow rules**: `isJobListingExpired(job, offers, now)` is date-expiry AND-ed with `job-requests.ts#getSettledOfferForJob(job.id, offers) === null` — a job whose offer has reached `accepted`/`in_progress`/`completion_requested`/`completion_disputed`/`completed` is **never** counted as expired regardless of the date, using the exact same status set the rest of the app already treats as "settled" (no new threshold invented). If that offer later falls out of the set (`agreement_failed`/`cancelled`), the job becomes subject to the publish-window rule again — same reversal pattern as re-offering eligibility elsewhere in this doc. `isJobListingActiveForOffers` is just the negated, named form of this same check, used for readability at offer-gating call sites. `isExpiredListingAwaitingAction(job, offers, now)` is a narrower, separate check — expired **and** not yet `republishedToJobId` — used wherever a *second*, already-handled expiry shouldn't re-surface (notification, panel counter; see below), as opposed to the "Süresi Dolan İlanlar" tab itself, which still lists every expired job (republished or not) so history stays visible.
- **Where it's enforced**: `offers.ts#canProviderSubmitNewOffer` and `#createOffer` both check `isJobListingExpired` as an independent, later gate than `isJobClosedToNewOffers` — an expired listing can't collect brand-new offers even before that other check would fire. `getOperationServiceCardStatus` deliberately shows the more specific "İşe Başlama Onayı Bekleniyor" instead of "Süresi Doldu" when a pending offer is still awaiting the requester's decision on an already-expired listing (see job-publish-window.ts's own note on why this doesn't conflict with the settled-offer exception above). `provider-job-listing.tsx`'s "Aktif İlanlar" filters out expired jobs entirely (`job.status === "yayinda" && !isJobListingExpired(job, offers)`) — the seeded example listings (no `createdAt`) never need special-casing since they simply never expire.
- **Republishing** (`job-store.ts#republishJob`) is the sole authorized entry point for "Yeniden Yayınla." It clones rather than mutates: the old `Job` is left untouched except for a new `republishedToJobId`, and a brand-new `Job` (new id, fresh `createdAt`/`publishEndAt` — a new 14-day window, user-supplied `workDate`/`workEndDate`, freshly-copied photo `storageKey`s via `duplicateJobPhotos` — same independent-copy rationale as multi-service operation photos) is created with every other field copied verbatim. The two-way link (new `republishedFromJobId` = old id, old `republishedToJobId` = new id) makes history traceable in both directions. A second republish attempt against the same already-republished old job is rejected outright (`existing.republishedToJobId` already set) — a listing can never fan out into more than one active successor. Offers stay keyed to `jobId`, so the republished job structurally starts with zero offers — no filtering code needed to keep the old listing's offers from leaking in.
- **"Süresi Dolan İlanlar" tab** (`job-requests-panel.tsx`, between "Aktif" and "Devam Eden") renders a dedicated card per expired job (createdAt/publishEndAt, a "Süresi Doldu" status, and Yeniden Yayınla/Kalıcı Olarak Sil actions) — "Kalıcı Olarak Sil" reuses the exact same `DeleteJobDialog`/`offers.ts#deleteJobWithOffers` path as ordinary deletion elsewhere in the panel, not a separate delete flow. Once a listing has been republished, its card drops the action buttons in favor of a link to the new listing, matching the data-layer's own already-republished guard.
- **Surfacing**: `panel-summary.ts#expiredListingCount` counts `isExpiredListingAwaitingAction` jobs — mutually exclusive with `activeRequestCount`, which now also excludes expired jobs, so a real job lands in exactly one bucket. `notifications.ts`'s `ilan_yayin_suresi_doldu` ("İlan Süresi Doldu") is the **one** `NotificationType` derived directly from a `Job` rather than an `Offer` (no `offerId`) — built straight from `isExpiredListingAwaitingAction` over the requester's own jobs, `id: listing-expired-${job.id}`, `createdAt` set to `publishEndAt` (the real event time). It persists until the job is republished, at which point `republishedToJobId` being set removes it — consistent with the rest of "Notifications are derived, not stored" below.

### İlan Kapatma (manual job closure)

`app/_lib/job-closure.ts` is a second, independent way a `Job` stops being active — deliberately the same architecture as the publish-window feature above (job never deleted, `Job.status` never touched, state derived from a stamped field, no `Offer`-status invented) but triggered by the requester's own decision rather than a date. `Job.closedAt`/`Job.closureReason` (`JobClosureReason`: `baska-hizmet-verenle-anlasildi` | `hizmete-ihtiyac-kalmadi` | `yanlislikla-olusturuldu` | `diger`) is the one field pair `isJobManuallyClosed(job)` checks — the single source of truth, read by `job-requests-panel.tsx`, `notifications.ts`, and `offers.ts#getOperationServiceCardStatus` (checked *before* any offer-derived branch there, since closing also flips still-`pending` siblings to `rejected` and would otherwise mislabel as "Başka Hizmet Verenle Anlaşıldı").

- `offers.ts#closeJobListing(session, jobId, reason)` is the sole write path — requester-owner only, rejects if already closed (`JOB_ALREADY_CLOSED_MESSAGE`) or if `job-requests.ts#isJobClosedToNewOffers` is already true (`JOB_CLOSURE_BLOCKED_MESSAGE`) — i.e. closing is only possible before any offer starts real work, the same `accepted`-but-not-yet-`in_progress` threshold used everywhere else in this doc. Any still-`pending` sibling offers are then flipped to `rejected`, best-effort (logged, not surfaced, on failure — same pattern as `deleteJobWithOffers`'s sibling-offer write).
- Rejected provider(s) get a notification whose message varies by reason (`job-closure.ts#getJobClosureNotificationMessage`) — `notifications.ts` distinguishes this from an ordinary `teklif_reddedildi`/`hizmet_kalemi_kaldirildi` by checking `job.closedAt` (a third branch alongside "job still exists" vs. "job was deleted," see "Notifications are derived, not stored" below).
- `job-requests-panel.tsx` gets a dedicated "Kapatılan İlanlar" tab (mutually exclusive with "Süresi Dolan İlanlar" — `isJobManuallyClosed` is checked first, so a closed-*and*-expired job always shows as closed) with its own "İlanı Kapat" confirmation dialog (reason picker, same modal shell as `DeleteJobDialog`) — a real, distinct terminal state, not a substitute for deletion, and independent of/checked earlier than `offers.ts#deleteJobWithOffers`.

### İlan Moderasyonu (admin job moderation)

Migration `0035_job_moderation.sql` (uncommitted) adds a second, deliberately orthogonal status axis to `Job` — **never conflate it with the existing operational `Job.status`/`listing_status`** (`yayinda`/`tamamlandi`/`iptal`, untouched, still never changes after creation) or with manual closure (`job-closure.ts#closeJobListing`/`Job.closedAt`, also untouched, still backed by the separate `close_job_as_admin` RPC). `types.ts#JobModerationStatus` (`"pending_review" | "approved" | "rejected"`) plus `Job.moderationStatus?`/`moderationReviewedAt?`/`moderationReviewedBy?`/`moderationRejectionReason?` answer one question only: "should this listing be visible to a Hizmet Veren at all." `undefined`/missing (every job created before this feature, including every static `jobs.ts` seed listing) reads as `"approved"` via `job-moderation.ts#getJobModerationStatus` — no retroactive hiding of old data. A brand-new job (`create_job`/`create_operation_with_jobs`, `0035`'s reissue of both with unchanged external signatures) always starts `"pending_review"` instead.

- **Enforcement today is client-side**, since `provider-job-listing.tsx`/`job-detail-content.tsx`/`offers.ts#createOffer` still read only localStorage — `job-moderation.ts#isJobVisibleForModeration`/`filterModerationVisibleJobs` is the real gate in the running app. On the Supabase side (dual-write only, same caveat as the rest of this doc), `jobs_select_visible` RLS now also hides a non-`approved` job from everyone except its own requester or an admin, and `create_offer` gained a matching `moderation_status <> 'approved'` guard (reusing the existing `MLK60`, not a new code) — both independent of, and AND'ed with, the pre-existing category-authorization check from "Service Authorization" above.
- **Three admin-only `SECURITY DEFINER` RPCs**, `is_admin()`-gated (`ML115`–`ML118`), all supporting optimistic-concurrency via an optional `p_expected_updated_at` (mismatch → `ML118`): `approve_job_as_admin(p_job_id, p_expected_updated_at?)`, `reject_job_as_admin(p_job_id, p_reason, p_expected_updated_at?)` (reason required, `ML117`), and `update_job_as_admin(...)` — a full content-edit RPC covering title/description/`operation_details`/location (province/district/type/address/neighborhood/URL/directions note)/dates/product-info (when in scope)/`customs_product_type`/delivery fields (when Nakliye), but never category/photos/customs-documents/`facility_id`/`location_mode`. `admin_job_list` (`0017`) is dropped and recreated (not `create or replace`d) so `j.*` finally picks up moderation plus the older `0028`/`0031` product/delivery columns it had never returned.
- **`0037_job_admin_edit_field_parity.sql` is a same-day bugfix, not a new capability**: `admin-job-edit-form.tsx` originally gated its Tonaj field on `isTonnageRequired` (Nakliye-only) instead of `requiresProductInfo` (Nakliye ∪ Liman Hizmetleri), so editing a Liman job as admin silently sent `undefined` for tonnage — and `update_job_as_admin`'s unconditional `product_tonnage = p_product_tonnage` assignment then nulled a real value on save. Fixed in both places: the form's visibility condition, and the RPC itself now wraps every optional/category-conditional column in `coalesce(p_x, x)` so a future form gap can only skip a field, never zero it — the stale 16-param `update_job_as_admin` overload is `drop function`'d outright, following the exact same discipline `0032`–`0034` established for `create_job`/`create_operation_with_jobs` (see "Future Supabase/PostgreSQL design" below).
- **`admin-jobs.ts#approveJobAsAdmin`/`rejectJobAsAdmin`/`updateJobAsAdmin`** call the Supabase RPC first, and only on success best-effort-patch a matching localStorage job (`job-store.ts#applyAdminModerationDecision`/`applyAdminJobEdit`) if that job id happens to exist in this browser's local table — a no-op mirror write, same "best-effort, doesn't gate on it" shape as `supabase-job-sync.ts`'s reverse direction (see "Supabase Geçişi Faz 2–4" below), not the authorization boundary.
- **Editing a previously-decided listing can re-trigger review.** `job-store.ts#updateJob` (the requester's own edit path — never `admin-job-edit-form.tsx`'s) calls `job-moderation.ts#didCriticalJobContentChange(before, after)` right after applying the edit: if the job was already `approved`/`rejected` and any "critical" field changed (category, title, description, full location, dates, product-info, Gümrük Müşavirliği fields — **not** the 7 Geri Dönüşüm fields above, nor photos/`operationDetails`, which are deliberately excluded from the critical-key list), `moderationStatus` is reset to `"pending_review"` (`moderationReviewedAt`/`moderationReviewedBy` cleared), dropping the listing back out of Hizmet Veren visibility until an admin re-decides it. A job already `"pending_review"` is left untouched by this check. The admin's own edit (`update_job_as_admin`) never calls this function — the admin is already making the review decision as part of that same action.

### Provider job listing ("Aktif İlanlar")

The Hizmet Veren's `/ilanlar` screen is `provider-job-listing.tsx` (swapped in by `job-listing-screen.tsx` when `session.role === "hizmet-veren"`; the guest and Hizmet Alan branches render the separate, unchanged `JobList`/`JobCard`). It's a single unified "Aktif İlanlar" list, not split into "Teklife Açık"/"Teklife Kapalı" sections — `getJobAvailabilityForProvider`'s per-row `open`/`closedReason` (see "New offers vs. accepted vs. work started" above) still surfaces as a small note under "İlanı İncele" (`getProviderClosedReasonLabel`, e.g. "İş Devam Ediyor" once a job's settled offer passes `accepted`), it just never gates which section a row appears in. There is also no favorites feature and no listing-badge system (Yeni/Acil/Teklif Bekliyor/etc.) — both were removed after shipping; `StatusBadge` itself is untouched and still used by other screens (offer/job status chips), only this screen's own badge computation (`job-listing-badges.ts`) was deleted. There is also no page heading, description paragraph, or "Son Görüntülenenler" strip anymore (removed 2026-07-26) — the compact filter toolbar is the first thing rendered, deliberately so the job list itself (not the filter chrome) is what a Hizmet Veren sees first; `recently-viewed-jobs.ts`/`use-recently-viewed-jobs.ts` still exist and `recordJobViewed` is still called from `handleJobClick` (so click history keeps accumulating), but nothing currently reads it back — an intentional "keep tracking, drop the display" choice in case the strip returns later. The feature is layered so the same per-job computation feeds both responsive render paths:

- `job-listing-row.ts#buildJobListingRows(jobs, offers, providerId)` is the single row-data builder (thumbnail photo, visible offer count, `getJobAvailabilityForProvider` availability, `job-location.ts#getJobLocationSummary` location) — computed once per job, consumed identically by `job-listing-table.tsx` (desktop, a real `<table>`, 7 columns: Fotoğraf/Hizmet Türü/İlan Başlığı/Firma-Bölge-Konum/İş Tarihi/Teklif Sayısı/İşlem) and `job-listing-cards.tsx` (mobile card list). Neither ever renders `Job.addressText` — see "Centralized job location" below for why.
- **Only one of the two trees is ever mounted**, chosen via `use-media-query.ts#useMediaQuery("(min-width: 1024px)")` rather than rendering both and toggling visibility with CSS — that CSS-both-trees approach was tried first and reverted after it broke an existing regression test (duplicate `<a>` elements sharing the same job title, a Playwright strict-mode violation) and silently doubled `useJobPhotoUrl`/IndexedDB work per row. A `matchMedia`-driven client read is safe here specifically because this whole subtree only ever mounts after `useSession()` resolves to a real session (`getServerSnapshot` is always `null`), so there is no SSR-hydrated markup for it to mismatch against.
- **The filter toolbar is deliberately thin and backgrounded, not the page's focal point** (`job-listing-filter-bar.tsx`) — there is no search box at all anymore (removed 2026-07-26, along with `job-listing-search-bar.tsx` and `job-listing-filters.ts#matchesJobSearch`, both deleted rather than left unused): six equal-width fields (Hizmet Türü, İl, İlçe, Bölge/Tesis, Tarih, Teklif Durumu) in one `grid-cols-1 sm:grid-cols-3 lg:grid-cols-6` grid — mobile stacks one per row, tablet wraps into two tidy rows of three, desktop is a single row. Tarih/Teklif Durumu are `SearchableSelect`s too (not the old pill-style `SegmentedControl`, removed) purely so every field shares the same height/padding as the others — their options (`DATE_BUCKET_OPTIONS`/`OFFER_STATUS_FILTER_OPTIONS`) already include their own "Tümü" entry, so unlike Hizmet Türü/İlçe/Bölge-Tesis they are *not* prefixed with the synthetic `CLEAR_OPTION`. Every `SearchableSelect` here passes `compact` (a new opt-in prop, default `false`) for tighter padding/smaller icon/quieter label — job-request-form.tsx/job-edit-form.tsx/login-form.tsx don't pass it, so their `SearchableSelect`s look exactly as before. The toolbar card itself uses a smaller `rounded-[10px]` (not the app-wide `rounded-card`, which is a much softer 0.875rem used for actual content cards) plus `shadow-sm`, matching the "recede into the background" intent. `job-listing-screen.tsx`'s hizmet-veren branch uses `pt-4` (not the site-wide `py-16` page-padding convention every other section keeps) so the toolbar sits right under the sticky header.
- **`SearchableSelect`'s dropdown popup can now open wider than its trigger** (`searchable-select.tsx`, `min-w-full` + content-fit `w-max` capped at `max-w-[min(320px,90vw)]`, option-label `truncate` removed) — this was a real bug, not scoped to this screen: cramming this toolbar's fields into narrow grid tracks made the popup inherit that narrow width too, so long option labels (e.g. "Konteyner Boşaltım", "Tehlikeli Madde Depolama") got cut off mid-word inside the *open list*. Fixed at the shared-component level since a narrow trigger with a wide popup is a universally-safe pattern (every other `SearchableSelect` caller already has a full-width field, so this is a no-op improvement there). The closed trigger's own selected-value text still truncates by design — that's a deliberate, compact-toolbar trade-off, not the bug that was reported.
- **Filters are deliberately grounded in the same central catalogs the job-creation form uses (`service-catalog.ts`, `turkey-locations.ts`), not in ad-hoc string comparisons on live job data** — `job-listing-filters.ts` is the single source of truth for both option-building and row-matching, so the two can never drift apart:
  - **Hizmet Türü**: options come from the *entire* `SERVICE_CATEGORY_GROUPS` catalog (`buildCategoryOptions`, no `jobs` param) — unlike the other filters below, a category with zero current listings still appears, and a removed/legacy category (`jobs.ts#SERVICE_CATEGORIES` raw text) never appears as an option. Matching (`matchesJobCategory`) goes through `resolveLegacyJobCategoryToId(job.category)` so both new (id-based) and legacy (raw Turkish text) jobs resolve to the same catalog id; a legacy value with no mapping (e.g. "Depolama", deliberately unmapped — see `service-catalog.ts`) simply never matches a specific category selection, same known limitation as everywhere else that function is used.
  - **İl**: Türkiye geneli serbestçe seçilebilir (a real `SearchableSelect`, fed by `buildProvinceOptions(activeJobs)`) — `FIXED_PROVINCE_LABEL`/`FIXED_PROVINCE_KEY` (`"Kocaeli"`/`slugifyTurkish("Kocaeli")`) now name only `DEFAULT_JOB_LISTING_FILTERS.province`'s starting value, not a lock; `matchesProvinceFilter`/`resolveJobProvinceKey` were never changed (they were always province-agnostic — the earlier Kocaeli-only restriction lived entirely in the UI, not the filter logic). Changing İl clears both İlçe and Liman/Sanayi/OSB (`job-listing-filter-bar.tsx#handleProvinceChange`) — a district/facility from the old province can never survive a province change.
  - **İlçe**: **not** job-data-scoped (fixed 2026-07-26 — it used to only list districts that already had at least one active job in them, so most of a province's official districts silently never appeared) — options come from `turkey-locations.ts#getDistrictsByProvinceCode` (`app/_data/turkey/districts.json`, the same static reference data `getProvinces()`/district pickers elsewhere already read), resolved from the province *slug* via `turkey-locations.ts#getProvinceCodeBySlug`. `buildDistrictOptions(selectedProvinceKey)` therefore always lists all of the selected province's official districts regardless of live job data, matching how Liman/Sanayi/OSB already worked. Row-matching is unchanged — still `matchesDistrictFilter` → `resolveJobDistrictKey`/`getDistrictId`.
  - **Liman / Sanayi / OSB**: the one filter that is *not* job-data-scoped — options come straight from `turkey-locations.ts#getFacilitiesByProvinceAndDistrict(provinceKey, districtKey)` (a same-array, no-type-filter sibling of `getFacilitiesByProvinceDistrictAndType`), so it can show a facility (e.g. Beldeport/Yılport Gebze in Kocaeli/Dilovası) even if no job currently references it by name, and conversely a facility whose real verified district differs from what a job's free-text `workLocationType` implies (e.g. "Safiport Derince Limanı" is `districtId: "derince"` and `active: false` in `data/locations/locations.json`, not Dilovası) correctly never shows there. Disabled until a district is selected. Row-matching (`matchesFacilityFilter` → `job-location.ts#resolveJobFacility`) prefers `Job.facilityId` (set when the job-creation form's Liman/Sanayi/OSB picker resolved to a real catalog facility) and falls back to a name/alias fold-match on `Job.workLocationType` *within that job's own province+district* for jobs created before `facilityId` existed; jobs whose `workLocationType` is a generic description ("Fabrika", "Kapalı Depo") rather than a specific catalog facility name never match a specific facility filter — a known, accepted limitation, not a bug.
- `recently-viewed-jobs.ts`/`use-recently-viewed-jobs.ts` is a per-user localStorage store following the exact `notification-reads.ts` convention (module cache, `Set<() => void>` listeners, stable `getServerSnapshot`). "Recently viewed" is recorded via the row's own link `onClick` inside the listing screen — deliberately not by instrumenting `job-detail-content.tsx`, which stays untouched.
- Rows belonging to the same `Çoklu Hizmet Operasyonu` operation are collapsed into one row/card here — see "Operation discovery & status UI" under that section above for `groupJobListingRowsByOperation` and the filter-visibility-vs-content distinction.

### Centralized job location

`app/_lib/job-location.ts` is the single module every screen goes through for anything derived from a `Job`'s location fields — no component hand-concatenates `province`/`district`/`workLocationType` itself:

- `toFacilitySelectOptions(facilities)` builds the `SearchableSelect` options for the merged **"Bölge / Tesis"** picker used identically by `job-request-form.tsx` (both the single-service form and each service card of the multi-service flow — see "Çoklu Hizmet Operasyonu" above) and `job-edit-form.tsx` (İl → İlçe → Bölge/Tesis → [custom-facility fields, only in custom mode] → Açık Adres, in that order). There is deliberately no separate "İşin Yapılacağı Yer Türü" step anymore (removed 2026-07-25) — the picker lists every facility type together for the chosen il+ilçe via `turkey-locations.ts#getFacilitiesByProvinceAndDistrict`, the exact same source and il/ilçe keying the Aktif İlanlar Bölge/Tesis filter already used, so the two screens can never drift onto different facility lists again. Each option's facility type shows as a `hint` (e.g. "Beldeport" hinting "Liman"), and a synthetic `FACILITY_FREE_TEXT_VALUE` option — labelled **"Listede yok — tesis bilgilerini kendim gireceğim"** (`CUSTOM_FACILITY_OPTION_LABEL`) — switches the form to custom-facility mode (below) instead of blocking creation when a job's real location isn't in the catalog. A "Hazır listeden seçmek için tıklayın." button lets the user go back to the `SearchableSelect` from custom mode (`handleFacilityChange("")`) — every caller needs this same round-trip, it's not automatic.
- **`companyOrFactoryName` ("Firma / Fabrika Adı") is legacy-only** — removed from both the job-creation and job-edit forms entirely as part of MALSEVK's Kocaeli-only simplification; no current screen collects it. `job-store.ts#resolveLocationFields` deliberately omits it from its return value, and since `updateJob` applies that result as `{...existing, ...resolveLocationFields(input)}`, a pre-existing job's value (if any) survives untouched across later edits even though the field is never shown again — no data loss, just a retired input. `job-location.ts#getJobLocationSummary` still reads `job.companyOrFactoryName` for display, purely for back-compat with jobs created before the field was removed.
- **Custom facility mode** (`Job.locationMode: "custom"`, Hizmet Alan-only, job create/edit only — never reachable by `hizmet-veren` or guests, gated the same way the rest of the form is): picking the free-text option shows a single **"Tesis / İşletme Adı"** field that reuses `workLocationType` itself, plus three new optional fields with no catalog/free-text equivalent: `neighborhood` ("Bölge / Mahalle"), `locationUrl` ("Konum Bağlantısı", validated as `http(s)://...` if non-empty), `directionsNote` ("Adres Tarifi"). `facilityId` is force-cleared to `undefined` in this mode by `job-store.ts#resolveLocationFields` regardless of what the caller passes — a custom entry can never carry a fabricated `Facility.id`. There is intentionally **no pre-built catalog of individual factories/companies** to choose from instead (a `FABRIKA` catalog of ~10 flagship Kocaeli plants was built via `location_manual_overrides.json` and then fully reverted 2026-07-25) — MALSEVK's location catalog is scoped to shared/official location types only (Liman, OSB, Serbest Bölge, Lojistik Merkezi); a single company's own site is always entered ad hoc, never pre-cataloged.
- `resolveJobFacility(job)` resolves a `Job` back to a `Facility` — returns `null` immediately for `locationMode: "custom"` jobs (skips catalog matching entirely, so a custom-typed name can never accidentally resolve to a real facility by coincidental name/alias match), otherwise prefers `job.facilityId` (validated against the job's own province+district) and falls back to a name/alias fold-match on `job.workLocationType` for jobs created before `facilityId` existed. This is the *only* copy of that resolution logic; `job-listing-filters.ts#matchesFacilityFilter` imports it rather than keeping its own.
- `getJobLocationSummary(job)` / `formatJobLocationLine(job)` are what every rendering surface calls instead of inline string-building: `job-card.tsx`, `job-requests-panel.tsx`, `incoming-offer-card.tsx`, `my-offers-panel.tsx`, and `job-listing-row.ts` (table/cards) all use one of these two — none of them needed to change for custom mode, since `facilityDisplayName` already falls back to `workLocationType` verbatim when `resolveJobFacility` returns `null`. `formatJobLocationLine` deliberately omits `companyOrFactoryName` (e.g. `"İMES OSB • Dilovası / Kocaeli"`) — callers that also want the company name (the listing table/cards, the job detail page) read it separately off the summary and render it as its own line.
- **`Job.addressText`/`neighborhood`/`locationUrl`/`directionsNote` are never included in either helper** — all four share one gate, `job-requests.ts#canViewJobAddress(session, job, offers)` (same `ENGAGED_OFFER_STATUSES` timing as `contact-access.ts`'s phone/email reveal, plus an "owner always sees their own" exception). `job-detail-content.tsx` is the only screen that renders them, conditionally, right after Firma/Fabrika Adı → Bölge/Tesis → İlçe/İl; `job-requests-panel.tsx` renders them unconditionally instead of calling the gate, because that panel is already structurally filtered to `job.requesterId === session.id` (the one case the gate always allows anyway). No other screen (listing table/cards, offer cards, job-card.tsx) ever reads them.
- Legacy back-compat: `job-store.ts#normalizeStoredJob` coerces `facilityId`/`companyOrFactoryName`/`addressText`/`locationMode`/`neighborhood`/`locationUrl`/`directionsNote` to `undefined` unless the stored value is actually a string (same pattern already used for `photos`; `locationMode` additionally requires the value to be exactly `"catalog"` or `"custom"`), so pre-existing localStorage jobs and the static `jobs.ts` seed listings (which have none of these fields) render their fallback (`workLocationType` verbatim, no company line, no address line) rather than crashing or showing `"undefined"`. A missing `locationMode` is always treated as catalog mode — editing such a job requires filling in Açık Adres before saving (catalog-mode rules); it no longer also requires Firma/Fabrika Adı now that that field has been removed from the forms entirely (see above).

### Notifications are derived, not stored

`notifications.ts` computes each user's `AppNotification[]` live from the current `Offer.status` (plus `Offer.createdAt`) every render — there is no separate notifications/events table. This is why notification ids stay stable (derived from `offer.id` + event name) and why read/dismissed state (tracked separately, see below) keeps matching up correctly across sessions. One consequence to keep in mind when adding a new offer-status transition: notifications tied to a **transient** status (e.g. `accepted`, `completion_requested`) disappear on their own the moment the offer moves past that status, because the derivation only ever looks at the offer's *current* status; only notifications tied to a **terminal** status (`completed`, `rejected`, `cancelled`, `agreement_failed`, `withdrawn`) persist. Requesters and providers each get their own distinct set of `NotificationType`s derived from the same offers — never the other role's. Almost every notification filters `myOffers` by the viewer's *own* `offer.status`; the one exception is `baska_hizmet_verenle_anlasildi` ("Başka Bir Hizmet Verenle Anlaşıldı"), which instead filters the viewer's still-`pending` offers by whether a *sibling* offer on the same job has closed them (`getProviderOfferFilter(offer, offers) === "kapanan-teklifler"`) and reads that sibling's `updatedAt` for `createdAt` — see "Provider offer tabs" above. Read state (`notification-reads.ts`) and dismissed state (`notification-dismissals.ts`) are two separate per-user localStorage keys (`malsevk_read_notifications_<userId>` / equivalent for dismissals) — intentionally not cleared by `clearSession()`, so logging out and back in doesn't reset what a user has already seen or dismissed.

**Deleting a job doesn't delete its offers.** `offers.ts#deleteJobWithOffers` (the authorized entry point for job deletion — never call `job-store.ts#deleteJob` directly) used to delete every associated `Offer` along with the job; now any still-`pending` offer on that job is instead flipped to `rejected` (an existing status, not a new one) and kept, while already-terminal offers (rejected/withdrawn/agreement_failed/cancelled/completed) are left untouched. Reason: since notifications are derived live from `Offer`s (above) rather than stored, a fully-deleted offer could never produce a notification again — but the provider still needs to learn their job disappeared, distinctly from an ordinary reject. `notifications.ts` tells the two `rejected` causes apart by checking whether `offer.jobId` still resolves in the live `jobs` array: found → the existing `teklif_reddedildi`; not found → a new `NotificationType`, `hizmet_kalemi_kaldirildi` ("Hizmet Talebi Kaldırıldı" / "İlan sahibi ilgili hizmet talebini yayından kaldırdı.") — the same "disambiguate by an extra condition, not a new status" pattern `siblingClosedNotifications` already uses for `pending`.

### Turkey location data

`app/_lib/turkey-locations.ts` reads facility data (OSB/port/free-zone/depot/etc.) from a **single source**, `data/locations/locations.json` — there is no separate hand-curated facility list anywhere else in the repo. That one file mixes three kinds of records under a shared schema (`id, name, type, provinceId, districtId, aliases, active`):

1. **Pipeline-verified entries** — provinces run through `locations:import:<province>` + `locations:verify` (currently only Kocaeli, `provinceCode: "41"`), scored via real Overpass/OSM polygon verification (`scripts/locations/lib/score.mjs`: ≥80 auto-`VERIFIED`/active, 60–79 `REVIEW`/inactive, duplicates within ~walking distance merged by `lib/dedup.mjs` into one canonical record with the losers' names folded into `aliases`).
2. **Migrated legacy entries** — every other province, carried over verbatim by the one-off `scripts/locations/migrate-legacy.mjs` from the old hand-curated `app/_data/turkey/{osb,ports,free-zones,logistics-centers}.json` files (which no longer exist in the repo; that script deleted them after migrating). These are `active: true` for display purposes but have **not** been through real polygon verification — don't assume "present in `locations.json`" implies "geo-verified" for any province other than Kocaeli.
3. **Manually overridden/added entries** — `data/locations/location_manual_overrides.json`, applied by `verify.mjs` on every run, *after* step 1's pipeline scoring and scoped by `provinceId`. Three actions, keyed by `matchId` (an existing record's `id`) except `add`: `"promote"` overwrites a matched record's `name`/`district(Id)`/`officialWebsite`/`phone`/`fullAddress`/`active`/`confidenceScore`/aliases (merged, never replaced) and always sets `verificationStatus: "VERIFIED"` regardless of the `active` value passed — this is the mechanism for both activating a real facility the auto-scorer under-scored *and* deliberately deactivating one it over-scored (e.g. a shipyard wrongly caught by a "liman" keyword match); `"reject"` sets `active: false` + `verificationStatus: "REJECTED"` on a matched record, used for confirmed OSM duplicates or wrong-province/wrong-category records, never for a record that's merely a different *kind* of facility (that's a `"promote"` with `active: false` instead, so the reason is auditable and distinct from "this candidate doesn't physically exist"); `"add"` inserts a brand-new record (its own `id`, prefixed `loc-manual-`) for a real facility the Overpass query never surfaced as a candidate at all — this has turned out to be the norm rather than the exception for whole categories (OSB, free zones, TCDD logistics centers, flagship industrial plants), since OSM/Overpass frequently has no usable polygon for large single-owner sites. Every override's `manualSource` field is a required, freeform citation of the real-world source (official site, ministry registry, chamber-of-commerce listing) — treat it as mandatory, not optional, when adding a new override. The whole file is designed to survive a full pipeline re-run: `verify.mjs` is idempotent, so re-running `locations:verify` after an Overpass re-fetch re-applies every override deterministically rather than needing to be redone by hand.

Only `active === true` records are ever shown; a `districtId: null` record (district unresolved) deliberately never matches any district filter rather than risk being shown under the wrong one. Lookups are always **province + district + facility type together** via `getFacilitiesByProvinceDistrictAndType(provinceId, districtId, type)` — there's no province-only or name-only lookup. `app/_data/turkey/{provinces,districts}.json` (81/81 provinces) are the unrelated, complete, static province/district reference and are not part of this pipeline.

### Site-wide access gate

`proxy.ts` (project root — Next.js 16 renamed `middleware.ts` to `proxy.ts`; per AGENTS.md, check `node_modules/next/dist/docs/` before assuming training-data conventions here) sits in front of every route (pages *and* `/api/*`) and redirects (pages) or 401s (`/api/*`) any request without a valid `malsevk_site_access` cookie — a temporary, site-wide password wall for the Vercel Hobby deployment being publicly reachable, **unrelated to and running before** the app's own `users.ts`/`session.ts` login system covered above. Active only when `NODE_ENV === "production"` (same convention as `demo-veri-sifirla`/`demo-accounts-panel.tsx`) — `npm run dev` is never gated, but `npm run build && npm start` now behaves like Vercel and needs `MALSEVK_SITE_PASSWORD` set locally too. `app/_lib/site-access.ts#computeSiteAccessToken` derives a one-way SHA-256 token from the password for the cookie value (never the plaintext password itself), validated with a timing-safe comparison; if `MALSEVK_SITE_PASSWORD` is unset, no token can ever validate — fail-closed by construction, not a separate check. `app/site-erisim/` (the only route besides `_next`/`favicon.ico` excluded from the gate) is a Server Action (`verifySiteAccess`) with a fixed ~1s delay plus a best-effort per-IP in-memory counter against brute-forcing; also handles logout (`clearSiteAccess`, clears the cookie).

As of the Supabase Auth migration (see that section below), `proxy.ts` does two more things *after* this gate, on every request regardless of environment: it calls `app/_lib/supabase/middleware-client.ts#updateSupabaseSession` to silently refresh the Supabase access-token cookie (so a long-open tab doesn't quietly appear "logged out" once the token expires), and it redirects to `/giris-yap?redirect=...` (or 401s under `/api/*`) any request to a `PROTECTED_ROUTE_PREFIXES` path (`/panel`, `/hizmet-talebi-olustur`, `/admin`, `/kayit-tamamla`) that has no real Supabase user — this second check is unconditional (not `NODE_ENV`-gated like the site-password wall above it). These two concerns are independent and stack: the site-password wall can be on or off per environment, Supabase session refresh + route protection always run.

### Legal documents (Gizlilik Politikası / Kullanım Koşulları / KVKK)

`app/_lib/legal-documents.ts` is the single central registry for the three legal texts — each `LegalDocumentMeta` (`id`, `title`, `version`, `effectiveDate`, `lastUpdatedDate`, `routePath`, `sections`) is the **only** place version/date metadata is defined; nothing else (modal, standalone page, footer) hardcodes a version or date. Publishing a new revision means editing only this registry (bump `version`/`lastUpdatedDate`) plus the corresponding content file — no other file changes. The actual legal text is data, not JSX: `legal-content-privacy.ts`/`legal-content-terms.ts`/`legal-content-kvkk.ts` each export a `LegalDocumentSection[]` (`heading`, `paragraphs`, optional `list`), kept as three separate documents deliberately — Gizlilik Politikası is the broad, general-audience policy while KVKK Aydınlatma Metni follows the narrower, mandatory Article-10 disclosure format; they overlap in subject matter but not in legal function. Bracketed placeholders (`[Şirket Unvanı]`, `[Şirket Adresi]`, `[MERSİS Numarası]`, `[Başvuru E-posta Adresi]`) mark the corporate-identity fields not present anywhere in the codebase — must be filled in (and the text given a final legal review) before this goes to production.
- **Rendering**: `legal-document-content.tsx` (title + Sürüm/Yürürlük Tarihi/Son Güncelleme meta row + sections) is the single content renderer, used identically by `legal-document-modal.tsx` and by each of the three standalone pages (`app/gizlilik-politikasi`, `app/kullanim-kosullari`, `app/kvkk-aydinlatma-metni`) — text is never duplicated between the modal and page paths.
- **Footer links are plain, real navigation — no modal interception.** `site-footer.tsx`'s "Yasal" links (and its "Bize Ulaşın" link, see below) are ordinary `<Link href={...}>`s with no `onClick`/`preventDefault` at all; a click always navigates to the real standalone page. This is a deliberate reversal of an earlier iteration where a plain left click opened `legal-document-modal.tsx` instead of navigating — that was explicitly undone (task requirement: none of these pages may ever open as a modal/dialog/drawer/pop-up, from any entry point). `legal-document-modal.tsx`/`DialogShell` themselves were **not deleted** — they're still the registration form's consent UI (see below) — only `site-footer.tsx`'s *usage* of them was removed.
- **`LegalDocumentModal`'s `mode: "readonly" | "consent"` prop now has exactly one real caller: `login-form.tsx` with `mode="consent"`** (the registration form's "Okudum, anladım." checkbox-gates-"Onaylıyorum" flow, unrelated to footer navigation and explicitly out of scope for the footer-link change above — it was never touched). `mode="readonly"` (no accept UI, just "Kapat") still exists in the component for API completeness but nothing in the app currently invokes it that way.
- **Every standalone legal page (and `app/bize-ulasin/page.tsx`, see below) shares one "Kapat" control: `close-page-button.tsx#ClosePageButton`.** These are real routes, not dialogs, so "closing" means real client-side navigation, not an `onClose()` callback: `router.back()` if the current *tab* has performed at least one in-app navigation, else `router.push("/")`. This deliberately does **not** check `window.history.length` — that count is unreliable (verified via Playwright: a freshly opened tab can already report `length === 2` because of a phantom `about:blank`-style entry, which would send `router.back()` to a broken blank page instead of home). Instead, `app/_lib/use-track-in-app-navigation.ts#useTrackInAppNavigation` — mounted once, globally, by the invisible (`return null`) `navigation-history-tracker.tsx#NavigationHistoryTracker` in `app/layout.tsx` (same "single-purpose root component" pattern as `whatsapp-support-button.tsx`) — watches `usePathname()` and writes a per-tab `sessionStorage` flag the *first time* (and every time after) the pathname actually changes post-mount (skips the initial render, same `isFirstRun` guard idiom as `use-focus-on-become-true.ts`). `ClosePageButton` reads that flag via `hasNavigatedInApp()`. Net effect: a tab that loaded straight into `/gizlilik-politikasi` (typed URL, email link, new tab) never sets the flag → Kapat goes home; a tab that visited any other in-app route first → Kapat genuinely goes back to wherever that was, not just "home." Never calls `window.close()` — it only ever navigates within the app.
- **Consent recording is Supabase-shaped but localStorage-backed today**: `legal-consent.ts#LegalConsentRecord` (`documentId`, `version`, `acceptedAt`, `userId`, `ipAddress`, `deviceInfo`) is designed to map 1:1 onto a future `legal_consents` table — `ipAddress`/`deviceInfo` are permanent `null` placeholders (no server-side request context exists to fill them; see "No real backend"), and `userId` is `null` for anonymous acceptance or the real `StoredUser.id` when known. `recordLegalConsent(documentId, userId?)` always reads the **current** version from the registry (never passed in by the caller), so a version bump automatically applies to the next consent without any call-site change. In `mode: "consent"`, checking "Okudum, anladım." is required before "Onaylıyorum" enables; only "Onaylıyorum" records a consent — ESC/backdrop-click/"Kapat" all close without writing anything, identically in both modes.
- **Registration form uses one combined checkbox, not three**: `login-form.tsx` replaced the old separate `kvkkAccepted`/`termsAccepted` checkboxes with a single `legalConsentAccepted` field (`register-form-validation.ts`'s `RegisterFormErrors.legalConsent` is now the one error key) — the sentence embeds all three document titles as independent `LegalDocumentInlineTrigger` buttons, each opening its own modal via `event.stopPropagation()` (critical: without it, a click on the nested button would bubble to the wrapping `<label>` and toggle the outer checkbox, since the button lives *inside* that label). Checking the outer box directly (without ever opening a modal) is sufficient to register — the modals are for optional reading, not a forced gate. On successful `registerUser`, `recordConsentForAllLegalDocuments(result.user.id)` writes three independent consent records (one per document, same timestamp) from that single checkbox action, satisfying "each text can have its own consent record" without requiring three separate UI confirmations at signup.

### Bize Ulaşın (contact form), the second `/admin` module, and site-wide support entry points

A public contact form, a second independent admin module to triage it, and an always-on WhatsApp button — three small additions that don't belong to any existing feature's data model, added together.

- **`contact-messages.ts` moved off localStorage onto Supabase (uncommitted) — a full cutover, not dual-write.** It originally was a from-scratch localStorage "table" (`malsevk.contact-messages.v1`, `contactMessagesStore`/`use-contact-messages.ts`, following the `job-store.ts`/`offers.ts` module-cache-plus-`useSyncExternalStore` pattern); that entire read/write implementation, the `StoredContactMessage` type, and `use-contact-messages.ts` itself are now deleted (the rewritten module's own header comment says explicitly this is not hybrid/dual-write). What's left in `contact-messages.ts` is pure catalog/labels (`ContactMessageStatus`, `CONTACT_MESSAGE_STATUS_OPTIONS`, `CONTACT_MESSAGE_SUBJECT_OPTIONS`, `getContactMessageSubjectLabel`/`getContactMessageStatusLabel`, min/max length constants). The real read/write now lives in **`app/_lib/supabase-contact-messages.ts`**, against the already-committed `contact_messages` table (migration `0021`, itself unchanged — the app just started calling into it for real): `submitContactMessageRemote(input)` calls RPC `submit_contact_message` (still enforces "email or phone required," `MLK98`, and still works for a fully anonymous/guest submitter — `user_id`/`user_role` are derived server-side from `auth.uid()`, never client-supplied); `listAllContactMessagesForAdminRemote()` is a plain admin-RLS-scoped `select` on `contact_messages`; `reviewContactMessageRemote(id, status, adminNote?)` wraps RPC `review_contact_message`. Reference numbers (`"BU-<year>-<seq>"`) are now generated inside the RPC rather than client-side — the original display-only/no-collision rationale for `referenceNumber` still holds, just server-side now.
- **No separate notification/read-state table** — same principle as "Notifications are derived, not stored" above: the admin nav's unread badge (`AdminNav`'s `newContactMessageCount` prop) is just a live count of `status === "yeni"` rows, so it self-corrects the moment an admin changes a message's status; there is nothing to mark "read" separately.
- **`reviewContactMessageRemote`** is the sole write path for status changes/admin notes now — authorization is enforced by the `review_contact_message` RPC itself (`is_admin()`, not a client-side session check), and "archiving" is still not a separate function, just the same call with `status: "arsivlendi"` — a message is never deleted.
- **`contact-section.tsx`** ("Bize Ulaşın") is the shared form, rendered by exactly one route, `app/bize-ulasin/page.tsx` — a real standalone page, not a modal/dialog/drawer (this flip-flopped during development: page → modal → page again; the modal version and its `contact-modal.tsx` wrapper were fully deleted, `ContactModal` no longer exists anywhere). `site-footer.tsx`'s "Bize Ulaşın" link is a plain `<Link href="/bize-ulasin">`, same as every other footer link — no `onClick` interception, no state. `ContactSection` itself has no page-level wrapper of its own (no `<section>`/max-width/padding, no border) — it starts directly with an `<h1 id="bize-ulasin-baslik">Bize Ulaşın</h1>` (the page's one and only top-level heading; there is no separate "MALSEVK.com" heading anywhere above it) and lets the page own the layout chrome, following the exact same pattern `app/gizlilik-politikasi/page.tsx` (and its two siblings) already use: page provides `mx-auto max-w-3xl px-4 py-16 ...` plus one `rounded-card border border-border bg-surface p-6 sm:p-8` box, content renders plainly inside it. The page also renders `close-page-button.tsx#ClosePageButton` (see above) top-right, identically to the legal pages. A logged-in user gets name/email/phone pre-filled from `users.ts#findUserById(session.id)` (editable before submit); a guest sees empty fields. Includes a CSS-hidden honeypot field (silently drops the submission before ever calling `submitContactMessageRemote`, no error shown, if filled) and a client-only 15s button-disable cooldown after a successful send — real server-side throttling now exists too (the RPC layer), but these client-side checks are unchanged. Header/mobile menu/profile dropdown/panel menus never had a Bize Ulaşın entry point to begin with (`site-header.tsx`'s middle nav section and `mobile-menu.tsx` only ever render account actions) — the footer link is and has always been the only one; the homepage no longer embeds `ContactSection` either (removed along with the modal-era cleanup), so `/bize-ulasin` is the form's single home.
- **`app/admin/iletisim-mesajlari`** (`ContactMessagesAdminPanel`) is a second, independent `/admin` module, now also gated server-side by `require-admin.ts#requireAdminOrRedirect` on top of its pre-existing client-side `session?.role === "admin"`/`AuthGateNotice` check (see "Admin panel modularization" below) — its own filter bar (status/subject/role/date) and inline status+note editing per message (`ContactMessageCard`) now fetch via `listAllContactMessagesForAdminRemote()` in a `useEffect`/`refreshKey` pattern (like `admin-jobs-list.tsx`), re-`refresh()`ing after a successful `reviewContactMessageRemote` call rather than reading a reactive store. `admin-nav.tsx#AdminNav`'s unread-message badge (`newContactMessageCount`) is computed the same way, from the same remote list — `AdminNav` itself now renders only on this page (its erstwhile other host, the old provider-document-review panel, is fully deleted — see "Admin panel modularization" below), so its stale "Hizmet Veren Belge Kontrolü" tab survives only here.
- **`whatsapp-support-button.tsx`** is an unrelated, always-on fixed "7/24 Canlı Destek" button added once at the root layout (`app/layout.tsx`), not per-page — a plain `wa.me` deep link (works identically on mobile/desktop, no platform branching needed). Pinned at `z-30`, deliberately below `mobile-menu.tsx`'s open-menu overlay (`z-40`/`z-50`), so it never competes with the mobile nav when both would otherwise occupy the same corner.
- **`use-focus-on-become-true.ts`** is a small reusable hook added alongside this feature but not specific to it — returns a ref that scroll-into-view-plus-focuses its element the moment a boolean flips from `false` to `true` (never on first render, so an already-`true` initial state on page load doesn't steal focus). Shared verbatim by `nakliye-location-fields.tsx` and the non-Nakliye location fields in `job-request-form.tsx`/`job-edit-form.tsx`, to auto-focus the manual facility-name input the instant "Listede yok, kendim gireceğim" is picked.

### Database scaffold

`app/_lib/db.ts` (lazy Neon client via `DATABASE_URL`) and `drizzle.config.ts` exist but are **not wired into any business logic** — their only consumer is `app/api/health/route.ts`, which runs `SELECT 1` and returns 503 if it fails or `DATABASE_URL` is unset. No `db/schema.ts` exists yet and no migrations have been generated. Every real feature still reads/writes `localStorage` exclusively (see "No real backend" above) — don't assume this scaffold means any given feature is backed by a real database.

### Future Supabase/PostgreSQL design (docs/database/, supabase/migrations/ — validated locally and against a real hosted dev Supabase project)

A separate, much more complete design pass exists for eventually replacing the whole localStorage/IndexedDB data layer with real Supabase/PostgreSQL. **This section is about the SQL schema/RLS/RPC design and its validation history — it predates, and is now partially superseded by, actual app-code wiring**: user auth/session is no longer just a design on paper, it runs on this schema today (see "Supabase Auth migration" below), and a few other features (provider services/profile/documents, legal/document consents) now dual-write into it too — as, more recently, does job creation itself (metadata/photos/Nakliye delivery fields), opt-in behind a feature flag; see "Supabase Geçişi Faz 2–4" below. Everything else described in this doc — offers, ratings, notifications, and so on — is still exactly what it says below: designed and SQL-validated, but not yet wired into any running feature. **A real hosted dev Supabase project has now been touched**: `0001`–`0021` were applied via `supabase link` + `supabase db push` and live-tested end-to-end against real GoTrue Auth/JWT/RLS/Storage flows (a separate task round from the local-only static review and Docker dry-run described below), which surfaced two critical runtime bugs that `0022` then fixed — see the validation-history paragraph below for both passes. This is still only a throwaway dev project (`trfnmpihcnriqgikglpu`) used for validation, not staging/production — no real user data has ever touched it. Start at [docs/database/architecture.md](docs/database/architecture.md) (indexes the other docs: schema reference, FK/relationship map, RLS matrix, RPC reference, index plan, storage plan, migration strategy, rollback strategy, test plan, admin permissions, subscriptions/quotas, payment readiness, and a documentation-only escrow design). The corresponding hand-written SQL for the core marketplace ("Faz 1") lives in `supabase/migrations/0001`–`0047` (extensions/helpers → service categories → profiles/provider catalog → operations/jobs/photos → offers+status history → ratings → provider documents/consents → legal consents → notifications → job activity/audit → indexes → RLS helpers → RLS policies → job RPCs → offer RPCs → document/notification/admin RPCs → views → scheduled jobs → storage policies → seed reference data → `contact_messages` (`0021`, added to mirror the app-side "Bize Ulaşın" table described below) → `0022`, a hosted-live-testing-only runtime fix: a `profiles` bootstrap trigger + `complete_registration` RPC, plus a `SELECT ... INTO` bare-alias bug across 8 offer-lifecycle RPCs — no schema or business-rule change → `0025`–`0027` (verification badges; job-customs-documents added then reverted — see "Provider verification badges" above) → `0028_job_product_fields.sql`, additive product-info columns on `jobs` (`product_quantity`/`product_tonnage`/`product_type`/`customs_product_type`, matching `types.ts#Job`'s existing localStorage fields 1:1 with the same value constraints, e.g. quantity ≤999999, text fields ≤100 chars) plus a backward-compatible optional client-supplied-id parameter (`p_client_id`/per-service `client_id`/`p_client_operation_id`, all `default null`) added to `create_job`/`create_operation_with_jobs` so a real-Supabase job-creation path could reuse the client's existing `crypto.randomUUID()` (`job-store.ts#createJob`/`createOperationId`) as the real Postgres primary key instead of inventing a reconciliation table — that groundwork didn't stay unused for long: `supabase-job-sync.ts` (added shortly after; see "Supabase Geçişi Faz 2–4" below) now calls it for real, opt-in behind a feature flag. `0028` itself deliberately scoped two field groups out: Gümrük Müşavirliği's "Operasyon Bilgileri" fields and Nakliye's 6 delivery-location fields (both already documented above as `types.ts#Job` fields) — Nakliye's were added three migrations later by `0031` (see below); Gümrük Müşavirliği's remain unrepresented in the Supabase schema to this day, still an explicit, documented scope boundary, not an oversight → `0029_facility_candidates.sql`, a brand-new, unrelated table pair (`facility_candidates`/`facility_candidate_raw_entries`) + 4 RPCs backing the admin panel's 7th module, "Sistem Beslemesi" (see "Admin panel modularization" above and "Supabase Geçişi Faz 2–4" below) → `0030_operation_service_province_override.sql`, a `create_operation_with_jobs` fix (per-service province override for a Nakliye sibling in a multi-service operation) → `0031_job_delivery_fields.sql`, the 6 Nakliye delivery columns → `0032`/`0033`, two same-day runtime bugfixes with no schema change (PostgREST couldn't resolve `create_job` to a single overload once `0028` and then `0031` each added parameters without matching the prior signature exactly — see "Supabase Geçişi Faz 2–4" below) → `0034_drop_stale_create_operation_with_jobs_overload.sql`, a same-day-pattern follow-up found during a technical-debt audit rather than new testing: `0032`/`0033` only ever audited `create_job`, leaving the identical defect live in its sibling RPC — `create_operation_with_jobs`'s own external signature was first widened back in `0028` (adding `p_client_operation_id`) and `0014`'s original 4-parameter overload was never dropped, so it stayed live alongside the 5-parameter one `0030`/`0031` correctly replaced — none of `0029`–`0034` touch or redefine anything from `0001`–`0028`) → `0035_job_moderation.sql`/`0037_job_admin_edit_field_parity.sql` (an admin approve/reject/edit workflow layered on `jobs`, orthogonal to the existing operational status — see "İlan Moderasyonu" above) → `0036_profile_edit_as_admin.sql` (`update_profile_as_admin`, letting an admin edit *any* profile's already-self-editable fields — see "Admin panel modularization" above) → `0038_service_authorization.sql`/`0039_request_provider_document.sql`/`0040_provider_documents_one_pending_per_service.sql` (the generalized per-category provider-authorization system that replaced ad hoc Nakliye/Gümrük isolation and the customs-only offer gate — see "Service Authorization" above) → `0041_auto_authorize_on_document_approval.sql` (links document approval to authorization — see "Service Authorization" above) → `0042_assert_active_user_enforcement.sql` (a central `assert_active_user()` enforcement helper, called from 41 mutation RPCs) → `0043_profile_fk_delete_lifecycle.sql` (classifies every `profiles(id)` foreign key's `ON DELETE` behavior) → `0044_provider_document_authorization_groups.sql` (two consolidated group documents replace several per-category ones) — see "Suspension enforcement, FK delete lifecycle, and document-group simplification" below for all three → `0045_recycling_service_category.sql`/`0046_recycling_job_fields.sql`/`0047_recycling_job_admin_edit_fields.sql` (a new "Geri Dönüşüm & Atık Tahliye" service category plus its 7 job fields and `update_job_as_admin` field parity — see the domain-model bullet above) → `0048_customs_transaction_type_and_requested_services.sql`, closing a schema gap in Gümrük Müşavirliği's own field group: of its 7 `types.ts#Job` fields, only `customsProductType` had ever been mirrored to Supabase (by `0028`) — `customsTransactionType`/`customsRequestedServices` (see "Gümrük Müşavirliği" above) were added as `jobs.customs_transaction_type`/`customs_requested_services` (with matching `CHECK` constraints against `customs-brokerage-catalog.ts`'s own constant lists), threaded through `create_job`/`create_operation_with_jobs`/`update_job_as_admin` following the exact `drop function if exists` → `create or replace` overload-safety discipline `0032`–`0034`/`0046`/`0047` already established; `customsDocuments` remains deliberately unmirrored (the `0027`-reverted `job_customs_documents` decision still stands, restated in `0048`'s own header) → `0049_deactivate_konteyner_dolum_bosaltim.sql`/`0050_fix_service_category_active_trigger_column.sql` (retires `konteyner-dolum-bosaltim` as a selectable service and adds a shared `ensure_service_category_active()` trigger blocking new records against any inactive category on `jobs`/`provider_services`/`provider_documents`/`provider_service_authorizations`; `0050` same-day-fixes a column-name bug in that trigger — see "Kritik İlan Senkronizasyonu & Development Temizliği" below) → `0051_requester_job_update.sql` (`update_job_as_requester`, the requester-facing counterpart to `0048`'s `update_job_as_admin` — same section below) — deliberately with **no** admin-RBAC (still one binary `admin` role, no permission levels), subscription, or payment tables. **Commit status is not monotonic with migration number**: `0001`–`0022`, `0025`–`0027`, and `0029`–`0034` are committed, but `0023`/`0024` (provider profile/document write paths, provider document lifecycle), `0028` (job product fields), and `0035`–`0051` (job moderation, admin profile-edit, admin job-edit field parity, service authorization, document-request notification, the one-pending-per-service index fix, the auto-authorize-on-approval fix, suspension enforcement, the FK delete-lifecycle audit, the document-group simplification, the Geri Dönüşüm & Atık Tahliye recycling category, the Gümrük Müşavirliği transaction-type/requested-services schema gap, the konteyner-dolum-bosaltim retirement plus its trigger fix, and the requester-facing job-update RPC) remain uncommitted working-tree state — see "Supabase Auth migration" above; `0025`–`0027` (verification badges, and a job-customs-documents feature added then fully reverted — see "Provider verification badges" above) and, later, `0029`–`0034` (Sistem Beslemesi, the job-creation sync, and its three overload-cleanup bugfixes — see "Supabase Geçişi Faz 2–4" below) were each committed on top of the still-uncommitted `0023`/`0024`/`0028` trio without depending on any of them being merged first — this repo has now done that repeatedly, so treat a commit/migration-number mismatch as the norm here, not a one-off. `supabase/seed.sql` (what `supabase/config.toml`'s `[db.seed] sql_paths` points at) is deliberately left almost empty rather than a second source of reference data — real seed data (service categories, etc.) already lives in `0020_seed_reference_data.sql`; the file exists only so `supabase db reset`/`start` doesn't fall back to an unexplained warning about a missing seed path. Those live as separate draft-only SQL in [docs/database/future-migrations/](docs/database/future-migrations/MANIFEST.md) instead ("Faz 2" admin RBAC + subscriptions/quotas in `phase2/`, "Faz 3" payments in `phase3/`) — deliberately kept outside `supabase/migrations/` rather than in a subfolder there, since some Supabase CLI versions scan subfolders too; nothing under `future-migrations/` can ever be auto-applied by `supabase db push`/`migration up`. The manifest itself (written when `0001`–`0020` were the latest Faz 1 migrations, and not since updated) describes the cutover steps — bootstrapping the first `super_admin`, swapping `get_active_job_limit()` for `get_effective_limit()`, re-enabling the offer daily-quota check — for whenever Faz 2/3 is actually greenlit; its stated renumbering starting point is now stale (`0021`–`0051` are all taken today, by `contact_messages`, the profile-bootstrap-and-offer-lifecycle fix, provider profile/document write paths, provider document lifecycle, customs-documents-and-verification-badges, job product fields, facility candidates, the job-creation-sync province-override fix, Nakliye delivery fields, three stale-overload bugfixes (two for `create_job`, one for `create_operation_with_jobs`), job moderation, admin profile-edit, admin job-edit field parity, service authorization, document-request notifications, the one-pending-per-service index fix, the auto-authorize-on-approval fix, suspension enforcement, the FK delete-lifecycle audit, the document-group simplification, the Geri Dönüşüm & Atık Tahliye recycling category, the Gümrük Müşavirliği transaction-type/requested-services schema gap, the konteyner-dolum-bosaltim retirement plus its trigger fix, and the requester-facing job-update RPC, so a real cutover would renumber starting at `0052`, not whatever number the manifest itself says). Key design decisions worth knowing before touching either tree: RLS + `SECURITY DEFINER` RPCs are the intended authorization boundary (no direct client `UPDATE`/`INSERT`/`DELETE` on status columns); notifications move from derived-on-read (today's `notifications.ts`) to a real persisted table with `event_key`-based idempotency; two places where the design brief's stated "current rule" didn't match the verified source code were resolved in favor of the actual code (job photo min/max is 1–10, not 4–15; a provider with no isolated category selected sees *every* job, per `job-visibility.ts`, not just matching categories) — both call-outs live in `architecture.md` §6. This is unrelated to the `drizzle`/`db.ts` scaffold above (different ORM/tooling, different status), and unrelated to any `data/locations/*.json` geo pipeline output.

**Validation history, in [docs/database/SUPABASE-MIGRATION-VALIDATION.md](docs/database/SUPABASE-MIGRATION-VALIDATION.md):** the design went through four passes. First, a line-by-line static review against `app/_lib/**` that found 5 issues that would break real user flows on first apply (`offers.estimated_duration` modeled as required free text vs. the app's actual optional 1–60 Nakliye-only number; missing `GRANT SELECT` on `profiles`/`provider_profiles`; `currency` CHECK missing `'EUR'`; `0020`'s category seed data reflecting a pre-merge/pre-removal version of `service-catalog.ts`; no write path for `legal_consents`/`provider_document_consents`) — all fixed directly in the migration files. Then a real `supabase db reset` against a fully local, isolated Docker + Supabase CLI stack (`npx supabase`, no remote project), which applied all 21 migrations cleanly, was re-run consecutively to confirm idempotency, and passed 9 negative security tests plus 1 positive control — this run caught 4 further execution-only bugs static review couldn't (a PL/pgSQL `SELECT...INTO` record-variable rule violation across 8 functions in `0015`; two ownership-restricted statements against `storage.buckets`/`storage.objects` in `0019`; and, most notably, a real privilege leak — missing `REVOKE ALL` left `anon`/`authenticated` with RLS-bypassing `TRUNCATE` on `jobs`/`profiles`/`provider_profiles`, `0003`/`0004`) — also fixed in-file and re-verified; still unvalidated by this local run were GoTrue's real signup/JWT flow and the Storage API's real HTTP upload flow, both outside the SQL/RPC layer that dry-run exercised. **A fourth pass then closed exactly that gap**: `0001`–`0021` were pushed to a real hosted dev Supabase project and exercised through the actual GoTrue signup/JWT flow, which surfaced two more execution-only bugs neither prior pass could reach — `public.profiles` had no bootstrap path at all (no `auth.users` trigger, no completion RPC; a real signup left `current_user_role()` permanently `NULL`, blocking every role-gated RPC), and all 8 offer-lifecycle RPCs (`accept_offer` through `submit_rating`) threw a runtime `22P02` on every real invocation from a `select o into v_offer from public.offers o where o.id = ...` bare-table-alias pattern that `0015` had only ever fixed at CREATE-time (the function compiled cleanly; it had simply never been called end-to-end before). `0022` fixed both — a `SECURITY DEFINER` `handle_new_auth_user()` trigger + `complete_registration()` RPC for the first, swapping all 8 RPCs to the already-proven `select * into v_offer from public.offers where id = ...` pattern for the second — and was re-verified against both the local Docker stack and the same hosted project end-to-end (full registration flow plus 6 independent offer-lifecycle chains). One finding from this pass is deliberately left open, not patched: on this hosted project `service_role` itself has no REST access to any `public`-schema table or function (a platform/project-level gap, not a migration bug) — the task brief explicitly forbade papering over it with a blanket `GRANT ALL`, so `0022`'s own file header documents it as out of scope rather than fixing it blindly. Full account of all four passes in `SUPABASE-MIGRATION-VALIDATION.md` (§25 for the fourth) — the doc's validation history stops at `0022`; `0025`–`0027` (verification badges, and the reverted job-customs-documents attempt) have no corresponding numbered section here, and are instead guarded by their own permanent Docker-only regression test, `scripts/test-customs-documents-and-badges.mjs` (see "Commands" above). `0028`–`0034` (job product fields, facility candidates, the job-creation sync, Nakliye delivery fields, and their overload bugfixes) likewise post-date this doc and have no numbered section here — `0032`/`0033` exist precisely *because* real end-to-end/RPC testing (not this doc's own validation passes) caught what static review of `0028`/`0031` couldn't: two stale `create_job` overloads left live by `create or replace function`'s exact-signature-match requirement (see "Supabase Geçişi Faz 2–4" below); `0034` then found the same defect's sibling instance in `create_operation_with_jobs` via a direct `pg_proc` catalog audit rather than new RPC testing. `0035`–`0041` (job moderation, admin profile/job edit RPCs, the generalized service-authorization system, and the auto-authorize-on-approval fix — see "İlan Moderasyonu"/"Admin panel modularization"/"Service Authorization" above) likewise have no numbered section here; their own `tmp-supabase-job-moderation-*`/`tmp-supabase-admin-*`/`tmp-supabase-service-authorization-test.mjs`/`tmp-admin-panel-*.mjs` scripts (see "Commands" above) are the only verification they've had. `0042`–`0044` (suspension enforcement, the FK delete-lifecycle audit, and the document-group simplification — see "Suspension enforcement, FK delete lifecycle, and document-group simplification" below) postdate this doc too, verified only by their own `tmp-supabase-suspend-enforcement-*`/`tmp-suspend-enforcement-browser-test.mjs`/`tmp-supabase-fk-lifecycle-local-test.mjs`/`tmp-supabase-document-group-*-test.mjs` scripts. `0045`–`0047` (the Geri Dönüşüm & Atık Tahliye recycling category — see the domain-model bullet above) likewise postdate this doc, verified only by their own `tmp-supabase-recycling-service-local-test.mjs`/`tmp-supabase-recycling-catalog-picker-browser-test.mjs`/`tmp-supabase-recycling-admin-and-profile-browser-test.mjs`/`tmp-supabase-recycling-test-provider-parity-test.mjs`/`tmp-supabase-recycling-test-provider-browser-test.mjs`/`tmp-supabase-seed-recycling-test-provider.mjs` scripts. `0048` (the Gümrük Müşavirliği transaction-type/requested-services schema gap) likewise postdates this doc and has no dedicated regression script of its own yet. `0049`–`0051` (the konteyner-dolum-bosaltim retirement and its trigger fix, and the requester-facing job-update RPC — see "Kritik İlan Senkronizasyonu & Development Temizliği" below) postdate this doc too and have no dedicated regression script of their own found in the repo yet. None of `0028`–`0051` has a dedicated regression test of its own yet beyond those one-off scripts.

### Suspension enforcement, FK delete lifecycle, and document-group simplification (0042–0044, uncommitted)

Three more migrations, none reflected elsewhere in this doc yet — each fixes a gap this doc previously called out as a *known, deliberately accepted* limitation, so read them together with whatever they supersede rather than as a bolt-on.

- **`0042_assert_active_user_enforcement.sql`** closes the gap the "Supabase Auth migration" section above used to describe as open: `account_status` suspension (`suspend_user`/`reinstate_user`, `0016`) previously only ever set a flag nothing read — a suspended user's still-valid JWT could call any mutation RPC directly and it would succeed. `assert_active_user()` (`SECURITY DEFINER`, same owner-to-owner-only pattern as `is_admin()`) is now the first statement in 41 mutation RPCs — raises `ML125` (no `auth.uid()`), `ML126` (no matching `profiles` row), or `ML127` (`account_status <> 'active'`). Deliberately scoped to *mutations* only: no RLS `SELECT` policy or read-helper function calls it, so a suspended user's own data is still readable exactly as before. Four categories of RPC are deliberately excluded (documented in the migration's own header): `record_legal_consent`/`submit_contact_message` (must work signed-out), `complete_registration` (a suspended account can't exist before registration finishes), local-UI-state RPCs (`mark_notification_read`, `dismiss_notification`, `record_job_viewed`), and `submit_facility_candidate_entry` (already fire-and-forget/best-effort). `app/_lib/supabase-mutation-errors.ts#getAccountStatusErrorMessage()` (new file) is the one shared ML125/126/127-to-Turkish mapper — every RPC-error `switch` (`admin-companies.ts`, `admin-requesters.ts`, `supabase-provider-documents.ts`, `supabase-provider-document-review.ts`, `supabase-provider-profile.ts`, ...) delegates to it before falling back to its own default case, rather than each re-implementing the same three messages.
- **`0043_profile_fk_delete_lifecycle.sql`** is a direct follow-on, found while validating `0042`: almost none of the ~32 foreign keys referencing `profiles(id)` had an explicit `ON DELETE` behavior (Postgres default `NO ACTION`), so hard-deleting any account with real activity (a job, an offer, an audit-log row, ...) failed with `23503` — the same root cause a prior audit had already surfaced as GoTrue `AuthRetryableFetchError`s on account deletion. Fixed with per-column judgment, not a blanket `CASCADE` (explicitly ruled out — it would let deleting a requester silently wipe every provider's offer/rating history on that requester's jobs): **group 1** (unchanged, still blocks deletion) — real content-ownership columns with a counterparty stake, e.g. `jobs.requester_id`, `offers.provider_id`, `ratings.provider_id`/`rater_id`, `provider_documents.provider_id`; **group 2** (`ON DELETE SET NULL`, 17 columns) — "who did this" attribution on a record that has its own independent meaning (`audit_logs.actor_id`, `notifications.actor_id`, `offer_status_history.changed_by`, `jobs.moderation_reviewed_by`, `legal_consents.user_id`, ...) — three of these (`provider_badges.granted_by`, `provider_document_reviews.admin_id`, `provider_service_authorizations.authorized_by`) were `NOT NULL` and had to be relaxed first; **group 3** (`ON DELETE CASCADE`, 3 columns) — rows whose *only* meaning is belonging to that one user (`notifications.recipient_id`, `recently_viewed_jobs.user_id`, `provider_document_consents.provider_id`). A group-1 account still can't be hard-deleted after this migration — deliberate, per the migration's own note; cleanup still means manually deleting dependent rows in order (see `scripts/tmp-supabase-suspend-enforcement-dev-test.mjs` for that order), just a shorter list than before.
- **`0044_provider_document_authorization_groups.sql`** simplifies the *provider-authorization* upload flow only — the job-posting category taxonomy (`service-catalog.ts#SERVICE_CATEGORY_GROUPS`, all 20 subcategories) is untouched. Two new `document_type` values, `operator-is-makinesi-belgesi` (covers İş Makinesi Hizmetleri ∪ Operatör Hizmetleri — forklift/reach-stacker/vinç/manlift and their operator variants) and `depo-hizmetleri-belgesi` (covers all 12 Depo Hizmetleri subcategories), let a provider upload **one** document to get authorized for a whole group instead of one document per subcategory. The category lists are hardcoded in the migration and must be kept in sync by hand with the single source of truth, `app/_lib/service-catalog.ts#PROVIDER_AUTHORIZATION_GROUPS` (PL/pgSQL can't import TypeScript) — Liman Hizmetleri, Nakliye, Gümrük Müşavirliği, and Diğer Hizmetler are deliberately excluded from grouping and still require one document per category. `review_provider_document`'s existing auto-authorize-on-approval chain (`0041`) gained two new branches that loop over `authorize_provider_service` once per category in the group, rather than a second authorization mechanism. Not retroactive: a provider already approved for a single subcategory (e.g. just `kapali-depolama`) is **not** auto-expanded to the whole group — that would grant an unproven capability — they'd need to upload the new group document to get the wider grant. `document-upload-content.tsx` (`/panel/belge-yukleme`) now offers both individual categories and, where applicable, a group option (`PROVIDER_AUTHORIZATION_GROUPS`) side by side; picking a group sends `serviceCategoryId: undefined` (matching `create_provider_document`'s NULL-category contract for group docs) and disables re-uploading once that `document_type` already exists for the provider.

### Supabase Geçişi Faz 2–4 (job creation now dual-writes, opt-in via feature flag)

`0028`'s client-supplied-id parameters (see "Future Supabase/PostgreSQL design" above) turned out to predict real, later work: three follow-up phases actually wired job creation into Supabase, each its own migration + commit, all already **committed** on this branch (unlike `0023`/`0024`/`0028` themselves, which remain uncommitted — see that section's "Commit status is not monotonic" note).

- **Faz 2 — real job creation** (`0030_operation_service_province_override.sql`, `app/_lib/supabase-job-sync.ts`): `job-request-form.tsx` calls `syncJobToSupabase(job)` / `syncOperationToSupabase(jobs, operationId, province, operationDetails)` right after the authoritative localStorage write (`createJob`/`createJobsForOperation`) already succeeded — gated entirely by `isSupabaseJobSyncEnabled()` (`NEXT_PUBLIC_ENABLE_SUPABASE_JOB_SYNC === "true"`, default off/fail-closed, documented in `.env.example`). Off, this code path never runs and behavior is byte-for-byte the pre-existing localStorage-only flow. On, it's still best-effort and non-blocking: the local job/operation is never rolled back or deleted on a remote failure and navigation is never blocked — the only user-visible trace is a `?senkronUyarisi=1` query param (read by `job-detail-content.tsx`/`job-requests-panel.tsx`) plus a `console.error` with no Supabase-internal details. It reuses the existing `job.id`/`operationId` (`crypto.randomUUID()`, already generated by `job-store.ts`) as `create_job`/`create_operation_with_jobs`'s `p_client_id`/`p_client_operation_id`, so local and remote records share one identity with no separate reconciliation table needed. `auth.uid()` (never a client-passed user id) is the RPCs' only source of requester identity, so a caller can never create a job as another user. `0030` fixed a real field-loss bug found by reading the source, not assumed: a multi-service operation's per-service province override (only ever non-empty for a Nakliye sibling — see "Nakliye Güzergâh Yönetimi") was silently dropped by `0028`'s original `create_operation_with_jobs`, which only ever wrote the shared `p_province`; the fixed RPC now reads an optional per-service `province` key from `p_services`, and `syncOperationToSupabase` only includes that key when a service's province genuinely differs from the operation's shared one — matching `job-store.ts#createJobsForOperation`'s own priority exactly.
- **Faz 3 — real photo Storage sync + "Sistem Beslemesi"** (`0029_facility_candidates.sql`, `app/_lib/supabase-job-photos.ts`, `app/_lib/supabase-facility-candidates.ts`): job photos are uploaded one at a time to the existing `job-photos` bucket (RLS/policies already existed from `0019` — no new bucket) at `job-photos/{requester_id}/{job_id}/{photo_id}.{ext}`, *before* `create_job`/`create_operation_with_jobs` is ever called; any failure (missing IndexedDB blob, Storage rejection) rolls back every already-uploaded photo for that call — and, for an operation, every sibling service's already-uploaded photos too — before returning, so the RPC is never invoked with a partial photo set and Storage never accumulates orphans (same "upload first, register second, roll back on failure" principle as `supabase-provider-documents.ts`). Separately, and **unconditional — not gated by the sync flag**: any custom/free-text facility name a user types (`locationMode: "custom"`, pickup or delivery side) is fire-and-forget reported to the `submit_facility_candidate_entry` RPC via `submitFacilityCandidateBestEffort()` — this never throws, is never awaited by the caller, and never surfaces any UI state. This is what feeds `/admin/sistem-beslemesi` (see "Admin panel modularization" above); reads/writes are entirely separate concerns (submission happens on every job creation with a custom facility name regardless of the flag; review happens only in the admin module).
- **Faz 4 — Nakliye delivery-field sync** (`0031_job_delivery_fields.sql`): adds the 6 nullable `delivery_*` columns to `jobs` (no `CHECK`, mirroring `job-store.ts#resolveDeliveryLocationFields`'s own client-side-only validation) and threads them through both RPCs; `supabase-job-sync.ts` now passes `job.deliveryProvince`/etc. alongside everything else — always `null` for a non-Nakliye job, per `job-store.ts`'s own active-clearing rule, so this module never re-checks category scope itself. **`0032`/`0033` are same-day runtime bugfixes, not schema changes** — `create or replace function` only truly replaces a function when the new definition has an *identical* parameter signature; both `0028` and (independently) `0031` added new parameters instead of matching the prior overload exactly, so each time Postgres kept the narrower prior `create_job` alive as a second live overload. Once more than one candidate existed, PostgREST could no longer resolve a call (`PGRST203 "Could not choose the best candidate function"`) for any caller not passing the full new parameter set. `0032` found and dropped the stale 21-parameter overload left behind by `0028` (surfaced by real hosted-dev RPC testing during this same Faz 4 task — `supabase-job-sync.ts` itself was never affected, since it already sent the full parameter set and resolved to a single candidate). `0033` then found and dropped the *original* 16-parameter overload left behind all the way back at `0014` (surfaced separately, during a full-app end-to-end regression pass, by an old seed script — `scripts/tmp-admin-panel-phase2-seed-local.mjs` — that still called `create_job` with only 16 arguments). `create_job` now has exactly one live overload, 27 parameters — the same "a prior migration's own claim turned out wrong, a later migration fixes it without editing the original" pattern already established by `0022` (see the validation-history paragraph above) and `0030`. **`0034_drop_stale_create_operation_with_jobs_overload.sql`** repeats this exact fix for `create_operation_with_jobs`'s own sibling defect, found separately during a technical-debt audit (a direct `pg_proc` catalog query plus a real RPC call, not new feature testing): `0032`/`0033` had only ever covered `create_job`, so `0028`'s earlier widening of `create_operation_with_jobs`'s signature (adding `p_client_operation_id`) had left `0014`'s original 4-parameter overload live the whole time, uncaught because `0030`/`0031` both happened to match the 5-parameter signature exactly and so never triggered a second overload themselves. `create_operation_with_jobs` now also has exactly one live overload, 5 parameters.

None of this changes the still-accurate claim elsewhere in this doc that every *other* feature (offers, ratings, notifications, ...) is localStorage-only — see "No real backend" above. Only job creation's own metadata/photos/delivery fields dual-write, and only when `NEXT_PUBLIC_ENABLE_SUPABASE_JOB_SYNC=true`; facility-candidate submission is the one sub-piece that's unconditional.

### Kritik İlan Senkronizasyonu & Development Temizliği (job-sync reliability, demo accounts, retired categories — uncommitted)

Two more uncommitted, separately-named tasks (migrations `0049`–`0051`, no matching numbered section in `SUPABASE-MIGRATION-VALIDATION.md`), both touching `use-jobs.ts`'s job-merging logic and both about making the localStorage/Supabase split above trustworthy rather than best-effort-and-forget.

- **Persistent sync-failure tracking replaces the old, page-refresh-losable `?senkronUyarisi=1` signal as the real source of truth.** `Job.supabaseSyncFailedAt?`/`supabaseSyncError?` (`types.ts`) are new fields `job-store.ts#markJobSupabaseSyncFailed`/`clearJobSupabaseSyncFailure` write — set by `job-request-form.tsx` (both the single-job and multi-service-operation creation paths; for an operation, since `create_operation_with_jobs` is one atomic RPC, every sibling job is marked individually on a shared failure) and `job-edit-form.tsx` (only when editing a still-`pending_review` job) whenever the best-effort Supabase dual-write (still gated by `NEXT_PUBLIC_ENABLE_SUPABASE_JOB_SYNC`) fails *after* the authoritative localStorage write already succeeded. While set, `job-detail-content.tsx` and `job-requests-panel.tsx` show a persistent, page-refresh-surviving danger banner ("Senkronizasyon Başarısız" — this listing only exists on this device, isn't in the admin queue, and isn't reachable from another device) instead of a normal moderation-status badge — `job-requests-panel.tsx`'s status badge explicitly checks this *before* `moderationStatus`, since a job that never reached the server must never claim to be "Admin Onayı Bekleniyor" — plus a "Yeniden Dene" retry button (disabled mid-retry, with its own inline retry-failure message). The old `?senkronUyarisi=1` query param is still appended on the immediate post-creation redirect and still renders a lighter, non-danger "senkron deneniyor" notice when `!syncFailed`, but only as a same-request-cycle fallback for the rare case the persistent-field write itself failed — `supabaseSyncFailedAt` is the durable source of truth from then on.
- **`supabase-job-sync.ts#retryJobSupabaseSync(job)`** checks whether the job's id already exists as a Supabase row: if not, re-runs the original create (`syncJobToSupabase`); if it does, calls the new `update_job_as_requester` RPC (migration `0051`, the requester-facing counterpart to `update_job_as_admin`/`0048` — same field scope/`coalesce` discipline, gated on `auth.uid() = requester_id` and `moderation_status = 'pending_review'`, rejecting with `ML131` otherwise, and never touching category/`facility_id`/`location_mode`/delivery-facility-id/`location_mode`/photos/`customsDocuments` — same deliberate scope-out as `0048`) instead of re-inserting. This closes a real, previously-documented gap: before `0051`, a requester's own edit to a still-pending listing never reached Supabase at all (`supabase-job-sync.ts` only ever called `create_job`/`create_operation_with_jobs`) — which is exactly why `use-jobs.ts#remoteWinsOverLocal` (below) has to defer to the local copy while a job is still pending; that asymmetry is now closed specifically for the retry path, though a normal (non-retry) edit still doesn't proactively push to Supabase on its own.
- **`reconcileLocalPendingJobsWithSupabase(jobs)`** (called once per mount from `job-requests-panel.tsx`, keyed only on `session.id` to avoid a re-trigger loop — `markJobSupabaseSyncFailed`'s own write already flows back through the same `useSyncExternalStore` store `useAllJobs()` subscribes to, so the effect body itself holds no local state) is a one-time backfill for jobs created *before* `supabaseSyncFailedAt` existed as a field: on every "Hizmet Taleplerim" load, it batch-checks the caller's own still-`pending_review` jobs that carry no known failure flag against Supabase, and retroactively marks any that don't actually exist there — without this, such a job would show a false "Admin Onayı Bekleniyor" forever. No-ops entirely when the sync flag is off; swallows query errors silently rather than false-flagging on a transient network blip.
- **Central data fallback ("Merkezi Veri Zorunluluğu," Development Temizliği Faz 5)**: `app/_lib/supabase-job-reads.ts#fetchAllVisibleJobsFromSupabase` is read via `use-jobs.ts#useRemoteJobsFallback` (a mount-once effect, only when the sync flag is on) and merged into `useAllJobs()` — this is what lets a job created on a *different* browser/account/device show up here too, since such a job has no localStorage row on this one to begin with. `useAllJobs()#remoteWinsOverLocal(remote)` decides, only when a job exists in *both* places, which copy wins: the remote copy wins only once an admin has made a real decision (`moderationStatus` is no longer `"pending_review"`, or `closedAt` is set) — otherwise the local copy wins, so an owner's own fresh edit is never shadowed by a stale/un-synced remote row. `fetchJobByIdFromSupabase` (single-job fetch, same file) is exported but currently has **no callers anywhere in the app** — `useJobById` is served by the merged `useAllJobs()` list instead, so this function is presently dead code, kept the same "documented but unused" way a few other functions in this doc are (e.g. `jobHasAcceptedOffer`, above).
- **`admin-jobs.ts` gained two bug fixes found during this same audit**: `listJobsForAdmin` used to let a genuine Supabase query error fall through as an empty array, indistinguishable from "zero pending jobs" — it now throws, so the admin UI shows a real error state instead of a fake all-clear. `AdminJobDetail` never included Gümrük Müşavirliği's `customsTransactionType`/`customsRequestedServices` (unlike `customsProductType`, which was already there) — both are now selected/mapped, closing that gap.
- **"Development Temizliği" separately retired `konteyner-dolum-bosaltim`** ("Konteyner Dolum / Boşaltım," itself already a merge of two older categories — see "Service catalog" below) as a selectable service entirely (migration `0049`, Faz 1, part of a task titled "Development Temizliği, 16 Demo Hizmet Veren Hesabı ve Konteyner Hizmetinin Kaldırılması") — active service count is now 16. `service-catalog.ts#REMOVED_CATEGORY_IDS` gained the id (existing rows are never deleted, only hidden from the catalog going forward — same non-destructive pattern as every other removed category), and `resolveServiceCategoryId` now also re-validates a merge-alias's *target* against `isServiceCategoryId`, so the old `"konteyner-dolum"`/`"konteyner-bosaltim"` aliases can no longer resolve to a now-removed id. Because `service_categories.is_active` had never actually been enforced anywhere in the schema (`0002`'s own header said as much — the code-side catalog was the real source of truth), `0049` also added a shared trigger, `ensure_service_category_active()`, blocking new INSERTs/category-column UPDATEs against an inactive category on `jobs`/`provider_services`/`provider_documents`/`provider_service_authorizations` directly at the database level (even bypassing an RPC entirely — raises `ML128`); `0050` is a same-day fix for a real column-name bug in that trigger (it read `category_id` for every table instead of `service_category_id` on everything but `jobs`, breaking every new service selection on `provider_services` with a `42703` until fixed).
- **A new dev-only login helper, `demo-accounts-panel.tsx` + `use-local-dev-host.ts`, is the actual replacement for the deleted `DEV_ACCOUNTS` seed** described under "No real backend" above — a completely separate mechanism, not a re-wiring of the old one. A collapsible "Demo Hesaplar" panel on the login screen lists 17 real Supabase Auth accounts (`*@demo.test` — one `hizmet-alan` requester plus one `hizmet-veren` provider per major service category, covering all 16 active services) and, on click, only *fills* `login-form.tsx`'s existing email/password fields — the real submit still goes through `supabase.auth.signInWithPassword`; this is not a second auth path. It's double-gated, deliberately not trusting `NODE_ENV` alone (a developer could run `next dev` on a public host): a build-time `process.env.NODE_ENV === "development"` check at the render call site in `login-form.tsx` strips the component/account list/password from the production bundle entirely, and a runtime `use-local-dev-host.ts#useIsLocalDevHost()` (`useSyncExternalStore` over `window.location.hostname`, same pattern as `use-media-query.ts`) additionally requires the browser to actually be on `localhost`/`127.0.0.1`. The shared password comes from `NEXT_PUBLIC_DEMO_ACCOUNT_PASSWORD` (`.env.example`), never hardcoded. **Known gap**: nothing in the repo actually creates these 17 `@demo.test` Supabase accounts — no migration, seed script, or `tmp-setup-*.mjs` script references `@demo.test` — so they must already exist in whichever Supabase project `.env.local` points at, or be created manually, before the panel is useful for anything beyond form-filling.

### Design tokens

Brand colors/spacing live as CSS custom properties in `app/globals.css` (`:root` raw values → `@theme inline` Tailwind v4 token mapping, e.g. `--color-primary`, `--color-accent`, `--color-success`, `--radius-card`). Add new colors there rather than hardcoding hex in components.

`page-container.tsx#PageContainer` (uncommitted) is the single shared source for page/section width, replacing 45 files' worth of hand-written `mx-auto max-w-X px-4 ...` wrappers that had drifted to 68 different `max-w-` values (most commonly a flat `max-w-7xl`/1280px regardless of viewport) — a desktop width/density fix, not a new layout system: `guest-access-card.tsx#PageCardShell` (the pre-existing equivalent for form-card pages) is its spiritual sibling, not a competing pattern. Three new `--container-page-{sm,md,lg}` tokens in `globals.css` (75rem/1200px, 85rem/1360px, 96rem/1536px) back its two `size` variants: `"default"` (most pages) adds no max-width at all below the `lg` breakpoint — matching every page's pre-existing mobile/tablet behavior exactly, since a `max-w-7xl` never bound below `lg` anyway — then steps through all three tokens at `lg`/`xl`/`2xl`; `"form"` (profile/settings-style read-width content) keeps the older `max-w-3xl` (768px) base and only ever widens to `max-w-page-sm` at `lg`, deliberately not following `"default"`'s further steps since wide form rows hurt readability. Vertical padding (`py-*`) is deliberately left to each call site rather than folded in, since pages already use inconsistent vertical rhythms (`py-16` vs `sm:py-20` vs `pt-4 pb-16`) that this component wasn't scoped to unify.
